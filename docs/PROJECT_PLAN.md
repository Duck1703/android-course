# PROJECT PLAN — Web học Android bằng tiếng Việt

> Đây là **nguồn sự thật về tiến độ** của dự án. Đọc file này trước khi trả lời `review plan`, `đang tới đâu rồi?`, `tiếp tục`, v.v. — xem quy tắc xử lý trong `CLAUDE.md`.

**Cập nhật lần cuối:** 2026-08-27 — sau khi hoàn tất Task 29 (Chapter 8), Task 30 (Chapter 1 + 2), Task 31 (README cho người non-tech) và Task 32 (cleanup rủi ro tồn đọng). **Phase 3c đóng → đã audit sâu đủ 11/11 chapter.** Phase 4: Task 31 + 32 xong; còn lại 2 việc môi trường cần user quyết (nâng Node, `git init`).

## Trạng thái hiện tại

- **Phase:** 4 — Hoàn thiện để dùng được. Phase 3c **hoàn tất**: đã audit sâu độ chính xác nội dung cho **11/11 chapter** (Phase 3b: 3/4/6/7/9/10/11 · Phase 3c: 5, 8, 1+2). Website chạy được; cả 11/11 chapter đều render lesson + quiz đầy đủ; đã có README cho người non-tech; `web/` đã sạch file rác.
- **Task hiện tại:** Task 32 — cleanup rủi ro tồn đọng → **hoàn thành**. Trước đó Task 30 (Chapter 1+2) và Task 31 (README) cũng hoàn thành. Tổng kết cả đợt audit: **11/11 chapter, sửa/bổ sung 28 điểm** (27 từ Phase 3b/3c + 1 điểm salvage quiz Chapter 3 ở Task 32). Phân bố lỗi: Ch05 6 · Ch08 4 (0 lỗi citation) · Ch01+02 4 · Ch11 3 · Ch09 3 · Ch04 3 · Ch03 3 · Ch10 2 · Ch06 1 · Ch07 0. Rút ra: chapter viết trước Task 21 (Ch05/Ch08) và chapter hướng dẫn bấm chuột trong IDE (Ch02) là 2 nhóm rủi ro cao nhất; citation lệch dòng gần như chỉ xuất hiện ở nhóm viết trước quy trình kiểm dòng.
- **Task tiếp theo:** Không còn task nội dung/website nào trong roadmap. Chỉ còn 2 việc môi trường **cần user quyết định**: (1) nâng Node lên >= 22.19.0; (2) `git init` ở root. Cả 2 đều không chặn việc dùng website.
- **Blocker:** Không có.

## Roadmap & trạng thái

Ký hiệu: `[x]` đã xong · `[~]` đang làm · `[ ]` chưa làm.

### Phase 1 — Xử lý nguồn (không đụng Android code, chưa có website UI)

- `[x]` **Task 0 — READ-ONLY AUDIT** (khảo sát EPUB, Android project, tool/skill hiện có, đề xuất roadmap). Hoàn thành 2026-08-26. Kết quả chi tiết → `docs/SOURCE_MAP.md`.
- `[x]` **Task 1 — Thiết lập Project Memory** (`CLAUDE.md` + `docs/PROJECT_PLAN.md` + `docs/SOURCE_MAP.md`). Hoàn thành 2026-08-26.
- `[x]` **Task 2 — Script trích xuất EPUB → Markdown.** Hoàn thành 2026-08-26. Script: `scripts/extract_epub.py` (chỉ dùng `zipfile` + `xml.etree.ElementTree`, không dependency ngoài). Đã test trên chapter 1, 5 (yêu cầu ban đầu) + chapter 8 (test thêm riêng phần "Note" callout box). Trong lúc test phát hiện + sửa 1 lỗi: các cụm `<em>` liên tiếp sát nhau trong sách (dùng để in nghiêng tên file như `MainActivity.kt`) bị ghép sai thành `**` — đã sửa bằng cách gộp các thẻ in nghiêng/đậm liền kề trước khi xuất Markdown. Đã verify số lượng code-block trong Markdown khớp 1:1 với số `<pre class="code-block">` trong EPUB gốc (ch1: 1/1, ch5: 27/27). File output test hiện có trong `content/book/`: `ch01-welcome-to-android-kotlin.md`, `ch05-jetpack-compose.md`, `ch08-networking.md`.
- `[x]` **Task 3 — Chạy trích xuất toàn bộ.** Hoàn thành 2026-08-26. Chạy `python scripts/extract_epub.py --all` → sinh đủ **23/23** file Markdown trong `content/book/` (11 chapter + 12 phần front/back matter: giới thiệu, license, dedications, about the team, 3 section header, conclusion...), khớp đúng số mục trong `toc.xhtml`. **Không có cảnh báo** "the la ... chua duoc xu ly" nào ở stderr trên toàn bộ sách (nghĩa là mọi loại thẻ HTML trong sách đều đã được xử lý đúng, không có nội dung bị rơi rớt/xử lý sai). Số dòng mỗi file từ 9 đến 941 dòng — hợp lý theo độ dài gốc, không có dấu hiệu bị cắt cụt. Đã kiểm tra thủ công 3 file đại diện (License — văn bản pháp lý dài, About the Team — có kiểu trình bày đặc biệt "author-figure" chưa test trước đó, Room Database — nhiều annotation code như `@Entity`/`@Dao`/`@Query`) → đều đúng định dạng. Đã quét lại toàn bộ 23 file để đảm bảo lỗi ghép `**` phát hiện ở Task 2 không còn xuất hiện ở đâu khác — không thấy lỗi nào.
- `[x]` **Task 4 — Tạo `docs/LEARNING_MAP.md`.** Hoàn thành 2026-08-26. Mapping đầy đủ 11 chapter: Concept → File Android tham chiếu → Composable/Class minh hoạ → gợi ý Exercise/Quiz. Trong lúc làm task này phát hiện vấn đề bản quyền nghiêm trọng — xem mục "Vấn đề/rủi ro" bên dưới (BLOCKER cho Task 7).

### Phase 2 — Website skeleton + vertical slice

- `[x]` **Task 5 — Khởi tạo Astro project** trong `web/`. Hoàn thành 2026-08-26. `npm create astro@latest` (template minimal, TypeScript strict). Astro 7.2.7. Gặp và sửa 1 bug môi trường: npm trên máy này không tự cài native binding cho Windows (`npm/cli#4828`) — thiếu `@astrojs/compiler-binding-win32-x64-msvc`, `@rolldown/binding-win32-x64-msvc`, `lightningcss-win32-x64-msvc` khiến `astro dev` báo lỗi "Cannot find native binding". Đã sửa bằng cách khai báo rõ 3 package này trong `dependencies` của `web/package.json` (không phải optionalDependencies — đã thử và vẫn bị skip). Verify bằng cách xoá sạch `node_modules`+`package-lock.json` rồi `npm install` lại từ đầu 2 lần, `astro dev` chạy được, `curl http://localhost:4321/` trả về HTTP 200 mỗi lần.
- `[x]` **Task 6 — Layout cơ bản.** Hoàn thành 2026-08-26. Tạo `web/src/data/chapters.ts` (dữ liệu 11 chapter + 3 section dùng chung), `web/src/layouts/BaseLayout.astro` (sidebar cố định bên trái, 3 nhóm section, highlight chapter đang xem), `web/src/pages/index.astro` (trang chủ giới thiệu + card 11 chapter), `web/src/pages/chapters/[slug].astro` (trang placeholder cho mỗi chapter — chỉ có tóm tắt tiếng Việt tự viết + đường dẫn tham chiếu nguồn, CHƯA có code/nội dung lesson thật vì đang chờ quyết định license ở Task 7). Verify: `npm run dev` + `curl` tất cả 11 route `/chapters/chNN-.../` → HTTP 200, route giả `/chapters/ch99-fake/` → đúng HTTP 404; `astro build` sinh đủ 12 trang tĩnh (1 trang chủ + 11 chapter) không lỗi.
- `[x]` **Task 7 — Lesson hoàn chỉnh đầu tiên** (Chapter 5 — Jetpack Compose). Hoàn thành 2026-08-26. Tạo `web/src/components/lessons/Ch05JetpackCompose.astro`: 6 mục (Composable function, Layout Group Column/Row/Box/Surface, Modifier chaining, LazyColumn, `@Preview`, dữ liệu giả `FakeData`), mỗi mục có giải thích tiếng Việt tự viết (diễn giải theo `content/book/ch05-jetpack-compose.md`, không copy nguyên văn sách) + đoạn code thật trích từ `aaf-materials/05-jetpack-compose/projects/final/...` (ghi rõ file + số dòng ở mỗi đoạn: `MainActivity.kt` 44-55, `Conversation.kt` 103-119/146-183/208-219, `KodecochatAppBar.kt` 82-88, `FakeData.kt` 43-75) kèm syntax highlighting bằng component `<Code>` có sẵn của Astro (`astro:components`). Gắn vào `web/src/pages/chapters/[slug].astro` — chỉ render lesson đầy đủ khi `slug === "ch05-jetpack-compose"`, các chapter khác vẫn giữ placeholder cũ (nói rõ có thể xem ví dụ ở chapter 5). Dùng code thật theo Quyết định #7 (không sửa file gốc trong `aaf-materials/`).
- `[x]` **Task 8 — Quiz mẫu + bài tập nhỏ** cho lesson Chapter 5. Hoàn thành 2026-08-26. Tạo `web/src/components/lessons/Ch05Quiz.astro`: 3 câu quiz trắc nghiệm (đúng theo gợi ý trong `docs/LEARNING_MAP.md` — `@Composable`, `@Preview` dùng để làm gì, vì sao dùng `LazyColumn`), chấm điểm bằng vanilla JavaScript phía client (không dùng framework, đúng định hướng "gần như 0 JS trừ chỗ cần tương tác" đã chốt cho Astro), hiện giải thích đáp án sau khi nộp. Bài tập nhỏ: hướng dẫn tự đổi `Modifier.padding(horizontal = 16.dp)` trong `MessageUi` (Conversation.kt) và quan sát qua `@Preview`, có `<details>` gợi ý nếu cần.
- `[x]` **Task 9 — Progress tracking local.** Hoàn thành 2026-08-26. Tạo `web/src/lib/progress.ts` (đọc/ghi `localStorage`, key `hoc-android-tv:progress`, mảng slug đã học). Nút đánh dấu `web/src/components/ProgressToggle.astro` gắn ở đầu mỗi trang chapter. Sidebar (`BaseLayout.astro`) và trang chủ (`index.astro`) đọc localStorage lúc load để hiện dấu ✓ ở chapter đã học + dòng tóm tắt "đã học X/11 chapter" ở trang chủ. Toàn bộ bằng vanilla JS (không thêm dependency/framework). Verify: `astro build` sạch, kiểm tra HTML output có đủ `id="progress-btn"`, `id="progress-summary"`, `data-slug` trên card/nav-link.
- `[x]` **Task 10 — Registry lesson + ổn định website.** Hoàn thành 2026-08-27. Thay cơ chế `if slug === ...` trong `[slug].astro` bằng registry `web/src/data/lessons.ts` (`slug -> { Lesson, Quiz }`), rồi sửa các lỗi tích hợp khiến website không chạy đúng:
  - `Ch08Networking.astro`: đổi 6 đoạn code Kotlin nhiều dòng từ chuỗi `"..."` không hợp lệ sang template literal; giữ nguyên `${apiKey}` dưới dạng văn bản Kotlin.
  - `Ch06Quiz.astro`: sửa backtick bị nhân đôi/thừa và khôi phục `$count` trong code minh hoạ.
  - `Ch08Quiz.astro`: escape `{id}` để Astro không hiểu nhầm là biến JavaScript.
  - Registry: import và đăng ký đủ cặp lesson + quiz Chapter 2–11; dùng `Partial<Record<...>>` để Chapter 1 chưa có entry vẫn hiện placeholder đúng thiết kế.
  - Quiz Chapter 2–11: khai báo kết quả `querySelector<HTMLInputElement>()` để `astro check` hiểu đúng thuộc tính `.value`.
  - Công cụ: pin TypeScript `6.0.3` vì `@astrojs/check@0.9.10` không hỗ trợ TypeScript 7; loại thư mục source lồng `web/web/` khỏi phạm vi type-check nhưng không xoá vì chưa xác định nguồn gốc.
  - **Verify 2026-08-27:** `astro check` → 0 errors (còn 6 hints không chặn); `astro build` → exit 0, sinh 13 trang; smoke test website thật tại dev server → 12/12 route chính HTTP 200; Chapter 2–11 có `Quiz nhanh` và không còn placeholder, Chapter 1 vẫn giữ placeholder đúng roadmap.
  - **Review cấu trúc:** pattern Chapter 5 (component lesson + component quiz trong `web/src/components/lessons/`, code thật kèm file/dòng, quiz JS thuần) tiếp tục được dùng. Roadmap Task 11–20 vẫn theo khuôn: đọc EPUB + source thật → lesson → quiz 2–3 câu + 1 bài tập → đăng ký registry. Audit chỉ xác nhận **đủ cấu trúc**, chưa xác nhận toàn bộ độ chính xác kỹ thuật/nội dung Android của từng lesson.

### Phase 3 — Nhân rộng ra các chapter còn lại

- `[x]` **Task 11 — Lesson Chapter 1** (Welcome to Android & Kotlin). Hoàn thành 2026-08-27. Tạo `Ch01WelcomeToAndroidKotlin.astro` (5 mục: Android/Kotlin, sandbox + 4 app component, Manifest, resources, Gradle — mỗi mục có citation `content/book/ch01-welcome-to-android-kotlin.md` kèm số dòng) và `Ch01Quiz.astro` (3 câu quiz + 1 bài tập "Đọc nhãn Manifest"). Chapter không có project Android thật (`aaf-materials/01-welcome-to-android-and-kotlin/` chỉ có file `.keep`), nên đoạn code duy nhất là trích đoạn XML `AndroidManifest.xml` thật từ EPUB (dòng 55–76), không phải Kotlin — đã sửa lại mô tả sai "học Kotlin thuần trên Kotlin Playground" trong `docs/LEARNING_MAP.md` và hint sai "học Kotlin nền tảng" trong `[slug].astro`. Đã đăng ký `ch01-welcome-to-android-kotlin` vào `web/src/data/lessons.ts`. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/6 hints (không đổi so với trước); `astro build` → exit 0, 13 trang; smoke test dev server thật → 12/12 route chính HTTP 200 (gồm cả `/`), route giả 404, Chapter 1 có "Quiz nhanh" + không còn placeholder + hiện đúng `MainActivity`/`MAIN`/`LAUNCHER`; Chapter 2/8/11 vẫn giữ nguyên hành vi (regression check). Chưa audit toàn bộ độ chính xác nội dung Android ngoài phạm vi đối chiếu trực tiếp với EPUB.
- `[x]` **Task 12 — Lesson Chapter 2** (Getting Started With Android Studio). Hoàn thành 2026-08-27. Đối chiếu `MainActivity.kt` thật tại `aaf-materials/02-getting-started-with-android-studio/projects/final/app/src/main/java/com/kodeco/chat/MainActivity.kt`, bổ sung số dòng cho cả 3 trích dẫn trong `Ch02GettingStartedAndroidStudio.astro`: mục MainActivity (dòng 15–30), Composable Greeting (dòng 32–38), `@Preview` (dòng 40–46). Phát hiện thêm: đoạn code hiển thị cho mục MainActivity thiếu 1 dòng comment thật trong file gốc (`// A surface container using the 'background' color from the theme`, dòng 20) — đã bổ sung lại để code hiển thị khớp 100% với file thật. `Ch02Quiz.astro` đã có sẵn citation dòng đúng (dòng 25, 42–46) từ trước, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/6 hints (không đổi so với baseline); `astro build` → exit 0, 13 trang; smoke test dev server thật → route Chapter 2 hiển thị đúng "(dòng 15–30)"/"(dòng 32–38)"/"(dòng 40–46)" và dòng comment mới; regression check Chapter 1/5/8/11 + route giả 404 vẫn đúng.
- `[x]` **Task 13 — Lesson Chapter 3** (Android Fundamentals). **Đủ cấu trúc theo audit 2026-08-27:** lesson có giải thích + 8 code extract kèm file/dòng, quiz 2 câu + 1 bài tập, đã đăng ký. **Đã audit sâu độ chính xác ở Task 21 (2026-08-27) — xem chi tiết bên dưới.**
- `[x]` **Task 14 — Lesson Chapter 4** (Gradle Basics). **Đủ cấu trúc theo audit 2026-08-27:** dùng `build.gradle.kts` + `gradle/libs.versions.toml`, trích dẫn có file/dòng, quiz 3 câu + 1 bài tập, đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 15 — Lesson Chapter 6** (Advanced Jetpack Compose). **Đủ cấu trúc theo audit 2026-08-27:** có `MainViewModel.kt`, `ChatRoom.kt`, `JumpToBottom.kt` kèm dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 16 — Lesson Chapter 7** (Advanced Architecture). **Đủ cấu trúc theo audit 2026-08-27:** có `Repository.kt`, `RepositoryImpl.kt`, `DittoHandler.kt`, `MainActivity.kt` kèm dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 17 — Lesson Chapter 8** (Networking). Hoàn thành 2026-08-27. Mở rộng từ 1 mục lên 6 mục, đối chiếu số dòng thật trong `aaf-materials/08-networking/projects/final/...`: Coroutine — `viewmodels/RecipeViewModel.kt` dòng 95–111; StateFlow — dòng 73–74; collect Flow trong UI — `ui/recipes/RecipeList.kt` dòng 75–79; Retrofit interface — `network/SpoonacularService.kt` dòng 47–59; `RetrofitInstance` — dòng 61–79; gọi API chi tiết — `RecipeViewModel.kt` dòng 117–129. Phát hiện + sửa 2 lỗi nhỏ khi đối chiếu: thiếu `for id $id` trong message Timber, và `$apiKey` bị hiển thị sai thành `${apiKey}`. Không dùng `ui/MainScreen.kt` (chỉ là điều hướng tab, không liên quan networking, sách không đề cập) — đã ghi lý do vào `docs/LEARNING_MAP.md`. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (giảm từ 6 hint baseline vì 5 cảnh báo "declared but never read" của Chapter 8 nay đã hết do mọi code block đều được dùng); `astro build` → exit 0, 13 trang; smoke test dev server thật → cả 6 mục + số dòng hiển thị đúng, `$apiKey` hiển thị đúng không thừa ngoặc; regression Chapter 1/2/5/11 + route giả 404 vẫn đúng.
- `[x]` **Task 18 — Lesson Chapter 9** (Data Store). **Đủ cấu trúc theo audit 2026-08-27:** `Prefs.kt` + wiring app/UI có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 19 — Lesson Chapter 10** (Room Database). **Đủ cấu trúc theo audit 2026-08-27:** `RecipeDao.kt`, `RecipeDatabase.kt`, `RecipeRepository.kt` + Entity code có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 20 — Lesson Chapter 11** (Advanced Storage). **Đủ cấu trúc theo audit 2026-08-27:** `SecurePrefs.kt`, encrypted `RecipeDatabase.kt`, app wiring có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.

### Phase 3b — Audit sâu độ chính xác từng chapter (chốt hướng 2026-08-27)

- `[x]` **Task 21 — Audit sâu Chapter 3** (Android Fundamentals). Hoàn thành 2026-08-27. Đối chiếu 8 đoạn code + citation trong `Ch03AndroidFundamentals.astro`/`Ch03Quiz.astro` với file thật `aaf-materials/03-android-fundamentals/projects/{final,starter}/...`. Phát hiện + sửa 2 lỗi: citation `strings.xml` sai (ghi "dòng 35-39", đúng phải "dòng 34-40" vì đoạn code hiển thị gồm cả thẻ `<resources>`); bài tập nhỏ ghi sai tên file "starter/Activity.kt" (đúng là `MainActivity.kt`) và thiếu citation dòng — đã sửa thành đường dẫn đúng + dòng 66–72/69. 6 đoạn code còn lại đối chiếu khớp 100%, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint; `astro build` → exit 0, 13 trang; smoke test dev server thật → citation mới hiển thị đúng, citation cũ sai không còn xuất hiện; regression Chapter 1/2/5/8/11 + route giả 404 vẫn đúng.
- `[x]` **Task 22 — Audit sâu Chapter 4** (Gradle Basics). Hoàn thành 2026-08-27. Đối chiếu 9 đoạn code + citation trong `Ch04GradleBasics.astro`/`Ch04Quiz.astro` với file thật `aaf-materials/04-gradle-basics-a-look-behind-the-curtain/projects/{final,starter}/...`. Phát hiện + sửa 2 lỗi: citation `starterRootBuildGradleCode` sai (ghi "dòng 2–4", đúng phải "dòng 2–5" vì đoạn code hiển thị gồm cả dấu `}` đóng khối); callout "Ghi chú phiên bản" nói sai là sách dùng AGP 8.2.0 giống code mẫu — thực ra sách ví dụ bằng AGP 8.1.4 (chỉ Kotlin 1.9.10 khớp), đã sửa lại đúng chênh lệch. Sửa thêm 1 câu callout dễ hiểu nhầm về mốc xuất hiện của app Kodeco Chat. 8 đoạn code còn lại đối chiếu khớp 100%, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → citation/nội dung mới hiển thị đúng, nội dung cũ sai không còn xuất hiện; regression Chapter 1/2/3/5/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 23 — Audit sâu Chapter 6** (Advanced Jetpack Compose). Hoàn thành 2026-08-27. Đối chiếu 6 đoạn code + citation trong `Ch06AdvancedJetpackCompose.astro` với file thật `aaf-materials/06-advanced-jetpack-compose/projects/final/app/src/main/java/com/kodeco/chat/{viewmodel/MainViewModel.kt,conversation/JumpToBottom.kt,data/model/ChatRoom.kt}`. Cả 6 citation khớp chính xác 100% — không có lỗi lệch dòng. Phát hiện 1 điểm cần ghi rõ: comment thật trong `MainViewModel.kt` ghi "see chapter 8" cho Data Store, trong khi sách gọi đúng là Chapter 9 — đã thêm chú thích trong lesson để làm rõ chênh lệch này. `Ch06Quiz.astro` đối chiếu đúng, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → route Chapter 6 HTTP 200, chú thích mới hiển thị đúng; regression Chapter 1/2/3/4/5/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 24 — Audit sâu Chapter 7** (Advanced Architecture). Hoàn thành 2026-08-27. Đối chiếu 5 đoạn code + citation trong `Ch07AdvancedArchitecture.astro` với file thật `aaf-materials/07-advanced-architecture/projects/final/app/src/main/java/com/kodeco/chat/{data/repository/Repository.kt,data/repository/RepositoryImpl.kt,DittoHandler.kt,MainActivity.kt}`. Cả 5 citation khớp chính xác 100% — **không phát hiện lỗi nào** (chapter đầu tiên trong đợt audit sạch hoàn toàn, kể cả citation lẫn nội dung giải thích). Ghi nhận (không cần sửa): comment "Chapter 8, Data Store" trong `RepositoryImpl.kt` thật (cùng loại lỗi số chương như Task 23) nằm ngoài đoạn code được trích ở lesson này nên không gây mâu thuẫn cho người đọc. `Ch07Quiz.astro` đối chiếu đúng, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → route Chapter 7 HTTP 200; regression Chapter 1/2/3/4/5/6/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 25 — Audit sâu Chapter 9** (Data Store). Hoàn thành 2026-08-27. Đối chiếu 4 đoạn code + citation trong `Ch09DataStore.astro` với file thật `aaf-materials/09-data-store/projects/final/app/src/main/java/com/kodeco/recipefinder/{data/Prefs.kt,RecipeApp.kt,MainActivity.kt,ui/MainScreen.kt}`. Nội dung code khớp 100%. **Đã sửa 3 lỗi:** (1) citation `recipeAppCode` ghi "dòng 9–20" nhưng đoạn hiển thị thật là dòng 9–21 (thiếu dấu `}` đóng class) → sửa thành 9–21; (2) `Ch09Quiz.astro` đáp án sai câu 3 lỗi chính tả "Lưu trữ trữ dữ liệu key-value thay thế DataStore" → "Lưu trữ dữ liệu key-value thay cho DataStore"; (3) phần giải thích câu 3 câu tối nghĩa "không cần truyền tham số tay nhiên" → "không cần truyền tham số thủ công qua từng hàm". Các citation khác đã đúng: `prefsCode` dòng 12–43, `localPrefsProviderCode` dòng 30–31 + 44–48, `mainScreenCode` dòng 22–38, và 2 citation trong văn xuôi (`MainScreen.kt` dòng 46–49 / 56–59). Version DataStore `prefsVersion = "1.0.0"` trong `libs.versions.toml` thật khớp callout. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (không đổi); `astro build` → exit 0, route Chapter 9 build ra `dist/`; smoke test dev server thật → Chapter 9 HTTP 200, các citation đã sửa hiện đúng, chuỗi lỗi cũ = 0; regression toàn bộ 11 chapter + trang chủ HTTP 200, route giả 404, các chỗ sửa Task 22/23/24 còn nguyên.
- `[x]` **Task 26 — Audit sâu Chapter 10** (Room Database). Hoàn thành 2026-08-27. Đối chiếu 4 đoạn code + citation trong `Ch10RoomDatabase.astro` với file thật `aaf-materials/10-room-db/projects/final/app/src/main/java/com/kodeco/recipefinder/data/{database/RecipeDb.kt,database/RecipeDao.kt,database/RecipeDatabase.kt,RecipeRepository.kt}`. **Cả 4 citation khớp chính xác** (`recipeDbCode` dòng 43–67; `recipeDaoCode` dòng 44–63 — trùng khớp từng dòng kể cả `}` đóng interface; `recipeDatabaseCode` dòng 42–74; `recipeRepositoryCode` dòng 43–101). Quiz 3 câu + bài tập nhỏ (kể cả citation "khoảng dòng 49-50" của `findRecipeById`) đều đúng. **Đã sửa 2 chỗ:** (1) callout phiên bản nói "Room 3.0 … mới ở giai đoạn alpha, chưa ổn định" — **sai tính đến 2026-08-27**: `androidx.room3` đã stable từ 3.0.0 (01-07-2026), bản mới nhất 3.0.2 (26-08-2026); đã viết lại callout với thông tin đúng, ghi rõ Room 2.5.2 của sách khớp với code mẫu thật, Room 2.x (2.8.4) đang ở chế độ bảo trì, Room 3.0 có breaking change (bắt buộc KSP, bỏ `SupportSQLite`, coroutine-based) nhưng annotation cốt lõi + Repository pattern học ở chapter vẫn dùng được; (2) ghi chú nguồn của `recipeDatabaseCode` chỉ nói "bỏ bớt comment `@Volatile`" trong khi đoạn hiển thị còn **dịch comment `// only one thread…` sang tiếng Việt** — đã ghi rõ cả 2 thay đổi để người đọc không nhầm là copy nguyên văn. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (không đổi); `astro build` → exit 0, route Chapter 10 build ra `dist/`; smoke test dev server thật → Chapter 10 HTTP 200, 4 citation + "3.0.2"/"2.8.4"/"2.5.2"/"androidx.room3" hiện đúng, chuỗi cũ "chưa ổn định"/"alpha" = 0; regression 11/11 chapter + trang chủ 200, `ch99-fake` → 404, các sửa của Task 22/23/25 còn nguyên.
- `[x]` **Task 27 — Audit sâu Chapter 11** (Advanced Storage). Hoàn thành 2026-08-27. **Chapter cuối của đợt audit này** → hoàn tất mục tiêu "audit sâu 7 chapter còn lại" user chọn ngày 2026-08-27. Đối chiếu **7 citation** trong `Ch11AdvancedStorage.astro` với file thật (`aaf-materials/11-advanced-storage/projects/final/.../recipefinder/`: `data/SecurePrefs.kt` dòng 41–54 + 56–74, `data/database/RecipeDatabase.kt` dòng 40–42 + 50–89, `RecipeApp.kt` dòng 58–69, `MainActivity.kt` dòng 66–67; cộng 1 citation so sánh sang chapter 10: `10-room-db/.../RecipeDatabase.kt` dòng 55–72) → **cả 7 citation khớp chính xác 100%**, không lỗi lệch dòng. Quiz 3 câu (`a`, `c`, `b`) + bài tập nhỏ đối chiếu đúng. **Phát hiện & sửa 3 lỗi:** (1) **claim kỹ thuật SAI** — lesson viết `SecurePrefs` "có chữ ký giống hệt class `Prefs` cũ"; đối chiếu file thật thì **khác 2 điểm thật sự**: `SecurePrefs` bỏ hết `suspend` (vì `EncryptedSharedPreferences` dùng API `SharedPreferences` đời cũ `edit().apply()`, không async như DataStore), và `getInt` đổi kiểu trả về từ `Int?` (ch09) sang `Int` (mặc định 0) → đã viết lại thành 1 đoạn + 2 gạch đầu dòng nêu rõ 2 khác biệt, kèm quan sát: `ui/MainScreen.kt` dòng 68 vẫn giữ `if (currentIndex != null)` từ chapter trước, với kiểu `Int` không nullable thì điều kiện này **luôn đúng → đã thành code dư thừa** (lỗi nhỏ trong code mẫu gốc của Kodeco, ghi lại làm điểm học); (2) callout phiên bản **thiếu và sai một phần** — bản cũ chỉ nói security-crypto deprecated và khuyên "DataStore + Tink" (trang release notes của Google **không** nói vậy), lại **bỏ sót hoàn toàn** việc `net.zetetic:android-database-sqlcipher` cũng đã bị khai tử; đã viết lại callout thành 2 gạch đầu dòng có nguồn: security-crypto deprecated từ 1.1.0-alpha07 (09-04-2025), stable cuối 1.1.0 (30-07-2025), Google khuyên dùng Android Keystore trực tiếp + Tink (theo Security checklist), **không có class thay thế 1-đổi-1**; SQLCipher: `android-database-sqlcipher` khai tử chính thức 31-08-2023 (cùng SQLCipher 4.5.5), thay bằng `net.zetetic:sqlcipher-android`, package đổi `net.sqlcipher.database` → `net.zetetic.database.sqlcipher`, `SupportFactory` → `SupportOpenHelperFactory`, bản mới nhất SQLCipher **4.18.0 (18-08-2026)** đã hỗ trợ Room 3; (3) lỗi chính tả trong `Ch11Quiz.astro` giải thích câu 2: "lớp lưu trữ phía dưồng" → "phía dưới". Đã kiểm tra thêm: `security = "1.0.0"` + `sqlcipher = "4.4.0"` trong `libs.versions.toml` thật **khớp đúng** version sách nêu (dòng 247, 407) → không có sai lệch sách-vs-code; ghi chú sẵn có "sách đặt code ở `MainActivity` nhưng code thật đặt ở `RecipeApp.onCreate()`" đã **xác minh đúng** (sách dòng 485 nói "Return to *MainActivity*", code thật ở `RecipeApp.kt` dòng 58–69). **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (hint cũ ở `Ch03Quiz.astro`, không liên quan); trong lúc sửa có phát sinh 2 lỗi build do viết `launch { ... }` thẳng trong JSX (Astro hiểu `{` là biểu thức) → đã sửa thành HTML entity và check lại sạch; `astro build` → exit 0, route Chapter 11 build ra `dist/`; smoke test dev server thật (`http://localhost:4323`) → Chapter 11 HTTP 200, đủ 7 citation + các mốc "1.1.0-alpha07"/"09-04-2025"/"30-07-2025"/"31-08-2023"/"4.18.0"/"sqlcipher-android"/"SupportOpenHelperFactory"/"Tink" và 2 đoạn mới, 3 chuỗi lỗi cũ đều = 0; regression đủ 11/11 chapter + trang chủ HTTP 200, route giả `ch99-fake` → 404, các chỗ sửa của Task 22 (Ch04), 23 (Ch06), 25 (Ch09), 26 (Ch10) vẫn còn nguyên.

### Phase 3c — Audit sâu 4 chapter còn lại (Chapter 5, 8, 1, 2 — chốt hướng 2026-08-27)

Lý do mở phase này: Chapter 5 và 8 được viết ở Task 7/17, **trước khi** có quy trình audit chặt (kiểm từng số dòng citation) áp dụng từ Task 21. Chapter 1/2 không có project Android nên rủi ro thấp hơn, để sau.

- `[x]` **Task 28 — Audit sâu Chapter 5** (Jetpack Compose). Hoàn thành 2026-08-27. Đối chiếu **6 citation** trong `Ch05JetpackCompose.astro` + 1 citation trong `Ch05Quiz.astro` với file thật `aaf-materials/05-jetpack-compose/projects/final/app/src/main/java/com/kodeco/chat/`. **Phát hiện & sửa 6 lỗi** (chapter nhiều lỗi nhất từ đầu đợt audit, đúng như dự đoán vì viết trước khi có quy trình kiểm dòng):
  - **2 lỗi lệch dòng citation:** (a) `conversationContentCode` ghi "dòng 103–119" nhưng đoạn hiển thị bắt đầu từ `@Composable`/`fun ConversationContent` = **dòng 95**, dòng 103 mới là `Surface()` → sửa thành "dòng 95–119"; (b) `modifierChainCode` ghi "dòng 208–219" nhưng `Image(` mở ở **dòng 207** (208 là `modifier = Modifier`) → sửa thành "dòng 207–219". Cùng lỗi này trong bài tập của `Ch05Quiz.astro` ("khoảng dòng 208") → sửa thành "dòng 207–219" + nêu `MessageUi` bắt đầu ở dòng 186.
  - **2 chỗ sửa code mà không khai báo** (vi phạm nguyên tắc truy ngược nguồn): `messagesCode` gộp biến `val userId` (dòng 167 file thật) vào thẳng chỗ dùng nó và bỏ tham số bị comment sẵn `// scrollState`, nhưng ghi chú chỉ nói "rút gọn"; `fakeDataCode` gộp `private val initialMessages` (dòng 66–70) vào thẳng `exampleUiState` — file thật khai báo riêng. Đã ghi rõ từng thay đổi vào ghi chú nguồn.
  - **1 lỗi bài tập không làm được như hướng dẫn:** bài tập bảo đổi `padding` của avatar rồi "mở `@Preview` của `KodecochatAppBarPreview` … để quan sát". Kiểm tra thật: cả project chỉ có **3 `@Preview`** (`components/KodecochatAppBar.kt` dòng 82 + 91, `conversation/UserInput.kt` dòng 120) — **không có preview nào vẽ bong bóng chat**, nên làm theo hướng dẫn sẽ không thấy gì thay đổi. Đã sửa: nói rõ file `Conversation.kt` không có `@Preview` nào, phải chạy app thật hoặc tự thêm `@Preview` cho `MessageUi`. Bổ sung cách tự kiểm chứng bằng chính code: 42 + 16×2 = 74 = đúng `Modifier.width(74.dp)` của `Spacer` ở dòng 222.
  - **1 lỗi tham chiếu chéo sai trong lesson:** câu "`KodecochatTheme { }` … áp màu/font riêng của app (**xem mục 6**)" — mục 6 là "Dữ liệu giả", hoàn toàn không nói về theme; theme thật nằm ở `theme/Themes.kt` dòng 109, không thuộc phạm vi lesson → đã sửa thành trỏ đúng file.
  - **Viết lại callout phiên bản.** Bản cũ ghi "AGP 9.2.0" (đã lạc hậu) và nói chênh lệch "chủ yếu ở version công cụ, không phải API" — đúng phần lớn nhưng **bỏ sót 1 thay đổi cấu hình thật sự**: `app/build.gradle.kts` dòng 76–78 của code mẫu dùng `composeOptions { kotlinCompilerExtensionVersion = "1.5.3" }`, từ **Kotlin 2.0** Compose compiler phát hành kèm trong Kotlin nên khối này bị bỏ, thay bằng plugin `org.jetbrains.kotlin.plugin.compose` (riêng `buildFeatures { compose = true }` vẫn giữ vì là thiết lập của AGP). Đã cập nhật mốc hiện tại: Kotlin **2.4** (2.4.0 tháng 6-2026, 2.4.20 dự kiến tháng 9-2026), AGP **9.3** (9.3.0 tháng 7-2026).
  - **Đã đối chiếu khớp 100%, không cần sửa:** `mainActivityCode` (dòng 44–55), `previewCode` (dòng 82–88), phần logic Kotlin của `messagesCode`/`fakeDataCode`, quiz 3 câu (`b`, `c`, `b`) đều đúng, và khẳng định "chapter 5 chưa có ViewModel" — xác minh bằng `find` không có file `*ViewModel*` nào trong project.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (hint cũ `Ch03Quiz.astro:3`, không đổi); `npx astro build` → exit 0, 13 trang, có `dist/chapters/ch05-jetpack-compose/index.html`; smoke test dev server thật (`http://localhost:4323`, pid 11380) → Chapter 5 HTTP 200, 6/6 citation mới hiện đúng, 17/17 chuỗi nội dung mới đều có, 7/7 chuỗi lỗi cũ = 0; regression 11/11 chapter + trang chủ = 200, `ch99-fake` → 404, các bản sửa Task 22/25/26/27 vẫn nguyên; `git status` trong `aaf-materials/` sạch (không sửa reference).
- `[x]` **Task 29 — Audit sâu Chapter 8** (Networking). Hoàn thành 2026-08-27. Đối chiếu **6 citation** trong `Ch08Networking.astro` + citation bài tập trong `Ch08Quiz.astro` với file thật `aaf-materials/08-networking/projects/final/app/src/main/java/com/kodeco/recipefinder/`. **Cả 6 citation khớp chính xác 100%** (`RecipeViewModel.kt` dòng 95–111, 73–74, 117–129; `RecipeList.kt` dòng 75–79; `SpoonacularService.kt` dòng 47–59 và 61–79) — chapter thứ 2 (cùng Chapter 7) sạch hoàn toàn ở lớp citation dù được viết ở Task 17 trước khi có quy trình kiểm dòng. Quiz 3 câu (`b`, `a`, `c`) + bài tập ("khoảng dòng 57–58", đáp án `RecipeInformationResponse`) đều xác minh đúng. **Nhưng phát hiện & sửa 4 lỗi nội dung** (khác loại với các chapter trước — không phải lệch dòng mà là thiếu/quá lời trong phần giải thích):
  - **Thiếu cảnh báo API key là chỗ trống.** `const val apiKey = "<Replace with API Key>"` — không thay bằng key thật thì mọi lời gọi mạng thất bại và danh sách công thức luôn trống, nhưng lesson không nói. Đã thêm `.hint`: phải tự đăng ký key miễn phí ở spoonacular.com rồi dán vào **dòng 47** của `SpoonacularService.kt`; kèm chính cảnh báo của sách (phần "Using Your API Key", `content/book/ch08-networking.md` dòng 314): không bao giờ commit key lên repo công khai, Google gợi ý `secrets-gradle-plugin`.
  - **Claim "StateFlow thay thế LiveData" quá mạnh** (ở cả tiêu đề mục 2 của lesson và giải thích câu 2 của quiz). Đối chiếu tài liệu chính thức: `LiveData` **chưa bị deprecated** tính đến 2026-08-27, vẫn chạy bình thường; chỉ là code Kotlin/Compose mới hầu như luôn chọn `StateFlow`. Chữ "replace" là cách nói của sách (Key Points, `ch08-networking.md` dòng 592) → đã ghi rõ chênh lệch ở cả 2 chỗ, giữ nguyên trích dẫn của sách thay vì âm thầm sửa.
  - **Giải thích `Dispatchers.Default` không phê phán.** Code thật `queryRecipe` (dòng 117–129) vừa khai báo `suspend` **vừa** bọc thân hàm trong `viewModelScope.launch(Dispatchers.Default)` — dư một lớp, khiến hàm trả về ngay và người gọi không thật sự chờ kết quả; ngoài ra `Dispatchers.Default` là dispatcher cho việc nặng CPU còn gọi mạng là I/O (`Dispatchers.IO`). Đã thêm 2 gạch đầu dòng "đọc code có phê phán", kèm ghi chú thực tế vẫn chạy đúng vì Retrofit/OkHttp tự chuyển sang thread pool riêng.
  - **Thiếu hoàn toàn callout phiên bản** (khác Ch05/06/10/11). Đã tra chéo Maven Central + changelog GitHub và thêm `.callout.note`: Retrofit `2.9.0` → mới nhất **3.0.0** (15-05-2025, nhánh 2.x cuối là **2.12.0** cùng ngày), 3.0.0 chỉ nâng OkHttp 3.14→4.12 và giữ tương thích nhị phân — annotation `@GET`/`@Query`/`@Path` **không đổi**; Moshi `1.15.0` → **1.15.2** (05-12-2024), `2.0.0-alpha.1` (28-01-2026) chưa ổn định; Timber `5.0.1` vẫn là bản mới nhất từ 2021. **Kết luận đáng ghi nhận: thư viện mạng của chapter này lỗi thời ít nhất trong cả sách**, chỉ Kotlin `1.9.10`/AGP `8.2.0` là cũ.
  - Bổ sung CSS thiếu trong `<style>` của `Ch08Networking.astro` (`.lesson p`, `.lesson ul`, `.lesson li`, `.hint`, `.callout.note`) — nếu không thì nội dung mới render không có style.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (hint cũ `Ch03Quiz.astro:3`); `npx astro build` → exit 0, 13 trang; smoke test dev server thật (`http://localhost:4323`) → Chapter 8 HTTP 200, 6/6 citation hiện đúng 1 lần, đủ marker nội dung mới, 2 chuỗi cũ = 0; regression 11/11 chapter + trang chủ = 200; `git status --porcelain` trong `aaf-materials/` sạch.
- `[x]` **Task 30 — Audit sâu Chapter 1 + 2.** Hoàn thành 2026-08-27 (**kết thúc Phase 3c → hoàn tất audit 11/11 chapter**). 2 chapter này không có project Android để đối chiếu code theo cách các chapter khác, nên trọng tâm chuyển sang: citation vào EPUB, tính chính xác của trích đoạn, và **độ lỗi thời của hướng dẫn bấm chuột**.
  - **Đã đối chiếu khớp, không cần sửa:** cả **3 citation của Chapter 2** khớp chính xác với file thật `aaf-materials/02-getting-started-with-android-studio/projects/final/.../com/kodeco/chat/MainActivity.kt` (47 dòng): `class MainActivity` dòng 15–30, `Greeting` dòng 32–38, `@Preview` dòng 40–46; bài tập trong `Ch02Quiz.astro` (`Greeting("Fuad")` ở dòng 25 và 44, `GreetingPreview` dòng 42–46) đều đúng. Quiz Ch02 (`b`,`b`,`a`) và Ch01 (`b`,`c`,`a`) đúng. Toàn bộ **6 citation EPUB của Chapter 1** (dòng 19–27, 29–49, 55–76, 78–86, 88–94, 96–104) khớp `content/book/ch01-welcome-to-android-kotlin.md`. Callout "thư mục tài liệu chỉ có file `.keep`" — xác minh đúng: `aaf-materials/01-welcome-to-android-and-kotlin/` chỉ chứa 4 file `.keep`.
  - **Sửa lỗi 1 — Chapter 1 trích code mà không khai báo đã cắt bớt** (cùng loại lỗi đã sửa ở Task 28): `manifestCode` bỏ dòng comment của sách ở dòng 67 (`<!-- Specifies the kind of Intents the Activity can respond to -->`) mà ghi chú nguồn không nói gì. Đã ghi rõ dòng bị bỏ + dịch nghĩa + lý do bỏ (ý đó đã được giải thích bằng tiếng Việt ở mục 3).
  - **Bổ sung 2 — Chapter 1 thiếu lý do chọn Kotlin.** Sách nêu Kotlin là ngôn ngữ **được Google khuyến nghị chính thức cho Android từ 2019** (`ch01-welcome-to-android-kotlin.md` dòng 25) nhưng lesson không dẫn — đã thêm.
  - **Bổ sung 3 — Chapter 2 mục 1 quá mỏng** (chỉ 1 câu "tải tại developer.android.com/studio", bỏ trắng toàn bộ luồng tạo project của sách). Đã thêm 4 thông tin thật của bước New Project: Name = `Kodeco Chat`, Package = `com.kodeco.chat`, Save location, **Minimum SDK = API 28 ("Pie", Android 9.0)**, và Build configuration language = **Kotlin DSL** (mặc định), kèm citation `ch02-...md` dòng 63–92.
  - **Sửa lỗi 4 — Chapter 2 thiếu callout phiên bản, dù đây là chapter lỗi thời nhất cả sách** (là chapter duy nhất hướng dẫn từng bước bấm chuột trong IDE → giao diện thật đã khác). Đã thêm `.callout.note` với 5 điểm đã tra chéo tài liệu chính thức (2026-08-27): (a) sách dùng **Android Studio Hedgehog** (2023) vs bản ổn định hiện tại **Quail 3 | 2026.1.3 Patch 1**, liệt kê chuỗi chu kỳ Hedgehog → … → Otter → Panda → Quail (kế tiếp Rabbit 1 | 2026.2 đang preview); (b) **bước "Enable new UI" (sách dòng 51–56) không còn tồn tại** — New UI đã là giao diện mặc định từ **Ladybug Feature Drop** (theo developer.android.com/studio/intro/new-ui), muốn dùng Classic UI phải cài plugin từ JetBrains Marketplace → người đọc bỏ qua bước này; (c) System Image: sách ví dụ **UpsideDownCake / API Level 34**, hiện tại mới nhất là **Android 17 = API level 37** — cứ làm đúng tinh thần "chọn API Level cao nhất", con số khác sách là bình thường, **nhưng Minimum SDK vẫn để API 28** như sách yêu cầu (2 con số này hay bị lẫn); (d) máy ảo **Pixel 7 Pro** (sách dòng 141) có thể không còn trong danh sách mặc định, chọn Pixel nào cũng được; (e) sách mặc định **macOS** (dòng 28) — trên Windows/Linux menu Settings ở `File ▸ Settings`. Ghi thêm project mẫu dùng Kotlin `1.9.10`/AGP `8.2.0`/`compileSdk 34` (đã đọc `build.gradle.kts` thật) vs Kotlin 2.4/AGP 9.3 hiện tại.
  - Bổ sung CSS thiếu trong `<style>` của `Ch02GettingStartedAndroidStudio.astro` (`.lesson p`, `.lesson ul`, `.lesson li`, `.hint`, `.callout`/`.callout.note`) cho khớp các lesson khác.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (không đổi); `npx astro build` → exit 0, 13 trang; smoke test dev server thật (`http://localhost:4323`) → Chapter 1 và Chapter 2 HTTP 200, 9/9 marker nội dung mới hiện đúng (`Quail 3 | 2026.1.3 Patch 1`, `Ladybug Feature Drop`, `API level 37`, `Enable new UI`, `Kodeco Chat`, `API 28` ×2, dòng comment bị bỏ, mốc 2019); regression 11/11 chapter + trang chủ = 200.

### Phase 4 — Hoàn thiện để dùng được (mở 2026-08-27)

- `[x]` **Task 31 — README hướng dẫn chạy cho người non-tech.** Hoàn thành 2026-08-27. Tạo mới `README.md` ở **root project** (`d:\book-course-vibe\README.md`) — trước đó root **không có README nào**, chỉ có `web/README.md` là file mẫu mặc định của template "Astro Starter Kit: Minimal" (nói về `npm create astro`, `localhost:4321`, Discord của Astro — không liên quan gì tới dự án này). Đây là điều kiện "hoàn thành dự án" trong `CLAUDE.md` chưa từng có task nào phụ trách.
  - **Nội dung 5 phần, viết cho người không phải dân kỹ thuật:** (1) chạy website theo 6 bước đánh số — cài Node.js từ nodejs.org (bản LTS), mở Command Prompt đúng thư mục bằng cách gõ `cmd` vào ô địa chỉ File Explorer, `npm install`, `npm run dev`, mở `http://localhost:4321`, tắt bằng `Ctrl + C`; (2) bảng 5 lỗi thường gặp kèm cách xử lý; (3) 4 lệnh khác (`build`/`preview`/`astro check`); (4) sơ đồ thư mục + bảng 3 file trong `docs/`; (5) các điều cần biết (chạy local, không sửa `.epub` và `aaf-materials/`, chỉ dùng cá nhân nên không public, muốn làm bài tập thì cần thêm Android Studio).
  - **3 chi tiết đã xác minh thật trước khi viết, thay vì chép mặc định:** cảnh báo `npm warn` là bình thường (đúng với cảnh báo Node 22.14.0 vs `undici@8.10.0` đang có trong môi trường này); **cổng có thể không phải 4321** — README dặn đọc dòng `Local` trong terminal, vì thực tế dev server của project đang chạy ở `4323` do 4321/4322 bị chiếm; script khả dụng lấy từ `package.json` thật (`dev`/`build`/`preview`/`astro`), không bịa lệnh không tồn tại.
  - **Verify 2026-08-27:** đối chiếu `web/package.json` (4 script, `engines.node >= 22.12.0`), `node -v` = v22.14.0, `npm -v` = 10.9.2; xác nhận `docs/` có đúng 3 file `.md` được nhắc tên (`PROJECT_PLAN.md`, `SOURCE_MAP.md`, `LEARNING_MAP.md`); trang chủ dev server trả về đúng **11 link chapter** như README mô tả; `web/dist/` tồn tại đúng như phần mô tả lệnh `build`.
- `[x]` **Task 32 — Cleanup rủi ro tồn đọng.** Hoàn thành 2026-08-27. User đã cho phép tự chọn phạm vi ("cứ theo cách tốt nhất của bạn"). **Trước khi xoá đã mở từng thứ ra xem, và phát hiện 2 mô tả trong bản kế hoạch cũ của chính task này là SAI** — nếu xoá theo mô tả cũ sẽ mất nội dung thật:
  - **Sai #1:** `web/web/` bị ghi là "bản source không đầy đủ". Thực tế `diff` cho thấy `web/web/src/components/lessons/Ch03Quiz.astro` có **238 dòng, 4 câu quiz đầy đủ + bài tập 3 bước + `<details>` gợi ý**, trong khi bản đang chạy `web/src/components/lessons/Ch03Quiz.astro` chỉ có **81 dòng, 2 câu** (câu 2 chỉ có 2 lựa chọn a/b) và bài tập 1 đoạn văn. Bản đang chạy mới hơn về thời gian nhưng **nghèo nội dung hơn** — tức là một bước lùi. → Đã **salvage** (cứu) nội dung tốt hơn sang bản đang chạy trước khi xoá, đồng thời sửa 2 lỗi có thật trong bản 238 dòng đó: (a) escape sai `remember {`{ }`}` → dùng `&#123;`/`&#125;`; (b) **specifier định dạng chuỗi Android sai `%1\$1` → đúng là `%1$s`** (đã đối chiếu tài liệu Android: `%1` = tham số thứ 1, `s` = kiểu string). Cũng bổ sung mốc dòng thật đã kiểm: `starter/.../strings.xml` chỉ có 1 chuỗi `app_name` (dòng 35–37), `starter/.../MainActivity.kt` hàm `Greeting` dòng 66–72 với `text = "Hello $name!"` ở dòng 69.
  - **Sai #2:** `',` bị ghi là "1 thư mục tên rác". Thực tế là **1 file thường 0 byte** tên `',` (do lỗi quoting lệnh shell). Không phải thư mục.
  - **Giữ lại bản đang chạy** `web/src/data/lessons.ts` (50 dòng) và **không** lấy bản trong `web/web/` (41 dòng) — bản lồng cũ hơn, thiếu import quiz Chapter 1/2 và còn dòng `// TODO: Thêm Quiz sau khi sửa file`.
  - **Đã xoá (sau khi xác minh từng thứ):** file 0 byte `',`; **10 file log** (`build-error.txt`, `build-full.txt`, `build-out.txt`, `build-output.txt`, `build_err.txt`, `build_output.txt`, `build_verbose.txt`, `err.txt`, `out.txt`, `full_build_log.txt`); **4 script tạm sinh file** (`fix_ch03_quiz.py`, `tmp_fix_escape.cjs`, `tmp_write_ch08.cjs`, `write_ch02.py` — phát hiện thêm `write_ch02.py` chưa có trong danh sách cũ, tổng 14 file log/tạm chứ không phải 13); thư mục lồng `web/web/` (sau khi salvage xong); `web/src/pages/test-minimal.astro` (route thử `/test-minimal/`); `web/src/components/lessons/dist` (rỗng) + `web/src/components/lessons/node_modules` (1.2 MB cache Vite lạc chỗ).
  - **Sửa kèm:** bỏ `"web"` khỏi `exclude` trong `web/tsconfig.json` (không còn thư mục đó để loại trừ); bỏ `import { Code } from "astro:components"` không dùng ở `Ch03Quiz.astro:3` → **hết hint cuối cùng của `astro check`**.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, **0 errors / 0 warnings / 0 hints** (31 file, giảm từ 34 do đã xoá file rác); `npx astro build` sau khi xoá sạch `dist` → exit 0, **12 trang** (giảm từ 13 vì đã bỏ `/test-minimal/`), đúng 11 chapter + trang chủ; HTML build ra của Chapter 3 có **4 `<fieldset>`** và chứa `%1$s`, **không còn** `%1\$1`; dev server thật → **12/12 route HTTP 200**, Chapter 3 live có 4 câu quiz, `/test-minimal/` trả **404** đúng như mong đợi; `git -C aaf-materials status --porcelain` rỗng → reference source không bị đụng.

## Quyết định quan trọng đã chốt
1. **Web stack: Astro.** Lý do: nội dung nhiều bài học + cần sidebar/search + Markdown là chính, Astro có sẵn xử lý Markdown/syntax highlighting, gần như 0 JS phía client trừ chỗ cần tương tác (quiz, progress) → ít code phải tự viết/bảo trì hơn so với tự viết HTML/JS thuần. **Đã cài đặt từ Task 5; hiện dùng Astro 7.2.8.**
2. **Trích xuất EPUB dùng Python stdlib** (`zipfile` + `xml.etree.ElementTree`), không cài `ebooklib`/`beautifulsoup4`/`lxml`. Lý do: nội dung EPUB (`OEBPS/html/seg*.xhtml`) là XHTML sạch, hợp lệ như XML, heading/code-block có class CSS rõ ràng (`segment-chapter`, `code-block`) → parser chuẩn của Python xử lý được, không cần thêm dependency.
3. **Cấu trúc thư mục:** `content/book/` (Markdown trích từ sách) · `docs/` (tài liệu quản lý project) · `web/` (website Astro). Không đụng `aaf-materials/` và file `.epub`.
4. **Reference sources không sửa:** mặc định. Chỉ sửa `aaf-materials/` nếu 1 task cụ thể yêu cầu và user đã đồng ý rõ ràng.
5. **Thứ tự ưu tiên nguồn khi viết lesson:** EPUB > `aaf-materials/` > developer.android.com / kotlinlang.org > nguồn khác.
6. **Chapter cho vertical slice (Task 7): CHỐT — Chapter 5 (Jetpack Compose).** Chapter đầu tiên có code Android thật + rõ ràng để mapping (Chapter 1-4 chủ yếu là nền tảng, Chapter 1 thậm chí không có Android project thật).
7. **Xử lý xung đột license `aaf-materials/` (mục Vấn đề/rủi ro, phát hiện khi chuẩn bị Task 7): user đã xác nhận — tiếp tục dùng code thật từ `aaf-materials/` trong lesson.** Lý do user đưa ra: đây là dự án cá nhân, không public/phân phối, và user đã trả tiền mua sách/vật liệu đi kèm nên tự nhận trách nhiệm rủi ro pháp lý này. Quyết định 2026-08-26, do user chốt trực tiếp (không phải suy luận của Claude). Vẫn giữ nguyên tắc: không sửa file gốc trong `aaf-materials/`, ghi rõ nguồn (file + dòng) khi trích code vào lesson.
8. **Không thêm giai đoạn "hiện đại hoá app" vào roadmap (2026-08-27, user xác nhận trực tiếp).** User hỏi làm rõ liệu website có nên tiến tới xây/nâng cấp lại app trong sách bằng kiến thức Android hiện nay không — đã trình bày 2 lựa chọn (giữ scope hiện tại vs. thêm phase hiện đại hoá) và user chọn **giữ scope hiện tại**. Nghĩa là: mục tiêu vẫn là dạy đúng nội dung + code thật trong sách/`aaf-materials/`, nguồn hiện đại (developer.android.com/kotlinlang.org) chỉ dùng để kiểm tra/ghi chú chênh lệch version (xem mục "Version công cụ trong sách cũ hơn hiện tại" bên dưới), không dùng để viết lại hay nâng cấp app trong lesson.

## Vấn đề / rủi ro đang theo dõi

- ✅ **[Đã xử lý] Giấy phép `aaf-materials/` cấm dùng cho mục đích giảng dạy.** Phát hiện 2026-08-26 khi chuẩn bị Task 7: file `aaf-materials/LICENSE` (và header lặp lại trong 404/495 file `.kt`) có điều khoản cấm dùng code cho "any work... designed, intended, or marketed for pedagogical or instructional purposes related to programming". Chi tiết → `docs/SOURCE_MAP.md` mục 2.5. **User đã quyết định (Quyết định #7): tiếp tục dùng code thật**, chấp nhận rủi ro vì là dự án cá nhân đã mua bản quyền, không public. Không còn là blocker — Task 7 tiếp tục bình thường.
- **Ảnh minh hoạ trong sách chưa được copy ra ngoài.** `scripts/extract_epub.py` chỉ chèn link `graphics/imgNN.png` (đường dẫn tương đối bên trong EPUB) kèm comment ghi chú, chưa copy file ảnh thật ra `content/book/`. Lesson Chapter 5 (Task 7) hoá ra không cần ảnh (chỉ giải thích + code), nên chưa xử lý. Cần quyết định trước khi có lesson nào thật sự cần hiển thị ảnh/screenshot từ sách.


- **Version công cụ trong sách cũ hơn hiện tại** (đã xác minh 2026-08-26): sách dùng Kotlin 1.9.10 / AGP 8.2.0 / Room 2.5.2; hiện tại bản ổn định là Kotlin 2.4.x / AGP 9.2.0 / Room 2.8.4. Khi viết lesson liên quan phải **ghi chú rõ chênh lệch**, không âm thầm đổi nội dung sách.
- **Sách dùng DI thủ công, không Hilt/Koin/Dagger** trong toàn bộ 11 chapter — là lựa chọn sư phạm có chủ đích (giảm phức tạp cho người mới), khác khuyến nghị chính thức của Android cho app production. Cần ghi chú khi dạy phần kiến trúc/DI.
- **Project gốc (thư mục `d:\book-course-vibe`) chưa có git ở cấp root** — chỉ `aaf-materials/` có git riêng (không đụng tới). Có thể cân nhắc `git init` ở root để version-control các file mới (docs/, content/, web/) sau này, nhưng đây là quyết định cần hỏi user trước, chưa thực hiện.
- ✅ **[Đã xử lý ở Task 32] Source/artifact lồng và file tạm trong `web/`** (phát hiện khi audit 2026-08-27, xử lý 2026-08-27): `web/web/`, `test-minimal.astro`, 14 file log/script tạm, file 0 byte `',`, `dist`+`node_modules` lạc trong `web/src/components/lessons/` — đã xoá sau khi salvage phần nội dung thật sự tốt hơn (quiz Chapter 3, 4 câu) từ `web/web/`. Bài học giữ lại: **mô tả artifact trong file kế hoạch phải được kiểm lại bằng cách mở file ra xem trước khi xoá** — 2 mô tả cũ của chính task này đều sai (xem Task 32).
- **Cảnh báo Node nhẹ khi `npm install`:** môi trường hiện tại Node 22.14.0, trong khi dependency gián tiếp `undici@8.10.0` (qua `astro -> unifont`) khai báo cần Node >=22.19.0. Hiện `astro check`, `astro build` và dev server đều chạy thành công nên chưa là blocker; nên nâng Node trong task môi trường riêng, không gộp vào lesson.
- ✅ **[Đã xử lý] Task 12 và Task 17 từng dở dù route đã render** — cả hai đã hoàn thành 2026-08-27 (citation Chapter 2 đủ số dòng; lesson Chapter 8 đủ 6 mục). Vẫn giữ nguyên tắc: không được hiểu “route HTTP 200” là “lesson hoàn thành” — luôn kiểm tra nội dung thật.
- **Kế hoạch triển khai Task 11** được lưu tại `docs/superpowers/plans/2026-08-27-chapter-1-lesson.md` để tham khảo lại nếu cần, không phải tài liệu bắt buộc đọc cho các task khác.

## Ghi chú cho session mới

Nếu nghi ngờ nội dung file này đã cũ so với code thực tế (ví dụ: thấy `web/` đã tồn tại nhưng ở đây ghi "chưa tạo"), **kiểm tra project thật trước, báo lệch cho user, rồi sửa lại file này** — không tự ý tin theo một trong hai nguồn mà không kiểm tra.
