# PROJECT PLAN — Web học Android bằng tiếng Việt

> Đây là **nguồn sự thật về tiến độ** của dự án. Đọc file này trước khi trả lời `review plan`, `đang tới đâu rồi?`, `tiếp tục`, v.v. — xem quy tắc xử lý trong `CLAUDE.md`.

**Cập nhật lần cuối:** 2026-09-12 — **REDESIGN: COMPLETE. FINAL CONTENT REVIEW: PASS. FINAL TECHNICAL QA: PASS.** (Ngày 2026-09-12 có thêm một đợt gia cố hậu đóng dự án — xem mục ngay dưới.)

## TRẠNG THÁI CUỐI CÙNG (2026-09-11) — DỰ ÁN ĐÃ ĐÓNG

- **Khoá học hiện hành:** 48 unit = **39 bài lõi** (2 nền tảng Kotlin + 7 giai đoạn: F, A×14, C×5, S×5, N×2, W×3, D×6, R×4, X×2) + **6 bài Mở rộng** (O1–O6, không bắt buộc) + **3 bài Phụ lục** tra cứu (AP1–AP3, không quiz). **8 redirect** cho URL cũ chết. SCHEMA_VERSION 13 · SPLIT_MAP 14 · ~22 giờ lõi (suy từ lessonStats, hiển thị trên trang chủ).
- **Redesign 2026-09 (spec v2 + 63 task IMP) đã chạy xong toàn bộ workstream:** pilot Ch10 (R1–R4) → Stage 1–7 (tách monolith, F/S/N bài mới) → quiz sweep (40 quiz cũ, 0 orphan) → Workstream F (O1–O6 + AP1–AP3) → Workstream G (Model B-lite: hoàn thành = tự xác nhận ∧ đã nộp quiz ≥1 lần, không ngưỡng điểm) → Workstream H (homepage roadmap v2 + sidebar theo stage, bỏ đánh số 5.1 learner-facing) → final content review (GATE PASS @ a74802d, freshness reconcile @ d084eed/f24b63c: nav 2.10.1 / Nav3 1.1.7 / AGP 9.4.0, web-checked 2026-09-11) → **final technical QA (GATE PASS — xem `docs/QA_FINAL_REPORT.md`)**.
- **Gate reports:** `docs/REVIEW_GATE_*.md` (mỗi workstream một file) + `docs/QA_FINAL_REPORT.md` (QA cuối). Quy tắc 30 phút cũ đã thay bằng chính sách quality-first — xem `docs/LESSON_TEMPLATE.md` đầu file.
- **Còn lại (bảo trì tuỳ chọn, KHÔNG phải việc dở):** nếu Android phát hành version mới đáng kể → cập nhật các claim version-sensitive (AP3 là bảng tra cứu nhanh); không có task nào đang mở.

---

## Cập nhật 2026-09-12 — gia cố hậu đóng dự án (6 commit)

Không nằm trong roadmap gốc; phát sinh từ một lượt review chất lượng phần trình
bày. Không đổi kiến trúc, không đổi số liệu hiển thị (`check:counts`: 0 lệch).

- **Bịt lỗ hổng luyện tập: 8/48 → 48/48 bài có khối luyện tập** (thêm 115 bài
  tập nhỏ). Trước đó 40 bài không có phần thực hành nào.
- **823 tham chiếu "mục N" được link hoá** (trước: 9). Trong đó 31 tham chiếu
  từng bị coi là "không link được" hoá ra chỉ thiếu `id` trên `<h3>` — đã thêm
  20 anchor.
- **11 lỗi render im lặng do dấu `{ }`** trong template Astro (build vẫn xanh
  nhưng chữ biến mất). Đã escape thành `&#123;`/`&#125;`.
- **Chuẩn hoá cấu trúc:** 20 lỗi cứng → 0; thứ tự đuôi bài 10 biến thể → 1.
- **Sửa lỗi thật** trong `scripts-maintenance/check_counts.mjs`: dùng
  `URL.pathname` nên khi repo nằm trong thư mục có dấu cách thì mọi
  `readFileSync` đều ENOENT — tức script **chưa từng** chạy đúng. Đổi sang
  `fileURLToPath`.
- **CI `.github/workflows/course-check.yml` (mới)** — 5 tầng:
  structure → progress → build → counts → anchors.
- **`web/tools/check-anchors.py` (mới)** — kiểm 1.409 link nội bộ có trỏ tới
  `id` thật không; kết quả 0 hỏng. Checker đã được kiểm chứng biết báo đỏ bằng
  cách tiêm anchor sai vào `dist/`.

Đợt này **không** đụng tới quiz và **không** rút gọn các mục `#nguon` (độ dài ở
đó là đặc tính — trích dẫn tới từng dòng file gốc).

---

## LỊCH SỬ — Trạng thái tại thời điểm Task 48 (2026-08-31, giữ nguyên để truy ngược)

**PHASE 7 MỞ: khoá học phải đứng độc lập, không nói về sách + chương dài phải tách.** Task 47 (Chương 10 → 10.1–10.4) làm chuẩn mẫu **đã xong**. **Task 48 đang chạy:** đã áp chuẩn xong **Chương 1 (→ 1.1–1.4), Chương 2 (→ 2.1–2.3), Chương 3 (→ 3.1–3.4)** — cả ba đã tách, đã tách quiz, đã wiring, build sạch. Còn lại Ch04–Ch09 + Ch11. **User đang tạm dừng để tự khảo sát web trước khi cho ý kiến — không tự sang Chương 4.**

## Trạng thái hiện tại (lịch sử — snapshot 2026-08-31)

- **Phase:** 7 — **ĐÃ ĐÓNG (lịch sử: từng "ĐANG MỞ" trước redesign 2026-09)**. Phase 1–6 đã đóng. Toàn bộ phạm vi Phase 7 + redesign spec v2 đã hoàn tất — xem "TRẠNG THÁI CUỐI CÙNG" ở đầu file.
- **Task hiện tại:** **Task 48 đang chạy — TẠM DỪNG theo yêu cầu user** sau khi xong Chương 1/2/3. User muốn tự khảo sát web rồi cho ý kiến; **không tự động sang Chương 4**. Đã áp chuẩn Phase 7 xong cho Ch01 (→ 4 chương nhỏ), Ch02 (→ 3), Ch03 (→ 4). Chapter kế trong Task 48 khi được phép tiếp: **Ch04 Gradle**. (Xem mục Task 48 trong Phase 7 để biết trạng thái từng chapter + số đo.)
  - **LỆCH đã phát hiện & sửa 2026-08-31:** file plan này trước đó vẫn ghi "Task 48 chưa bắt đầu" (dòng 5 + dòng này), nhưng trên đĩa Ch01/Ch02/Ch03 **đã tách xong và đã wiring** từ các session trước — plan đã không được cập nhật lúc đó. Đã đối chiếu `lessons.ts` (22 route thật) + build sạch + đo lại giọng và ghi đúng trạng thái vào đây.
- **Bối cảnh mở Phase 6 (2026-08-29):** user yêu cầu redesign UI/UX theo hướng "Android Developer Learning Platform + Jetpack Compose + Material Design 3", **giữ nguyên 100% nội dung khoá học và chức năng**. Đã làm xong ở Task 46 (chi tiết trong Roadmap), gồm design system 8 file CSS, header có search + progress + theme, curriculum sidebar 3 module, dashboard, lesson page mới, code block có copy button, light+dark theme, responsive 375/tablet/1440. Nội dung khoá học **đã chứng minh không đổi** (md5 `e688285a164f793a33abe66d475b42d6`). Audit đối kháng tìm ra **6 defect thật** (đã sửa hết) + 3 defect có sẵn từ trước.
- **Bối cảnh mở Phase 5 (2026-08-28):** user phản hồi nội dung website "quá ngắn và quá sơ sài so với sách", yêu cầu depth phải theo source material. Đã đo bằng số từ prose (bỏ `<style>`/frontmatter) so với chapter tương ứng trong `content/book/`:

  | Chapter | Từ trong sách | Từ trên web | Tỷ lệ | Heading sách | Heading web |
  |---|---:|---:|---:|---:|---:|
  | Ch02 Android Studio | 3.734 | 645 | **17%** | 10 | 4 |
  | Ch05 Jetpack Compose | 6.349 | 1.209 | **19%** | 13 | 6 |
  | Ch04 Gradle | 5.360 | 1.342 | **25%** | 15 | 4 |
  | Ch10 Room | 4.648 | 1.318 | **28%** | 24 | 5 |
  | Ch08 Networking | 3.205 | 1.048 | 32% | 19 | 6 |
  | Ch03 Fundamentals | 4.395 | 1.464 | 33% | 13 | 6 |
  | Ch06 Advanced Compose | 3.797 | 1.375 | 36% | 7 | 5 |
  | Ch01 Welcome | 2.227 | 841 | 37% | 8 | 5 |
  | Ch09 DataStore | 2.294 | 976 | 42% | 13 | 3 |
  | Ch07 Architecture | 2.934 | 1.262 | 43% | 6 | 4 |
  | Ch11 Advanced Storage | 2.978 | 1.609 | 54% | 16 | 4 |

  Tỷ lệ từ **không phải KPI** (web viết lại gọn hơn sách là đúng, và web không cần lặp phần hướng dẫn bấm chuột từng bước). Nhưng khoảng cách heading là dấu hiệu thật: Ch10 sách có 24 mục, web chỉ 5 — nghĩa là **có concept trong sách chưa hề xuất hiện trên web**. Đó là thứ Phase 5 phải sửa.

  **Tiến độ depth pass — 11/11 chapter đã xong (đóng 2026-08-29):**

  | Chapter | Task | Heading sách → web | Từ prose web | Quiz |
  |---|---|---|---:|---|
  | Ch05 Jetpack Compose | 35 `[x]` | 13 → 14 `h2` + 21 `h3` + 5 `h4` | 9.410 | 10 câu |
  | Ch02 Android Studio | 36 `[x]` | 10 → 13 `h2` + 15 `h3` | 7.987 | 11 câu + 2 bài tập |
  | Ch04 Gradle | 37 `[x]` | 15 → 15 `h2` | 9.484 | 19 câu + 4 bài tập |
  | Ch06 Advanced Compose | 41 `[x]` | 7 → 20 `h2` | 10.424 | 19 câu + 4 bài tập |
  | Ch01 Welcome | 42 `[x]` | 8 → 12 `h2` | 9.799 | 19 câu + 4 bài tập |
  | Ch09 DataStore | 43 `[x]` | 13 → 18 `h2` + 5 `h3` | 9.169 | 19 câu + 4 bài tập |
  | Ch10 Room | 38 `[x]` | 24 → 23 `h2` + 14 `h3` | 14.604 | 19 câu + 4 bài tập |
  | Ch08 Networking | 39 `[x]` | 19 → 23 `h2` + 22 `h3` | 13.451 | 19 câu + 4 bài tập |
  | Ch03 Fundamentals | 40 `[x]` | 13 → 27 `h2` + 30 `h3` | 13.717 | 19 câu + 4 bài tập |
  | Ch07 Architecture | 44 `[x]` | 6 → 29 `h2` + 2 `h3` | 13.647 | 19 câu + 4 bài tập |
  | Ch11 Advanced Storage | 45 `[x]` | 16 → 21 `h2` + 23 `h3` | 12.145 | 19 câu + 4 bài tập |

  **Chuẩn đã ổn định từ Task 37 trở đi:** lesson ≈ 9–14 nghìn từ prose, mọi `##` heading của sách có mục tương ứng, quiz **19 câu + 4 bài tập** trong đó có 1 widget xếp thứ tự tương tác, và một mục riêng gom mọi chỗ **sách lệch project mẫu** kèm cột "nên theo cái nào".
- **Thứ tự đã làm:** tăng dần theo tỷ lệ bao phủ, nhưng **Ch05 làm trước tiên** để dựng chuẩn vàng (gold standard) về cấu trúc lesson sâu, rồi các chapter sau bám theo.
- **Blocker:** Không có.


## Roadmap & trạng thái

Ký hiệu: `[x]` đã xong · `[~]` đang làm · `[ ]` chưa làm.

### Phase 1 — Xử lý nguồn (không đụng Android code, chưa có website UI)

- `[x]` **Task 0 — READ-ONLY AUDIT** (khảo sát EPUB, Android project, tool/skill hiện có, đề xuất roadmap). Hoàn thành 2026-08-26. Kết quả chi tiết → `docs/SOURCE_MAP.md`.
- `[x]` **Task 1 — Thiết lập Project Memory** (`CLAUDE.md` + `docs/PROJECT_PLAN.md` + `docs/SOURCE_MAP.md`). Hoàn thành 2026-08-26.
- `[x]` **Task 2 — Script trích xuất EPUB → Markdown.** Hoàn thành 2026-08-26. Script: `scripts/extract_epub.py` (chỉ dùng `zipfile` + `xml.etree.ElementTree`, không dependency ngoài). Đã test trên chapter 1, 5 (yêu cầu ban đầu) + chapter 8 (test thêm riêng phần "Note" callout box). Trong lúc test phát hiện + sửa 1 lỗi: các cụm `<em>` liên tiếp sát nhau trong sách (dùng để in nghiêng tên file như `MainActivity.kt`) bị ghép sai thành `**` — đã sửa bằng cách gộp các thẻ in nghiêng/đậm liền kề trước khi xuất Markdown. Đã verify số lượng code-block trong Markdown khớp 1:1 với số `<pre class="code-block">` trong EPUB gốc (ch1: 1/1, ch5: 27/27). File output test hiện có trong `content/book/`: `ch01-welcome-to-android-kotlin.md`, `ch05-jetpack-compose.md`, `ch08-networking.md`.
- `[x]` **Task 3 — Chạy trích xuất toàn bộ.** Hoàn thành 2026-08-26. Chạy `python scripts/extract_epub.py --all` → sinh đủ **23/23** file Markdown trong `content/book/` (11 chapter + 12 phần front/back matter: giới thiệu, license, dedications, about the team, 3 section header, conclusion...), khớp đúng số mục trong `toc.xhtml`. **Không có cảnh báo** "the la ... chua duoc xu ly" nào ở stderr trên toàn bộ sách (nghĩa là mọi loại thẻ HTML trong sách đều đã được xử lý đúng, không có nội dung bị rơi rớt/xử lý sai). Số dòng mỗi file từ 9 đến 941 dòng — hợp lý theo độ dài gốc, không có dấu hiệu bị cắt cụt. Đã kiểm tra thủ công 3 file đại diện (License — văn bản pháp lý dài, About the Team — có kiểu trình bày đặc biệt "author-figure" chưa test trước đó, Room Database — nhiều annotation code như `@Entity`/`@Dao`/`@Query`) → đều đúng định dạng. Đã quét lại toàn bộ 23 file để đảm bảo lỗi ghép `**` phát hiện ở Task 2 không còn xuất hiện ở đâu khác — không thấy lỗi nào.
- `[x]` **Task 4 — Tạo `docs/LEARNING_MAP.md`.** Hoàn thành 2026-08-26. Mapping đầy đủ 11 chapter: Concept → File Android tham chiếu → Composable/Class minh hoạ → gợi ý Exercise/Quiz. Trong lúc làm task này phát hiện vấn đề bản quyền nghiêm trọng — xem mục "Vấn đề/rủi ro" bên dưới (BLOCKER cho Task 7).

### Phase 2 — Website skeleton + vertical slice

- `[x]` **Task 5 — Khởi tạo Astro project** trong `web/`. Hoàn thành 2026-08-26. `npm create astro@latest` (template minimal, TypeScript strict). Astro 7.2.7. Gặp và sửa 1 bug môi trường: npm trên máy này không tự cài native binding cho Windows (`npm/cli#4828`) — thiếu `@astrojs/compiler-binding-win32-x64-msvc`, `@rolldown/binding-win32-x64-msvc`, `lightningcss-win32-x64-msvc` khiến `astro dev` báo lỗi "Cannot find native binding". Đã sửa bằng cách khai báo rõ 3 package này trong `dependencies` của `web/package.json` (không phải optionalDependencies — đã thử và vẫn bị skip). Verify bằng cách xoá sạch `node_modules`+`package-lock.json` rồi `npm install` lại từ đầu 2 lần, `astro dev` chạy được, `curl http://localhost:4321/` trả về HTTP 200 mỗi lần.
- `[x]` **Task 6 — Layout cơ bản.** Hoàn thành 2026-08-26. Tạo `web/src/data/chapters.ts` (dữ liệu 11 chapter + 3 section dùng chung), `web/src/layouts/BaseLayout.astro` (sidebar cố định bên trái, 3 nhóm section, highlight chapter đang xem), `web/src/pages/index.astro` (trang chủ giới thiệu + card 11 chapter), `web/src/pages/chapters/[slug].astro` (trang placeholder cho mỗi chapter — chỉ có tóm tắt tiếng Việt tự viết + đường dẫn tham chiếu nguồn, CHƯA có code/nội dung lesson thật vì đang chờ quyết định license ở Task 7). Verify: `npm run dev` + `curl` tất cả 11 route `/chapters/chNN-.../` → HTTP 200, route giả `/chapters/ch99-fake/` → đúng HTTP 404; `astro build` sinh đủ 12 trang tĩnh (1 trang chủ + 11 chapter) không lỗi.
- `[x]` **Task 7 — Lesson hoàn chỉnh đầu tiên** (Chapter 5 — Jetpack Compose). Hoàn thành 2026-08-26. Tạo `web/src/components/lessons/Ch05JetpackCompose.astro`: 6 mục (Composable function, Layout Group Column/Row/Box/Surface, Modifier chaining, LazyColumn, `@Preview`, dữ liệu giả `FakeData`), mỗi mục có giải thích tiếng Việt tự viết (diễn giải theo `content/book/ch05-jetpack-compose.md`, không copy nguyên văn sách) + đoạn code thật trích từ `aaf-materials/05-jetpack-compose/projects/final/...` (ghi rõ file + số dòng ở mỗi đoạn: `MainActivity.kt` 44-55, `Conversation.kt` 103-119/146-183/208-219, `KodecochatAppBar.kt` 82-88, `FakeData.kt` 43-75) kèm syntax highlighting bằng component `<Code>` có sẵn của Astro (`astro:components`). Gắn vào `web/src/pages/chapters/[slug].astro` — chỉ render lesson đầy đủ khi `slug === "ch05-jetpack-compose"`, các chapter khác vẫn giữ placeholder cũ (nói rõ có thể xem ví dụ ở chapter 5). Dùng code thật theo Quyết định #7 (không sửa file gốc trong `aaf-materials/`).
- `[x]` **Task 8 — Quiz mẫu + bài tập nhỏ** cho lesson Chapter 5. Hoàn thành 2026-08-26. Tạo `web/src/components/lessons/Ch05Quiz.astro`: 3 câu quiz trắc nghiệm (đúng theo gợi ý trong `docs/LEARNING_MAP.md` — `@Composable`, `@Preview` dùng để làm gì, vì sao dùng `LazyColumn`), chấm điểm bằng vanilla JavaScript phía client (không dùng framework, đúng định hướng "gần như 0 JS trừ chỗ cần tương tác" đã chốt cho Astro), hiện giải thích đáp án sau khi nộp. Bài tập nhỏ: hướng dẫn tự đổi `Modifier.padding(horizontal = 16.dp)` trong `MessageUi` (Conversation.kt) và quan sát qua `@Preview`, có `<details>` gợi ý nếu cần.
- `[x]` **Task 9 — Progress tracking local.** Hoàn thành 2026-08-26. Tạo `web/src/lib/progress.ts` (đọc/ghi `localStorage`, key `hoc-android-tv:progress`, mảng slug đã học). Nút đánh dấu `web/src/components/ProgressToggle.astro` gắn ở đầu mỗi trang chapter. Sidebar (`BaseLayout.astro`) và trang chủ (`index.astro`) đọc localStorage lúc load để hiện dấu ✓ ở chapter đã học + dòng tóm tắt "đã học X/11 chapter" ở trang chủ. Toàn bộ bằng vanilla JS (không thêm dependency/framework). Verify: `astro build` sạch, kiểm tra HTML output có đủ `id="progress-btn"`, `id="progress-summary"`, `data-slug` trên card/nav-link.
- `[x]` **Task 10 — Registry lesson + ổn định website.** Hoàn thành 2026-08-27. Thay cơ chế `if slug === ...` trong `[slug].astro` bằng registry `web/src/data/lessons.ts` (`slug -> { Lesson, Quiz }`), rồi sửa các lỗi tích hợp khiến website không chạy đúng:
  - `Ch08Networking.astro`: đổi 6 đoạn code Kotlin nhiều dòng từ chuỗi `"..."` không hợp lệ sang template literal; giữ nguyên `${apiKey}` dưới dạng văn bản Kotlin.
  - `Ch06Quiz.astro`: sửa backtick bị nhân đôi/thừa và khôi phục `$count` trong code minh hoạ.
  - `Ch08Quiz.astro`: escape `{id}` để Astro không hiểu nhầm là biến JavaScript.
  - Registry: import và đăng ký đủ cặp lesson + quiz Chapter 2–11; dùng `Partial<Record<...>>` để Chapter 1 chưa có entry vẫn hiện placeholder đúng thiết kế.
  - Quiz Chapter 2–11: khai báo kết quả `querySelector<HTMLInputElement>()` để `astro check` hiểu đúng thuộc tính `.value`.
  - Công cụ: pin TypeScript `6.0.3` vì `@astrojs/check@0.9.10` không hỗ trợ TypeScript 7; loại thư mục source lồng `web/web/` khỏi phạm vi type-check nhưng không xoá vì chưa xác định nguồn gốc.
  - **Verify 2026-08-27:** `astro check` → 0 errors (còn 6 hints không chặn); `astro build` → exit 0, sinh 13 trang; smoke test website thật tại dev server → 12/12 route chính HTTP 200; Chapter 2–11 có `Quiz nhanh` và không còn placeholder, Chapter 1 vẫn giữ placeholder đúng roadmap.
  - **Review cấu trúc:** pattern Chapter 5 (component lesson + component quiz trong `web/src/components/lessons/`, code thật kèm file/dòng, quiz JS thuần) tiếp tục được dùng. Roadmap Task 11–20 vẫn theo khuôn: đọc EPUB + source thật → lesson → quiz 2–3 câu + 1 bài tập → đăng ký registry. Audit chỉ xác nhận **đủ cấu trúc**, chưa xác nhận toàn bộ độ chính xác kỹ thuật/nội dung Android của từng lesson.

### Phase 3 — Nhân rộng ra các chapter còn lại

- `[x]` **Task 11 — Lesson Chapter 1** (Welcome to Android & Kotlin). Hoàn thành 2026-08-27. Tạo `Ch01WelcomeToAndroidKotlin.astro` (5 mục: Android/Kotlin, sandbox + 4 app component, Manifest, resources, Gradle — mỗi mục có citation `content/book/ch01-welcome-to-android-kotlin.md` kèm số dòng) và `Ch01Quiz.astro` (3 câu quiz + 1 bài tập "Đọc nhãn Manifest"). Chapter không có project Android thật (`aaf-materials/01-welcome-to-android-and-kotlin/` chỉ có file `.keep`), nên đoạn code duy nhất là trích đoạn XML `AndroidManifest.xml` thật từ EPUB (dòng 55–76), không phải Kotlin — đã sửa lại mô tả sai "học Kotlin thuần trên Kotlin Playground" trong `docs/LEARNING_MAP.md` và hint sai "học Kotlin nền tảng" trong `[slug].astro`. Đã đăng ký `ch01-welcome-to-android-kotlin` vào `web/src/data/lessons.ts`. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/6 hints (không đổi so với trước); `astro build` → exit 0, 13 trang; smoke test dev server thật → 12/12 route chính HTTP 200 (gồm cả `/`), route giả 404, Chapter 1 có "Quiz nhanh" + không còn placeholder + hiện đúng `MainActivity`/`MAIN`/`LAUNCHER`; Chapter 2/8/11 vẫn giữ nguyên hành vi (regression check). Chưa audit toàn bộ độ chính xác nội dung Android ngoài phạm vi đối chiếu trực tiếp với EPUB.
- `[x]` **Task 12 — Lesson Chapter 2** (Getting Started With Android Studio). Hoàn thành 2026-08-27. Đối chiếu `MainActivity.kt` thật tại `aaf-materials/02-getting-started-with-android-studio/projects/final/app/src/main/java/com/kodeco/chat/MainActivity.kt`, bổ sung số dòng cho cả 3 trích dẫn trong `Ch02GettingStartedAndroidStudio.astro`: mục MainActivity (dòng 15–30), Composable Greeting (dòng 32–38), `@Preview` (dòng 40–46). Phát hiện thêm: đoạn code hiển thị cho mục MainActivity thiếu 1 dòng comment thật trong file gốc (`// A surface container using the 'background' color from the theme`, dòng 20) — đã bổ sung lại để code hiển thị khớp 100% với file thật. `Ch02Quiz.astro` đã có sẵn citation dòng đúng (dòng 25, 42–46) từ trước, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/6 hints (không đổi so với baseline); `astro build` → exit 0, 13 trang; smoke test dev server thật → route Chapter 2 hiển thị đúng "(dòng 15–30)"/"(dòng 32–38)"/"(dòng 40–46)" và dòng comment mới; regression check Chapter 1/5/8/11 + route giả 404 vẫn đúng.
- `[x]` **Task 13 — Lesson Chapter 3** (Android Fundamentals). **Đủ cấu trúc theo audit 2026-08-27:** lesson có giải thích + 8 code extract kèm file/dòng, quiz 2 câu + 1 bài tập, đã đăng ký. **Đã audit sâu độ chính xác ở Task 21 (2026-08-27) — xem chi tiết bên dưới.**
- `[x]` **Task 14 — Lesson Chapter 4** (Gradle Basics). **Đủ cấu trúc theo audit 2026-08-27:** dùng `build.gradle.kts` + `gradle/libs.versions.toml`, trích dẫn có file/dòng, quiz 3 câu + 1 bài tập, đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 15 — Lesson Chapter 6** (Advanced Jetpack Compose). **Đủ cấu trúc theo audit 2026-08-27:** có `MainViewModel.kt`, `ChatRoom.kt`, `JumpToBottom.kt` kèm dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 16 — Lesson Chapter 7** (Advanced Architecture). **Đủ cấu trúc theo audit 2026-08-27:** có `Repository.kt`, `RepositoryImpl.kt`, `DittoHandler.kt`, `MainActivity.kt` kèm dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 17 — Lesson Chapter 8** (Networking). Hoàn thành 2026-08-27. Mở rộng từ 1 mục lên 6 mục, đối chiếu số dòng thật trong `aaf-materials/08-networking/projects/final/...`: Coroutine — `viewmodels/RecipeViewModel.kt` dòng 95–111; StateFlow — dòng 73–74; collect Flow trong UI — `ui/recipes/RecipeList.kt` dòng 75–79; Retrofit interface — `network/SpoonacularService.kt` dòng 47–59; `RetrofitInstance` — dòng 61–79; gọi API chi tiết — `RecipeViewModel.kt` dòng 117–129. Phát hiện + sửa 2 lỗi nhỏ khi đối chiếu: thiếu `for id $id` trong message Timber, và `$apiKey` bị hiển thị sai thành `${apiKey}`. Không dùng `ui/MainScreen.kt` (chỉ là điều hướng tab, không liên quan networking, sách không đề cập) — đã ghi lý do vào `docs/LEARNING_MAP.md`. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (giảm từ 6 hint baseline vì 5 cảnh báo "declared but never read" của Chapter 8 nay đã hết do mọi code block đều được dùng); `astro build` → exit 0, 13 trang; smoke test dev server thật → cả 6 mục + số dòng hiển thị đúng, `$apiKey` hiển thị đúng không thừa ngoặc; regression Chapter 1/2/5/11 + route giả 404 vẫn đúng.
- `[x]` **Task 18 — Lesson Chapter 9** (Data Store). **Đủ cấu trúc theo audit 2026-08-27:** `Prefs.kt` + wiring app/UI có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 19 — Lesson Chapter 10** (Room Database). **Đủ cấu trúc theo audit 2026-08-27:** `RecipeDao.kt`, `RecipeDatabase.kt`, `RecipeRepository.kt` + Entity code có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.
- `[x]` **Task 20 — Lesson Chapter 11** (Advanced Storage). **Đủ cấu trúc theo audit 2026-08-27:** `SecurePrefs.kt`, encrypted `RecipeDatabase.kt`, app wiring có file/dòng; quiz 3 câu + 1 bài tập; đã đăng ký. Chưa audit toàn bộ độ chính xác nội dung Android.

### Phase 3b — Audit sâu độ chính xác từng chapter (chốt hướng 2026-08-27)

- `[x]` **Task 21 — Audit sâu Chapter 3** (Android Fundamentals). Hoàn thành 2026-08-27. Đối chiếu 8 đoạn code + citation trong `Ch03AndroidFundamentals.astro`/`Ch03Quiz.astro` với file thật `aaf-materials/03-android-fundamentals/projects/{final,starter}/...`. Phát hiện + sửa 2 lỗi: citation `strings.xml` sai (ghi "dòng 35-39", đúng phải "dòng 34-40" vì đoạn code hiển thị gồm cả thẻ `<resources>`); bài tập nhỏ ghi sai tên file "starter/Activity.kt" (đúng là `MainActivity.kt`) và thiếu citation dòng — đã sửa thành đường dẫn đúng + dòng 66–72/69. 6 đoạn code còn lại đối chiếu khớp 100%, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint; `astro build` → exit 0, 13 trang; smoke test dev server thật → citation mới hiển thị đúng, citation cũ sai không còn xuất hiện; regression Chapter 1/2/5/8/11 + route giả 404 vẫn đúng.
- `[x]` **Task 22 — Audit sâu Chapter 4** (Gradle Basics). Hoàn thành 2026-08-27. Đối chiếu 9 đoạn code + citation trong `Ch04GradleBasics.astro`/`Ch04Quiz.astro` với file thật `aaf-materials/04-gradle-basics-a-look-behind-the-curtain/projects/{final,starter}/...`. Phát hiện + sửa 2 lỗi: citation `starterRootBuildGradleCode` sai (ghi "dòng 2–4", đúng phải "dòng 2–5" vì đoạn code hiển thị gồm cả dấu `}` đóng khối); callout "Ghi chú phiên bản" nói sai là sách dùng AGP 8.2.0 giống code mẫu — thực ra sách ví dụ bằng AGP 8.1.4 (chỉ Kotlin 1.9.10 khớp), đã sửa lại đúng chênh lệch. Sửa thêm 1 câu callout dễ hiểu nhầm về mốc xuất hiện của app Kodeco Chat. 8 đoạn code còn lại đối chiếu khớp 100%, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → citation/nội dung mới hiển thị đúng, nội dung cũ sai không còn xuất hiện; regression Chapter 1/2/3/5/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 23 — Audit sâu Chapter 6** (Advanced Jetpack Compose). Hoàn thành 2026-08-27. Đối chiếu 6 đoạn code + citation trong `Ch06AdvancedJetpackCompose.astro` với file thật `aaf-materials/06-advanced-jetpack-compose/projects/final/app/src/main/java/com/kodeco/chat/{viewmodel/MainViewModel.kt,conversation/JumpToBottom.kt,data/model/ChatRoom.kt}`. Cả 6 citation khớp chính xác 100% — không có lỗi lệch dòng. Phát hiện 1 điểm cần ghi rõ: comment thật trong `MainViewModel.kt` ghi "see chapter 8" cho Data Store, trong khi sách gọi đúng là Chapter 9 — đã thêm chú thích trong lesson để làm rõ chênh lệch này. `Ch06Quiz.astro` đối chiếu đúng, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → route Chapter 6 HTTP 200, chú thích mới hiển thị đúng; regression Chapter 1/2/3/4/5/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 24 — Audit sâu Chapter 7** (Advanced Architecture). Hoàn thành 2026-08-27. Đối chiếu 5 đoạn code + citation trong `Ch07AdvancedArchitecture.astro` với file thật `aaf-materials/07-advanced-architecture/projects/final/app/src/main/java/com/kodeco/chat/{data/repository/Repository.kt,data/repository/RepositoryImpl.kt,DittoHandler.kt,MainActivity.kt}`. Cả 5 citation khớp chính xác 100% — **không phát hiện lỗi nào** (chapter đầu tiên trong đợt audit sạch hoàn toàn, kể cả citation lẫn nội dung giải thích). Ghi nhận (không cần sửa): comment "Chapter 8, Data Store" trong `RepositoryImpl.kt` thật (cùng loại lỗi số chương như Task 23) nằm ngoài đoạn code được trích ở lesson này nên không gây mâu thuẫn cho người đọc. `Ch07Quiz.astro` đối chiếu đúng, không cần sửa. **Verify 2026-08-27:** `astro check` → 0 errors/1 hint; `astro build` → exit 0; smoke test dev server thật → route Chapter 7 HTTP 200; regression Chapter 1/2/3/4/5/6/8/11 + trang chủ + route giả 404 vẫn đúng.
- `[x]` **Task 25 — Audit sâu Chapter 9** (Data Store). Hoàn thành 2026-08-27. Đối chiếu 4 đoạn code + citation trong `Ch09DataStore.astro` với file thật `aaf-materials/09-data-store/projects/final/app/src/main/java/com/kodeco/recipefinder/{data/Prefs.kt,RecipeApp.kt,MainActivity.kt,ui/MainScreen.kt}`. Nội dung code khớp 100%. **Đã sửa 3 lỗi:** (1) citation `recipeAppCode` ghi "dòng 9–20" nhưng đoạn hiển thị thật là dòng 9–21 (thiếu dấu `}` đóng class) → sửa thành 9–21; (2) `Ch09Quiz.astro` đáp án sai câu 3 lỗi chính tả "Lưu trữ trữ dữ liệu key-value thay thế DataStore" → "Lưu trữ dữ liệu key-value thay cho DataStore"; (3) phần giải thích câu 3 câu tối nghĩa "không cần truyền tham số tay nhiên" → "không cần truyền tham số thủ công qua từng hàm". Các citation khác đã đúng: `prefsCode` dòng 12–43, `localPrefsProviderCode` dòng 30–31 + 44–48, `mainScreenCode` dòng 22–38, và 2 citation trong văn xuôi (`MainScreen.kt` dòng 46–49 / 56–59). Version DataStore `prefsVersion = "1.0.0"` trong `libs.versions.toml` thật khớp callout. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (không đổi); `astro build` → exit 0, route Chapter 9 build ra `dist/`; smoke test dev server thật → Chapter 9 HTTP 200, các citation đã sửa hiện đúng, chuỗi lỗi cũ = 0; regression toàn bộ 11 chapter + trang chủ HTTP 200, route giả 404, các chỗ sửa Task 22/23/24 còn nguyên.
- `[x]` **Task 26 — Audit sâu Chapter 10** (Room Database). Hoàn thành 2026-08-27. Đối chiếu 4 đoạn code + citation trong `Ch10RoomDatabase.astro` với file thật `aaf-materials/10-room-db/projects/final/app/src/main/java/com/kodeco/recipefinder/data/{database/RecipeDb.kt,database/RecipeDao.kt,database/RecipeDatabase.kt,RecipeRepository.kt}`. **Cả 4 citation khớp chính xác** (`recipeDbCode` dòng 43–67; `recipeDaoCode` dòng 44–63 — trùng khớp từng dòng kể cả `}` đóng interface; `recipeDatabaseCode` dòng 42–74; `recipeRepositoryCode` dòng 43–101). Quiz 3 câu + bài tập nhỏ (kể cả citation "khoảng dòng 49-50" của `findRecipeById`) đều đúng. **Đã sửa 2 chỗ:** (1) callout phiên bản nói "Room 3.0 … mới ở giai đoạn alpha, chưa ổn định" — **sai tính đến 2026-08-27**: `androidx.room3` đã stable từ 3.0.0 (01-07-2026), bản mới nhất 3.0.2 (26-08-2026); đã viết lại callout với thông tin đúng, ghi rõ Room 2.5.2 của sách khớp với code mẫu thật, Room 2.x (2.8.4) đang ở chế độ bảo trì, Room 3.0 có breaking change (bắt buộc KSP, bỏ `SupportSQLite`, coroutine-based) nhưng annotation cốt lõi + Repository pattern học ở chapter vẫn dùng được; (2) ghi chú nguồn của `recipeDatabaseCode` chỉ nói "bỏ bớt comment `@Volatile`" trong khi đoạn hiển thị còn **dịch comment `// only one thread…` sang tiếng Việt** — đã ghi rõ cả 2 thay đổi để người đọc không nhầm là copy nguyên văn. **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (không đổi); `astro build` → exit 0, route Chapter 10 build ra `dist/`; smoke test dev server thật → Chapter 10 HTTP 200, 4 citation + "3.0.2"/"2.8.4"/"2.5.2"/"androidx.room3" hiện đúng, chuỗi cũ "chưa ổn định"/"alpha" = 0; regression 11/11 chapter + trang chủ 200, `ch99-fake` → 404, các sửa của Task 22/23/25 còn nguyên.
- `[x]` **Task 27 — Audit sâu Chapter 11** (Advanced Storage). Hoàn thành 2026-08-27. **Chapter cuối của đợt audit này** → hoàn tất mục tiêu "audit sâu 7 chapter còn lại" user chọn ngày 2026-08-27. Đối chiếu **7 citation** trong `Ch11AdvancedStorage.astro` với file thật (`aaf-materials/11-advanced-storage/projects/final/.../recipefinder/`: `data/SecurePrefs.kt` dòng 41–54 + 56–74, `data/database/RecipeDatabase.kt` dòng 40–42 + 50–89, `RecipeApp.kt` dòng 58–69, `MainActivity.kt` dòng 66–67; cộng 1 citation so sánh sang chapter 10: `10-room-db/.../RecipeDatabase.kt` dòng 55–72) → **cả 7 citation khớp chính xác 100%**, không lỗi lệch dòng. Quiz 3 câu (`a`, `c`, `b`) + bài tập nhỏ đối chiếu đúng. **Phát hiện & sửa 3 lỗi:** (1) **claim kỹ thuật SAI** — lesson viết `SecurePrefs` "có chữ ký giống hệt class `Prefs` cũ"; đối chiếu file thật thì **khác 2 điểm thật sự**: `SecurePrefs` bỏ hết `suspend` (vì `EncryptedSharedPreferences` dùng API `SharedPreferences` đời cũ `edit().apply()`, không async như DataStore), và `getInt` đổi kiểu trả về từ `Int?` (ch09) sang `Int` (mặc định 0) → đã viết lại thành 1 đoạn + 2 gạch đầu dòng nêu rõ 2 khác biệt, kèm quan sát: `ui/MainScreen.kt` dòng 68 vẫn giữ `if (currentIndex != null)` từ chapter trước, với kiểu `Int` không nullable thì điều kiện này **luôn đúng → đã thành code dư thừa** (lỗi nhỏ trong code mẫu gốc của Kodeco, ghi lại làm điểm học); (2) callout phiên bản **thiếu và sai một phần** — bản cũ chỉ nói security-crypto deprecated và khuyên "DataStore + Tink" (trang release notes của Google **không** nói vậy), lại **bỏ sót hoàn toàn** việc `net.zetetic:android-database-sqlcipher` cũng đã bị khai tử; đã viết lại callout thành 2 gạch đầu dòng có nguồn: security-crypto deprecated từ 1.1.0-alpha07 (09-04-2025), stable cuối 1.1.0 (30-07-2025), Google khuyên dùng Android Keystore trực tiếp + Tink (theo Security checklist), **không có class thay thế 1-đổi-1**; SQLCipher: `android-database-sqlcipher` khai tử chính thức 31-08-2023 (cùng SQLCipher 4.5.5), thay bằng `net.zetetic:sqlcipher-android`, package đổi `net.sqlcipher.database` → `net.zetetic.database.sqlcipher`, `SupportFactory` → `SupportOpenHelperFactory`, bản mới nhất SQLCipher **4.18.0 (18-08-2026)** đã hỗ trợ Room 3; (3) lỗi chính tả trong `Ch11Quiz.astro` giải thích câu 2: "lớp lưu trữ phía dưồng" → "phía dưới". Đã kiểm tra thêm: `security = "1.0.0"` + `sqlcipher = "4.4.0"` trong `libs.versions.toml` thật **khớp đúng** version sách nêu (dòng 247, 407) → không có sai lệch sách-vs-code; ghi chú sẵn có "sách đặt code ở `MainActivity` nhưng code thật đặt ở `RecipeApp.onCreate()`" đã **xác minh đúng** (sách dòng 485 nói "Return to *MainActivity*", code thật ở `RecipeApp.kt` dòng 58–69). **Verify 2026-08-27:** `astro check` → 0 errors/0 warnings/1 hint (hint cũ ở `Ch03Quiz.astro`, không liên quan); trong lúc sửa có phát sinh 2 lỗi build do viết `launch { ... }` thẳng trong JSX (Astro hiểu `{` là biểu thức) → đã sửa thành HTML entity và check lại sạch; `astro build` → exit 0, route Chapter 11 build ra `dist/`; smoke test dev server thật (`http://localhost:4323`) → Chapter 11 HTTP 200, đủ 7 citation + các mốc "1.1.0-alpha07"/"09-04-2025"/"30-07-2025"/"31-08-2023"/"4.18.0"/"sqlcipher-android"/"SupportOpenHelperFactory"/"Tink" và 2 đoạn mới, 3 chuỗi lỗi cũ đều = 0; regression đủ 11/11 chapter + trang chủ HTTP 200, route giả `ch99-fake` → 404, các chỗ sửa của Task 22 (Ch04), 23 (Ch06), 25 (Ch09), 26 (Ch10) vẫn còn nguyên.

### Phase 3c — Audit sâu 4 chapter còn lại (Chapter 5, 8, 1, 2 — chốt hướng 2026-08-27)

Lý do mở phase này: Chapter 5 và 8 được viết ở Task 7/17, **trước khi** có quy trình audit chặt (kiểm từng số dòng citation) áp dụng từ Task 21. Chapter 1/2 không có project Android nên rủi ro thấp hơn, để sau.

- `[x]` **Task 28 — Audit sâu Chapter 5** (Jetpack Compose). Hoàn thành 2026-08-27. Đối chiếu **6 citation** trong `Ch05JetpackCompose.astro` + 1 citation trong `Ch05Quiz.astro` với file thật `aaf-materials/05-jetpack-compose/projects/final/app/src/main/java/com/kodeco/chat/`. **Phát hiện & sửa 6 lỗi** (chapter nhiều lỗi nhất từ đầu đợt audit, đúng như dự đoán vì viết trước khi có quy trình kiểm dòng):
  - **2 lỗi lệch dòng citation:** (a) `conversationContentCode` ghi "dòng 103–119" nhưng đoạn hiển thị bắt đầu từ `@Composable`/`fun ConversationContent` = **dòng 95**, dòng 103 mới là `Surface()` → sửa thành "dòng 95–119"; (b) `modifierChainCode` ghi "dòng 208–219" nhưng `Image(` mở ở **dòng 207** (208 là `modifier = Modifier`) → sửa thành "dòng 207–219". Cùng lỗi này trong bài tập của `Ch05Quiz.astro` ("khoảng dòng 208") → sửa thành "dòng 207–219" + nêu `MessageUi` bắt đầu ở dòng 186.
  - **2 chỗ sửa code mà không khai báo** (vi phạm nguyên tắc truy ngược nguồn): `messagesCode` gộp biến `val userId` (dòng 167 file thật) vào thẳng chỗ dùng nó và bỏ tham số bị comment sẵn `// scrollState`, nhưng ghi chú chỉ nói "rút gọn"; `fakeDataCode` gộp `private val initialMessages` (dòng 66–70) vào thẳng `exampleUiState` — file thật khai báo riêng. Đã ghi rõ từng thay đổi vào ghi chú nguồn.
  - **1 lỗi bài tập không làm được như hướng dẫn:** bài tập bảo đổi `padding` của avatar rồi "mở `@Preview` của `KodecochatAppBarPreview` … để quan sát". Kiểm tra thật: cả project chỉ có **3 `@Preview`** (`components/KodecochatAppBar.kt` dòng 82 + 91, `conversation/UserInput.kt` dòng 120) — **không có preview nào vẽ bong bóng chat**, nên làm theo hướng dẫn sẽ không thấy gì thay đổi. Đã sửa: nói rõ file `Conversation.kt` không có `@Preview` nào, phải chạy app thật hoặc tự thêm `@Preview` cho `MessageUi`. Bổ sung cách tự kiểm chứng bằng chính code: 42 + 16×2 = 74 = đúng `Modifier.width(74.dp)` của `Spacer` ở dòng 222.
  - **1 lỗi tham chiếu chéo sai trong lesson:** câu "`KodecochatTheme { }` … áp màu/font riêng của app (**xem mục 6**)" — mục 6 là "Dữ liệu giả", hoàn toàn không nói về theme; theme thật nằm ở `theme/Themes.kt` dòng 109, không thuộc phạm vi lesson → đã sửa thành trỏ đúng file.
  - **Viết lại callout phiên bản.** Bản cũ ghi "AGP 9.2.0" (đã lạc hậu) và nói chênh lệch "chủ yếu ở version công cụ, không phải API" — đúng phần lớn nhưng **bỏ sót 1 thay đổi cấu hình thật sự**: `app/build.gradle.kts` dòng 76–78 của code mẫu dùng `composeOptions { kotlinCompilerExtensionVersion = "1.5.3" }`, từ **Kotlin 2.0** Compose compiler phát hành kèm trong Kotlin nên khối này bị bỏ, thay bằng plugin `org.jetbrains.kotlin.plugin.compose` (riêng `buildFeatures { compose = true }` vẫn giữ vì là thiết lập của AGP). Đã cập nhật mốc hiện tại: Kotlin **2.4** (2.4.0 tháng 6-2026, 2.4.20 dự kiến tháng 9-2026), AGP **9.3** (9.3.0 tháng 7-2026).
  - **Đã đối chiếu khớp 100%, không cần sửa:** `mainActivityCode` (dòng 44–55), `previewCode` (dòng 82–88), phần logic Kotlin của `messagesCode`/`fakeDataCode`, quiz 3 câu (`b`, `c`, `b`) đều đúng, và khẳng định "chapter 5 chưa có ViewModel" — xác minh bằng `find` không có file `*ViewModel*` nào trong project.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (hint cũ `Ch03Quiz.astro:3`, không đổi); `npx astro build` → exit 0, 13 trang, có `dist/chapters/ch05-jetpack-compose/index.html`; smoke test dev server thật (`http://localhost:4323`, pid 11380) → Chapter 5 HTTP 200, 6/6 citation mới hiện đúng, 17/17 chuỗi nội dung mới đều có, 7/7 chuỗi lỗi cũ = 0; regression 11/11 chapter + trang chủ = 200, `ch99-fake` → 404, các bản sửa Task 22/25/26/27 vẫn nguyên; `git status` trong `aaf-materials/` sạch (không sửa reference).
- `[x]` **Task 29 — Audit sâu Chapter 8** (Networking). Hoàn thành 2026-08-27. Đối chiếu **6 citation** trong `Ch08Networking.astro` + citation bài tập trong `Ch08Quiz.astro` với file thật `aaf-materials/08-networking/projects/final/app/src/main/java/com/kodeco/recipefinder/`. **Cả 6 citation khớp chính xác 100%** (`RecipeViewModel.kt` dòng 95–111, 73–74, 117–129; `RecipeList.kt` dòng 75–79; `SpoonacularService.kt` dòng 47–59 và 61–79) — chapter thứ 2 (cùng Chapter 7) sạch hoàn toàn ở lớp citation dù được viết ở Task 17 trước khi có quy trình kiểm dòng. Quiz 3 câu (`b`, `a`, `c`) + bài tập ("khoảng dòng 57–58", đáp án `RecipeInformationResponse`) đều xác minh đúng. **Nhưng phát hiện & sửa 4 lỗi nội dung** (khác loại với các chapter trước — không phải lệch dòng mà là thiếu/quá lời trong phần giải thích):
  - **Thiếu cảnh báo API key là chỗ trống.** `const val apiKey = "<Replace with API Key>"` — không thay bằng key thật thì mọi lời gọi mạng thất bại và danh sách công thức luôn trống, nhưng lesson không nói. Đã thêm `.hint`: phải tự đăng ký key miễn phí ở spoonacular.com rồi dán vào **dòng 47** của `SpoonacularService.kt`; kèm chính cảnh báo của sách (phần "Using Your API Key", `content/book/ch08-networking.md` dòng 314): không bao giờ commit key lên repo công khai, Google gợi ý `secrets-gradle-plugin`.
  - **Claim "StateFlow thay thế LiveData" quá mạnh** (ở cả tiêu đề mục 2 của lesson và giải thích câu 2 của quiz). Đối chiếu tài liệu chính thức: `LiveData` **chưa bị deprecated** tính đến 2026-08-27, vẫn chạy bình thường; chỉ là code Kotlin/Compose mới hầu như luôn chọn `StateFlow`. Chữ "replace" là cách nói của sách (Key Points, `ch08-networking.md` dòng 592) → đã ghi rõ chênh lệch ở cả 2 chỗ, giữ nguyên trích dẫn của sách thay vì âm thầm sửa.
  - **Giải thích `Dispatchers.Default` không phê phán.** Code thật `queryRecipe` (dòng 117–129) vừa khai báo `suspend` **vừa** bọc thân hàm trong `viewModelScope.launch(Dispatchers.Default)` — dư một lớp, khiến hàm trả về ngay và người gọi không thật sự chờ kết quả; ngoài ra `Dispatchers.Default` là dispatcher cho việc nặng CPU còn gọi mạng là I/O (`Dispatchers.IO`). Đã thêm 2 gạch đầu dòng "đọc code có phê phán", kèm ghi chú thực tế vẫn chạy đúng vì Retrofit/OkHttp tự chuyển sang thread pool riêng.
  - **Thiếu hoàn toàn callout phiên bản** (khác Ch05/06/10/11). Đã tra chéo Maven Central + changelog GitHub và thêm `.callout.note`: Retrofit `2.9.0` → mới nhất **3.0.0** (15-05-2025, nhánh 2.x cuối là **2.12.0** cùng ngày), 3.0.0 chỉ nâng OkHttp 3.14→4.12 và giữ tương thích nhị phân — annotation `@GET`/`@Query`/`@Path` **không đổi**; Moshi `1.15.0` → **1.15.2** (05-12-2024), `2.0.0-alpha.1` (28-01-2026) chưa ổn định; Timber `5.0.1` vẫn là bản mới nhất từ 2021. **Kết luận đáng ghi nhận: thư viện mạng của chapter này lỗi thời ít nhất trong cả sách**, chỉ Kotlin `1.9.10`/AGP `8.2.0` là cũ.
  - Bổ sung CSS thiếu trong `<style>` của `Ch08Networking.astro` (`.lesson p`, `.lesson ul`, `.lesson li`, `.hint`, `.callout.note`) — nếu không thì nội dung mới render không có style.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (hint cũ `Ch03Quiz.astro:3`); `npx astro build` → exit 0, 13 trang; smoke test dev server thật (`http://localhost:4323`) → Chapter 8 HTTP 200, 6/6 citation hiện đúng 1 lần, đủ marker nội dung mới, 2 chuỗi cũ = 0; regression 11/11 chapter + trang chủ = 200; `git status --porcelain` trong `aaf-materials/` sạch.
- `[x]` **Task 30 — Audit sâu Chapter 1 + 2.** Hoàn thành 2026-08-27 (**kết thúc Phase 3c → hoàn tất audit 11/11 chapter**). 2 chapter này không có project Android để đối chiếu code theo cách các chapter khác, nên trọng tâm chuyển sang: citation vào EPUB, tính chính xác của trích đoạn, và **độ lỗi thời của hướng dẫn bấm chuột**.
  - **Đã đối chiếu khớp, không cần sửa:** cả **3 citation của Chapter 2** khớp chính xác với file thật `aaf-materials/02-getting-started-with-android-studio/projects/final/.../com/kodeco/chat/MainActivity.kt` (47 dòng): `class MainActivity` dòng 15–30, `Greeting` dòng 32–38, `@Preview` dòng 40–46; bài tập trong `Ch02Quiz.astro` (`Greeting("Fuad")` ở dòng 25 và 44, `GreetingPreview` dòng 42–46) đều đúng. Quiz Ch02 (`b`,`b`,`a`) và Ch01 (`b`,`c`,`a`) đúng. Toàn bộ **6 citation EPUB của Chapter 1** (dòng 19–27, 29–49, 55–76, 78–86, 88–94, 96–104) khớp `content/book/ch01-welcome-to-android-kotlin.md`. Callout "thư mục tài liệu chỉ có file `.keep`" — xác minh đúng: `aaf-materials/01-welcome-to-android-and-kotlin/` chỉ chứa 4 file `.keep`.
  - **Sửa lỗi 1 — Chapter 1 trích code mà không khai báo đã cắt bớt** (cùng loại lỗi đã sửa ở Task 28): `manifestCode` bỏ dòng comment của sách ở dòng 67 (`<!-- Specifies the kind of Intents the Activity can respond to -->`) mà ghi chú nguồn không nói gì. Đã ghi rõ dòng bị bỏ + dịch nghĩa + lý do bỏ (ý đó đã được giải thích bằng tiếng Việt ở mục 3).
  - **Bổ sung 2 — Chapter 1 thiếu lý do chọn Kotlin.** Sách nêu Kotlin là ngôn ngữ **được Google khuyến nghị chính thức cho Android từ 2019** (`ch01-welcome-to-android-kotlin.md` dòng 25) nhưng lesson không dẫn — đã thêm.
  - **Bổ sung 3 — Chapter 2 mục 1 quá mỏng** (chỉ 1 câu "tải tại developer.android.com/studio", bỏ trắng toàn bộ luồng tạo project của sách). Đã thêm 4 thông tin thật của bước New Project: Name = `Kodeco Chat`, Package = `com.kodeco.chat`, Save location, **Minimum SDK = API 28 ("Pie", Android 9.0)**, và Build configuration language = **Kotlin DSL** (mặc định), kèm citation `ch02-...md` dòng 63–92.
  - **Sửa lỗi 4 — Chapter 2 thiếu callout phiên bản, dù đây là chapter lỗi thời nhất cả sách** (là chapter duy nhất hướng dẫn từng bước bấm chuột trong IDE → giao diện thật đã khác). Đã thêm `.callout.note` với 5 điểm đã tra chéo tài liệu chính thức (2026-08-27): (a) sách dùng **Android Studio Hedgehog** (2023) vs bản ổn định hiện tại **Quail 3 | 2026.1.3 Patch 1**, liệt kê chuỗi chu kỳ Hedgehog → … → Otter → Panda → Quail (kế tiếp Rabbit 1 | 2026.2 đang preview); (b) **bước "Enable new UI" (sách dòng 51–56) không còn tồn tại** — New UI đã là giao diện mặc định từ **Ladybug Feature Drop** (theo developer.android.com/studio/intro/new-ui), muốn dùng Classic UI phải cài plugin từ JetBrains Marketplace → người đọc bỏ qua bước này; (c) System Image: sách ví dụ **UpsideDownCake / API Level 34**, hiện tại mới nhất là **Android 17 = API level 37** — cứ làm đúng tinh thần "chọn API Level cao nhất", con số khác sách là bình thường, **nhưng Minimum SDK vẫn để API 28** như sách yêu cầu (2 con số này hay bị lẫn); (d) máy ảo **Pixel 7 Pro** (sách dòng 141) có thể không còn trong danh sách mặc định, chọn Pixel nào cũng được; (e) sách mặc định **macOS** (dòng 28) — trên Windows/Linux menu Settings ở `File ▸ Settings`. Ghi thêm project mẫu dùng Kotlin `1.9.10`/AGP `8.2.0`/`compileSdk 34` (đã đọc `build.gradle.kts` thật) vs Kotlin 2.4/AGP 9.3 hiện tại.
  - Bổ sung CSS thiếu trong `<style>` của `Ch02GettingStartedAndroidStudio.astro` (`.lesson p`, `.lesson ul`, `.lesson li`, `.hint`, `.callout`/`.callout.note`) cho khớp các lesson khác.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, 0 errors / 0 warnings / 1 hint (không đổi); `npx astro build` → exit 0, 13 trang; smoke test dev server thật (`http://localhost:4323`) → Chapter 1 và Chapter 2 HTTP 200, 9/9 marker nội dung mới hiện đúng (`Quail 3 | 2026.1.3 Patch 1`, `Ladybug Feature Drop`, `API level 37`, `Enable new UI`, `Kodeco Chat`, `API 28` ×2, dòng comment bị bỏ, mốc 2019); regression 11/11 chapter + trang chủ = 200.

### Phase 4 — Hoàn thiện để dùng được (mở 2026-08-27)

- `[x]` **Task 31 — README hướng dẫn chạy cho người non-tech.** Hoàn thành 2026-08-27. Tạo mới `README.md` ở **root project** (`d:\book-course-vibe\README.md`) — trước đó root **không có README nào**, chỉ có `web/README.md` là file mẫu mặc định của template "Astro Starter Kit: Minimal" (nói về `npm create astro`, `localhost:4321`, Discord của Astro — không liên quan gì tới dự án này). Đây là điều kiện "hoàn thành dự án" trong `CLAUDE.md` chưa từng có task nào phụ trách.
  - **Nội dung 5 phần, viết cho người không phải dân kỹ thuật:** (1) chạy website theo 6 bước đánh số — cài Node.js từ nodejs.org (bản LTS), mở Command Prompt đúng thư mục bằng cách gõ `cmd` vào ô địa chỉ File Explorer, `npm install`, `npm run dev`, mở `http://localhost:4321`, tắt bằng `Ctrl + C`; (2) bảng 5 lỗi thường gặp kèm cách xử lý; (3) 4 lệnh khác (`build`/`preview`/`astro check`); (4) sơ đồ thư mục + bảng 3 file trong `docs/`; (5) các điều cần biết (chạy local, không sửa `.epub` và `aaf-materials/`, chỉ dùng cá nhân nên không public, muốn làm bài tập thì cần thêm Android Studio).
  - **3 chi tiết đã xác minh thật trước khi viết, thay vì chép mặc định:** cảnh báo `npm warn` là bình thường (đúng với cảnh báo Node 22.14.0 vs `undici@8.10.0` đang có trong môi trường này); **cổng có thể không phải 4321** — README dặn đọc dòng `Local` trong terminal, vì thực tế dev server của project đang chạy ở `4323` do 4321/4322 bị chiếm; script khả dụng lấy từ `package.json` thật (`dev`/`build`/`preview`/`astro`), không bịa lệnh không tồn tại.
  - **Verify 2026-08-27:** đối chiếu `web/package.json` (4 script, `engines.node >= 22.12.0`), `node -v` = v22.14.0, `npm -v` = 10.9.2; xác nhận `docs/` có đúng 3 file `.md` được nhắc tên (`PROJECT_PLAN.md`, `SOURCE_MAP.md`, `LEARNING_MAP.md`); trang chủ dev server trả về đúng **11 link chapter** như README mô tả; `web/dist/` tồn tại đúng như phần mô tả lệnh `build`.
- `[x]` **Task 32 — Cleanup rủi ro tồn đọng.** Hoàn thành 2026-08-27. User đã cho phép tự chọn phạm vi ("cứ theo cách tốt nhất của bạn"). **Trước khi xoá đã mở từng thứ ra xem, và phát hiện 2 mô tả trong bản kế hoạch cũ của chính task này là SAI** — nếu xoá theo mô tả cũ sẽ mất nội dung thật:
  - **Sai #1:** `web/web/` bị ghi là "bản source không đầy đủ". Thực tế `diff` cho thấy `web/web/src/components/lessons/Ch03Quiz.astro` có **238 dòng, 4 câu quiz đầy đủ + bài tập 3 bước + `<details>` gợi ý**, trong khi bản đang chạy `web/src/components/lessons/Ch03Quiz.astro` chỉ có **81 dòng, 2 câu** (câu 2 chỉ có 2 lựa chọn a/b) và bài tập 1 đoạn văn. Bản đang chạy mới hơn về thời gian nhưng **nghèo nội dung hơn** — tức là một bước lùi. → Đã **salvage** (cứu) nội dung tốt hơn sang bản đang chạy trước khi xoá, đồng thời sửa 2 lỗi có thật trong bản 238 dòng đó: (a) escape sai `remember {`{ }`}` → dùng `&#123;`/`&#125;`; (b) **specifier định dạng chuỗi Android sai `%1\$1` → đúng là `%1$s`** (đã đối chiếu tài liệu Android: `%1` = tham số thứ 1, `s` = kiểu string). Cũng bổ sung mốc dòng thật đã kiểm: `starter/.../strings.xml` chỉ có 1 chuỗi `app_name` (dòng 35–37), `starter/.../MainActivity.kt` hàm `Greeting` dòng 66–72 với `text = "Hello $name!"` ở dòng 69.
  - **Sai #2:** `',` bị ghi là "1 thư mục tên rác". Thực tế là **1 file thường 0 byte** tên `',` (do lỗi quoting lệnh shell). Không phải thư mục.
  - **Giữ lại bản đang chạy** `web/src/data/lessons.ts` (50 dòng) và **không** lấy bản trong `web/web/` (41 dòng) — bản lồng cũ hơn, thiếu import quiz Chapter 1/2 và còn dòng `// TODO: Thêm Quiz sau khi sửa file`.
  - **Đã xoá (sau khi xác minh từng thứ):** file 0 byte `',`; **10 file log** (`build-error.txt`, `build-full.txt`, `build-out.txt`, `build-output.txt`, `build_err.txt`, `build_output.txt`, `build_verbose.txt`, `err.txt`, `out.txt`, `full_build_log.txt`); **4 script tạm sinh file** (`fix_ch03_quiz.py`, `tmp_fix_escape.cjs`, `tmp_write_ch08.cjs`, `write_ch02.py` — phát hiện thêm `write_ch02.py` chưa có trong danh sách cũ, tổng 14 file log/tạm chứ không phải 13); thư mục lồng `web/web/` (sau khi salvage xong); `web/src/pages/test-minimal.astro` (route thử `/test-minimal/`); `web/src/components/lessons/dist` (rỗng) + `web/src/components/lessons/node_modules` (1.2 MB cache Vite lạc chỗ).
  - **Sửa kèm:** bỏ `"web"` khỏi `exclude` trong `web/tsconfig.json` (không còn thư mục đó để loại trừ); bỏ `import { Code } from "astro:components"` không dùng ở `Ch03Quiz.astro:3` → **hết hint cuối cùng của `astro check`**.
  - **Verify 2026-08-27:** `npx astro check` → exit 0, **0 errors / 0 warnings / 0 hints** (31 file, giảm từ 34 do đã xoá file rác); `npx astro build` sau khi xoá sạch `dist` → exit 0, **12 trang** (giảm từ 13 vì đã bỏ `/test-minimal/`), đúng 11 chapter + trang chủ; HTML build ra của Chapter 3 có **4 `<fieldset>`** và chứa `%1$s`, **không còn** `%1\$1`; dev server thật → **12/12 route HTTP 200**, Chapter 3 live có 4 câu quiz, `/test-minimal/` trả **404** đúng như mong đợi; `git -C aaf-materials status --porcelain` rỗng → reference source không bị đụng.

- `[x]` **Task 33 — `git init` ở root + `.gitignore` bảo vệ nội dung bản quyền.** Hoàn thành 2026-08-27. Trước đó root **chưa có git** (chỉ `aaf-materials/` có repo riêng). Đã `git init -b main` và tạo `.gitignore` ở root với 5 nhóm quy tắc, quan trọng nhất là **nhóm 1 — loại nội dung có bản quyền khỏi git**: file `.epub` gốc (34 MB) và toàn bộ `content/book/` (nguyên văn sách đã trích). Lý do: theo `CLAUDE.md` văn bản sách chỉ dùng cá nhân; đưa vào git là bước đầu tiên dẫn tới rủi ro phát hành lại nếu sau này có ai push repo lên đâu đó. `content/book/` mất thì tạo lại được bằng `python scripts/extract_epub.py --all` (đã ghi chú ngay trong `.gitignore`). Nhóm 2 loại `aaf-materials/` (có repo git riêng, remote `kodecocodes/aaf-materials` — lồng vào sẽ xung đột). Nhóm 3–5 loại `node_modules`/`dist`/`.astro`, file log/tạm, file rác của OS/IDE.
  - **Commit đầu tiên:** `9e8800d` — **50 file** (docs 4, README + CLAUDE.md, `scripts/extract_epub.py`, toàn bộ `web/src` gồm 11 lesson + 11 quiz). Dùng identity git global có sẵn trên máy (`Duck1703`), **không sửa git config** theo đúng nguyên tắc.
  - **Verify 2026-08-27:** `git check-ignore` xác nhận đúng 5/5 đường dẫn cần loại trừ đều bị ignore (`.epub`, `content/book/*`, `aaf-materials/*`, `web/node_modules`, `web/dist`); `git ls-files | grep '\.epub$'` → **0 kết quả** (không có file sách nào trong git); `git ls-files | grep content/` → **0 kết quả**; `.git` chỉ nặng **379 KB** (bằng chứng gián tiếp là 34 MB sách không bị nhét vào); `git status --porcelain` rỗng sau commit. Đã quét trước khi commit: không có file `.env`, không có API key thật nào — chỗ duy nhất giống key là `"<Replace with API Key>"` (placeholder trong lesson Chapter 8, an toàn).
  - **Chưa làm và không tự làm:** không tạo remote, không push đi đâu — đúng nguyên tắc "không deploy/không public".

- `[x]` **Task 34 — Nâng Node lên >= 22.19.0.** Hoàn thành 2026-08-27. Nguyên nhân cảnh báo: dependency gián tiếp `undici@8.10.0` khai báo `engines.node >= 22.19.0` (đã xác minh trực tiếp từ `package.json` của nó), trong khi máy đang chạy Node 22.14.0. Máy đã có sẵn `nvm-windows 1.2.2` nên **không cần cài thêm phần mềm mới** — chỉ `nvm install 22.23.2` (bản 22.x mới nhất, cùng dòng LTS với bản cũ nên rủi ro thấp hơn nhảy sang 24.x) rồi `nvm use 22.23.2`. Giữ nguyên bản 22.14.0 và 23.10.0 đã cài, có thể `nvm use 22.14.0` để quay lại nếu cần.
  - **Một chi tiết dễ nhầm đã ghi lại:** trên máy này có **2 bản Node cùng nằm trong PATH** — `/c/nvm4w/nodejs/node` (bản do nvm quản lý, thắng vì đứng trước) và `/c/Program Files/nodejs/node` v22.22.0 (bản cài rời, đứng sau). Sau `nvm use`, `node -v` trong shell cũ có thể vẫn hiện bản cũ do shell cache đường dẫn; `hash -r` là xong. Không xoá bản trong `Program Files` vì chưa rõ có gì khác đang dùng nó.
  - **Verify 2026-08-27:** `node -v` = **v22.23.2**, `npm -v` = 10.9.8; **xoá sạch `web/node_modules` rồi `npm install` lại từ đầu** → exit 0, **0 dòng `npm warn`, 0 `EBADENGINE`** (trước đây luôn có cảnh báo engine) và 0 vulnerabilities; `npx astro check` → exit 0, 0 errors / 0 warnings / 0 hints; `npx astro build` sau khi xoá `dist` → exit 0, 12 trang; dev server thật (cổng 4322 sau khi `astro dev stop` bản cũ ở 4323) → **12/12 route HTTP 200**, route giả `ch99-fake` → **404**, `/test-minimal/` → **404**, Chapter 3 live vẫn đủ **4 câu quiz**.
  - **Đã sửa README:** dòng ghi Node của máy 22.14.0 → **22.23.2**.

### Phase 5 — Depth pass: viết lại lesson để bám độ sâu của sách (mở 2026-08-28)

**Lý do mở phase:** user yêu cầu rõ — website đang là *bản tóm tắt* sách, phải trở thành *learning experience* dựng trên sách. Quy tắc đầy đủ đã ghi vào `CLAUDE.md` mục "Working instructions (user chốt 2026-08-28)". Hai thay đổi lớn về cách làm việc: (1) **depth follows source material**; (2) **không dừng xin confirm giữa các task** — tự review rồi làm tiếp.

**Quy trình cho mỗi lesson trong phase này:**
1. Đọc **toàn bộ** chapter tương ứng trong `content/book/` (không chỉ đọc heading).
2. Lập inventory: concept chính · concept phụ · giải thích quan trọng · API/component · quyết định architecture · caveat tác giả nhấn mạnh · code example · liên hệ project.
3. Đối chiếu inventory với lesson hiện tại → tìm phần thiếu / nói qua loa / generic / thiếu liên hệ project.
4. Viết lại lesson: mỗi concept phải có *tại sao* trước *cách làm*, code phải được **giải thích từng phần**, mọi citation `file + dòng` phải mở file thật để kiểm.
5. Tự review theo 3 nhóm (content / technical / learning experience) trong `CLAUDE.md` mục D.
6. `npx astro check` + `npx astro build` + kiểm route thật → sang task tiếp theo, không dừng hỏi.

**Chuẩn "đủ sâu" (self-check, không phải KPI cứng):** mọi `##` heading của chapter trong sách phải có nội dung tương ứng trên web, hoặc có lý do rõ ràng vì sao bỏ (ví dụ: phần hướng dẫn bấm chuột từng bước trong Android Studio → thay bằng mô tả kết quả + cảnh báo phiên bản, vì screenshot chưa trích được).

- `[x]` **Task 35 — Viết lại Ch05 Jetpack Compose (gold standard).** Hoàn thành 2026-08-28. Sách 6.349 từ / 13 heading → web **9.410 từ prose (không tính code) / 14 `h2` + 21 `h3` + 5 `h4`**, 36 code block, file lesson 1.793 → 1.925 dòng. Mọi `##` heading của chapter đã có mục tương ứng, cộng thêm 2 mục web tự thêm: "Ghi chú phiên bản" và "Tóm lại — 6 điều sách chốt". Quiz nâng từ 3 lên **10 câu** (mỗi câu có giải thích, không chỉ đáp án). **Mọi citation `file + dòng` đều mở file thật để kiểm** (`starter/MainActivity.kt` 51–79 · `components/KodecochatAppBar.kt` 55–79, 81–97 · `conversation/Conversation.kt` 94–119, 121–144, 146–183, 185–235, 237–263, 265–289, 292–320, 322–352, 354–379 · `ConversationUiState.kt` 46–68 · `data/model/MessageUiModel.kt` 43–69 · `data/FakeData.kt` 43–75 · `theme/Typography.kt` 46–65 · `theme/Themes.kt` 107–109 · `conversation/UserInput.kt` 126–128). **4 chỗ sách ≠ project `final` đã ghi rõ thành nội dung dạy, không che:** (1) `ChannelNameBar` trong `final` thiếu `style = MaterialTheme.typography.titleMedium` mà sách có (dòng 361–363); (2) `// scrollState: LazyListState,` bị comment sẵn ở dòng 149; (3) `val pressedState` ở dòng 299 là code chết, không ai dùng; (4) sách yêu cầu xoá `SimpleUserInput` nhưng `final` vẫn giữ ở 121–144. **Cấu trúc này là chuẩn cho Task 36–45.**

  *Chuẩn cấu trúc rút ra (áp dụng cho các task sau):* mở đầu 3 callout (`goals` = sẽ học gì · app này là gì và cố tình thiếu gì · `journey` = starter có gì → final có gì, file nào copy / file nào tự viết) → các mục theo đúng thứ tự sách, mỗi mục *tại sao* trước *cách làm* → code luôn kèm `<p class="src">file + dòng</p>` và giải thích từng phần → `.hint` cho kiến thức bổ sung của mình (đánh dấu rõ là phụ) → `.notice` / `.callout diff` cho chỗ sách lệch project → đóng bài bằng "Ghi chú phiên bản" + Key Points của sách + callout "việc dở dang sang chapter sau".
- `[x]` **Task 36 — Viết lại Ch02 Getting Started With Android Studio.** Hoàn thành 2026-08-28. Sách 3.734 từ / 10 heading → web **7.987 từ prose (không tính code) / 13 `h2` lesson + 15 `h3`**, 14 code block, 7 bảng, file lesson 111 → 1.113 dòng. Mọi `##` heading của sách đều có mục tương ứng. **Web thêm 3 mục sách không có:** (mục 7) đọc từng file của project vừa sinh ra — chia 5 mục con `MainActivity.kt` / `AndroidManifest.xml` / `ui/theme/` 3 file / 2 file Gradle / `strings.xml`+`settings.gradle.kts`; bảng đối chiếu "sách 2023 vs hiện nay" 6 dòng; Key Points của sách được mở rộng bằng callback vào từng mục. Quiz nâng từ 3 lên **11 câu** + thêm bài tập 2 (đọc `build.gradle.kts` trả lời 3 câu). **Citation đã mở file thật để kiểm:** `MainActivity.kt` 15–30 / 32–38 / 40–46 · `AndroidManifest.xml` 1–28 · `app/build.gradle.kts` 6–20 · gốc `build.gradle.kts` 1–5 · `ui/theme/Theme.kt` 40–69 · `Type.kt` 10–33 · `Color.kt` 5–11 · `res/values/strings.xml` · chapter 2 dòng 11, 28, 65, 79, 84–92, 101, 105–113, 117/173, 131–169, 175–212, 210, 217–224, 230–256, 265–280, 287–301, 312–319, 321–336, 340–346. **3 chỗ sách ≠ project đã ghi rõ thành nội dung dạy:** (1) sách yêu cầu `Minimum SDK = API 28` nhưng project ship `minSdk = 30`; (2) sách in `Greeting("Android")` còn `final` là `Greeting("Fuad")` (tên tác giả — vì `final` là trạng thái sau khi làm bài); (3) theme chapter 2 là `KodecoChatTheme` trong `com.kodeco.chat.ui.theme`, chapter 5 đổi thành `KodecochatTheme` trong `com.kodeco.chat.theme`. **Cách xử lý 38 screenshot không có ảnh:** mô tả *bạn đang tìm cái gì và vì sao cần nó* thay cho *bấm nút ở đâu*, cộng bảng đối chiếu phiên bản gom mọi chỗ UI đã đổi. Project chapter này **chỉ có `final`, không có `starter`** (hợp lý: chapter dạy tự tạo project từ đầu).
  Đã kiểm: `npx astro check` 0 lỗi / 0 warning / 0 hint (31 file) · `npx astro build` exit 0, 12 page · route `/chapters/ch02-getting-started-with-android-studio/` HTTP 200, 11 fieldset quiz render, không còn placeholder.
- `[x]` **Task 37 — Viết lại Ch04 Gradle Basics.** Hoàn thành 2026-08-28. Sách 5.360 từ / 15 heading → web **9.484 từ prose / 15 `h2`**, 42 code block, file lesson 328 → **2.024 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch04Quiz.astro` 581 dòng), có 1 widget xếp thứ tự tương tác. Mọi `##` heading của sách đều có mục tương ứng, cộng 3 mục web tự thêm. **Citation đã mở file thật để kiểm:** `build.gradle.kts` (root + module), `settings.gradle.kts`, `gradle.properties`, `gradle/wrapper/gradle-wrapper.properties`, `gradle/libs.versions.toml`, `keys.properties`, `AndroidManifest.xml`, `ExampleInstrumentedTest.kt`. **7 chỗ sách ≠ project mẫu đã ghi rõ tại chỗ, không âm thầm sửa:** AGP 8.1.4↔8.2.0 · Java 1.8↔17 · Compose compiler 1.5.1↔1.5.3 · Compose BOM 2023.10.01↔2023.10.00 · activity-compose 1.8.0↔1.8.2 · `compileOptions` sách bảo xoá↔code giữ · `keys.properties` sách dặn đừng commit↔thực tế đã commit. **3 bẫy đã kiểm bằng code thật:** `applicationIdSuffix = ".debug"` làm vỡ `ExampleInstrumentedTest.kt`; thiếu `keys.properties` làm Gradle sync fail; `.gitignore` không có tác dụng với file git đã track. Web thêm 3 file sách không nhắc (`gradle.properties`, `gradle-wrapper.properties`, `gradlew`) vì người học gặp ngay lần đầu tạo project. Đã kiểm: `astro check` 0/0/0 (31 file) · `astro build` 12 trang · route `/chapters/ch04-gradle-basics-a-look-behind-the-curtain/` HTTP 200, 302.110 byte, 15 `h2` + 19 fieldset + 4 bài tập, 0 placeholder.
- `[x]` **Task 38 — Viết lại Ch10 Room Database.** Hoàn thành 2026-08-28. Sách 24 heading (khoảng cách lớn nhất toàn bộ sách) → web **14.604 từ prose / 23 `h2` + 14 `h3`**, file lesson 281 → **2.403 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch10Quiz.astro` 213 → **707 dòng**), gồm 1 widget xếp thứ tự tương tác (`#save-order`, 6 bước đường đi của dữ liệu khi bookmark một công thức). Mạch bài: SQLite → 3 thành phần Room (mental model nhà hàng) → Architecture Components → kiến trúc 5 lớp + luồng một chiều → cách tiếp cận "cắt bánh theo miếng" → thêm thư viện (toml + KSP) → Room Classes → Entities (`RecipeDb`, `IngredientDb`) → DAOs + bảng `OnConflictStrategy` → Database + Singleton → Repository → `Conversions.kt` và 3 họ model song song → ViewModels → `RecipeApp` → CompositionLocal → Bookmarks/SwipeToDismiss → Recipe Details → cập nhật ViewModel refs + Preview + Groceries → Alternatives → bảng 7 điểm lệch → ghi chú phiên bản → Key Points tự viết → nối chapter 11. **Citation đã mở file thật để kiểm:** `data/database/{RecipeDb,IngredientDb,RecipeDao,IngredientDao,RecipeDatabase}.kt`, `data/{RecipeRepository,Conversions}.kt`, `viewmodels/RecipeViewModel.kt`, `RecipeApp.kt`, `MainActivity.kt`, `ui/RecipeDetails.kt`, `ui/recipes/ShowBookmarks.kt`, `ui/groceries/GroceryList.kt`, `gradle/libs.versions.toml`, `app/build.gradle.kts`. **7 điểm lệch sách ↔ project mẫu, gom thành bảng riêng (mục 20) kèm cột "nên theo cái nào":** (1) tên database sách dạy `recipe_database` nhưng code chạy thật là `"Recipes"` (`RecipeApp.kt:62`); (2) **cả hàm `getInstance()` là code chết** — 0 call site, nên `@Volatile`, `synchronized`, `fallbackToDestructiveMigration()` và tên `recipe_database` đều không bao giờ thực thi; tính singleton thật đến từ `RecipeApp.onCreate()`; (3) sách giải thích `autoGenerate` như đang bật nhưng code là `autoGenerate = false` — đúng cho app này vì id do API Spoonacular cấp (natural key); (4) `ksp(libs.moshiCodeGen)` khai **hai lần** (`app/build.gradle.kts` dòng 79 và 86), sách không nhắc; (5) `@Parcelize`/`Parcelable` **chưa được dùng ở đâu** — navigation truyền `Int` id, không có `putExtra`/`Bundle`; (6) sách mô tả `data/models` chứa "tất cả Room Entity" nhưng entity nằm ở `data/database/`; (7) `findBookmarkById()` giống `findRecipeById()` từng ký tự, và `findBookmarkById`/`deleteIngredient`/`deleteIngredients` không có call site. **Phân tích web tự thêm:** `ShowBookmarks.kt:83–87` bọc dispatcher **hai lần** (ViewModel đã `withContext(Dispatchers.IO)`) trong khi `GroceryList.kt` làm đúng — hai file cùng project, hai cách khác nhau; `recipeToDb()` chỉ điền 3/10 field nên an toàn với `@Delete` (khớp khoá chính) nhưng **nguy hiểm nếu dùng với `@Update`** (ghi đè 7 field thật bằng giá trị rỗng — lỗi im lặng, không crash); preview đọc `LocalRepositoryProvider.current` mà không nằm dưới `CompositionLocalProvider` nên **preview trong Android Studio hiện lỗi thay vì render** (sách hướng dẫn đủ để build nhưng không nói điều này); `RecipeDetails` nhận **hai** id (`recipeId` cho đường vào từ tìm kiếm → gọi mạng, `databaseRecipeId` cho đường vào từ bookmark → đọc máy) và phần UI không biết dữ liệu tới từ đâu — ví dụ cụ thể nhất cho lợi ích của kiến trúc; project **không khai `foreignKeys`** nên không có cascade delete, đó là lý do `deleteRecipeIngredients()` phải tự truy vấn rồi xoá vòng lặp; `rememberDismissState` trả `true` **trước khi** việc xoá hoàn tất nên UI luôn cho dòng biến mất kể cả khi xoá thất bại; danh sách "Alternatives" của sách gộp lẫn giải pháp local (GreenDAO, Realm) với remote (Firebase, GraphQL) — dễ khiến người mới tưởng cả 4 thay được cho nhau, và **SQLDelight không có trong danh sách**. **Key Points của sách chỉ có 2 dòng** (mỏng nhất cả sách) → web tự viết 12 điểm, ghi rõ 2 dòng nào là của sách. **Ghi chú phiên bản:** Room 2.5.2 → nhánh 2.x đã lên 2.8.x/bảo trì, có `androidx.room3` 3.x hỗ trợ Kotlin Multiplatform; `SwipeToDismiss`/`rememberDismissState`/`DismissDirection` đã deprecated → `SwipeToDismissBox`; `fallbackToDestructiveMigration(dropAllTables = true)` giờ cần tham số tường minh; DAO trả `Flow<List<T>>` là cách làm phổ biến hôm nay và sẽ xoá luôn nhu cầu gọi lại `getBookmarks()` sau khi xoá; sách dùng KSP (đúng lựa chọn hiện đại, `kapt` đã ngừng). **Ghi nhận thêm:** `ui/MainScreen.kt` xuất hiện trong `diff -rq` chỉ vì CRLF→LF + thiếu newline cuối file, nội dung y hệt — đã viết vào callout "hành trình starter → final" để người học không đi tìm thay đổi không tồn tại. Đã kiểm: `astro check` 0/0/0 (31 file) · `astro build` 12 trang · route `/chapters/ch10-room-database/` HTTP 200, 339.809 byte, 19 fieldset + 4 bài tập + widget `#save-order`/`#check-order`, 0 placeholder. **Sự cố trong lúc làm (đã xử lý):** 3 lỗi build do dấu `{` `}` chưa escape — 2 chỗ trong `<pre class="tree">` và 1 chỗ trong `<code>` inline; đã đổi sang `&#123;`/`&#125;`. **Bài học giữ lại:** trong `<Code code={const} />` không cần escape (template literal JS), nhưng mọi `{` viết trực tiếp trong markup Astro đều bị parse thành expression.
- `[x]` **Task 39 — Viết lại Ch08 Networking.** Hoàn thành 2026-08-28. Sách 19 heading → web **13.451 từ prose / 23 `h2` + 22 `h3`**, 37 code block, 42 citation `class="src"`, 7 bảng, file lesson 219 → **2.196 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch08Quiz.astro` 221 → **679 dòng**), gồm 1 widget xếp thứ tự tương tác (`#save-order`, 7 bước đường đi của một lời gọi mạng) và 1 bài "phá rồi sửa" (gộp 2 `scope.launch` thành 1 để tự chứng kiến `collect` chặn lệnh phía sau — kèm cảnh báo chỉ làm trên bản copy, không sửa `aaf-materials/`). Mạch bài: chapter giải quyết vấn đề gì → Getting Started → thread/core/dispatcher và ngân sách 16 ms → coroutine + `suspend` (5 mục con: `suspend`, đọc source `viewModelScope`, `SupervisorJob`, chữ ký `launch`, thêm thư viện) → Flows (`collectAsState` vs `collect` trong `LaunchedEffect`, đọc `RecipeList.kt` thật) → Network Requests + Alternatives → Response Parsing → đăng ký Spoonacular → response model → `SpoonacularService` + 3 annotation → `RetrofitInstance` → nối vào ViewModel → `queryRecipes` → **đi ngược từ cú chạm của người dùng (10 bước)** → Run the App → **bên dưới một lời gọi `suspend` + before/after callback** → phân trang → reflection → codegen (KSP) → Details Screen → bảng 8 điểm lệch → ghi chú phiên bản → Key Points tự viết (11 điểm) → nối chapter 9. **Citation đã mở file thật để kiểm:** `network/SpoonacularService.kt` (47, 49–59, 61–79), `viewmodels/RecipeViewModel.kt` (49–61, 63, 71, 73–89, 95–111, 117–129, 131–149, 167–173), `ui/recipes/RecipeList.kt` (55, 58, 61–67, 68, 75, 81, 95), `ui/recipes/ShowRecipeList.kt` (69, 73, 77–84, 85–93, 124–135), `ui/recipes/SearchRow.kt` (92–104, 113–119), `ui/recipes/ImageRow.kt` (70–77), `ui/RecipeDetails.kt` (84, 92, 94–117), `MainActivity.kt` (59–60, 78–90), `utils/ComposeUtils.kt` (77–80), `data/models/{SearchRecipesResponse,Recipe,RecipeInformationResponse,ExtendedIngredient,Ingredient}.kt`, `gradle/libs.versions.toml`, `app/build.gradle.kts`, `AndroidManifest.xml` (dòng 5, cả `starter` và `final`), `RecipeApp.kt`. **8 điểm lệch sách ↔ project mẫu, gom thành bảng riêng (mục 20) kèm cột "nên theo cái nào":** (1) Key Points dòng 592 nói "Flows thay thế `LiveData`" — `LiveData` **chưa deprecate** tính tới 2026-08-28, phát biểu đúng là code Kotlin/Compose mới ưu tiên `StateFlow`; (2) `moshi-kotlin` (bản reflection sách thêm rồi bỏ) **không tồn tại ở đâu trong `final`** nên diff `starter`↔`final` không bao giờ thấy trạng thái trung gian, `provideMoshi()` trong `final` chỉ là `Moshi.Builder().build()` trơn; (3) sách cảnh báo không commit API key (dòng 314) nhưng chính project commit `const val apiKey` ở `SpoonacularService.kt:47` — giá trị chỉ là placeholder nên không rò rỉ, nhưng cấu trúc mời gõ key thật vào đúng chỗ đó; đã ghi thêm rằng key được nội suy vào string của `@GET` nên **bị nướng vào class lúc biên dịch**, tức `BuildConfig` cũng chỉ giảm rủi ro commit chứ không chống dịch ngược APK; (4) sách bảo thêm `ksp(libs.moshiCodeGen)` nhưng **không nói phải khai plugin KSP** — nó đã có sẵn trong `starter` (`app/build.gradle.kts` 1–5, `libs.versions.toml` 65) nên người dựng project mới phải tự thêm; (5) `Ingredient` được gắn `@JsonClass(generateAdapter = true)` do câu "làm tương tự với các file còn lại", nhưng **không endpoint nào trả về nó** (chỉ dùng bởi state của `GroceryListViewModel`/`RecipeViewModel`, lại mang field `recipeId` — mùi khoá ngoại) → đó là model của Chapter 10; (6) `suspend fun queryRecipe` vẫn mở `viewModelScope.launch` — một lớp dư, hàm trả về ngay nên `suspend` là lời hứa không giữ được, và bất đối xứng với `queryRecipes` (không `suspend`); (7) `Dispatchers.Default` (dành cho CPU) dùng cho lời gọi mạng — chạy đúng vì Retrofit/OkHttp tự chuyển sang pool riêng, nhưng dạy thói quen sai; (8) **2 hàm `@Preview` không render được**: `PreviewRecipeList` (`RecipeList.kt:95–101`) và `PreviewShowRecipeList` (`ShowRecipeList.kt:124–135`) đều chạm `LocalNavigatorProvider.current`, mà `MainActivity.kt:59–60` khai mặc định là `error("No navigation provided")` — cùng loại vấn đề đã gặp ở Ch09/Ch10; cách sửa: bọc `CompositionLocalProvider(LocalNavigatorProvider provides rememberNavController())`. **Phân tích web tự thêm:** trạng thái lỗi và trạng thái rỗng bị gộp (`_recipeListState.value = listOf()` trong `catch`) nên "mất mạng" trông hệt "không tìm thấy gì", log Timber chỉ lập trình viên thấy; phân trang trong `ShowRecipeList.kt` **ghi đè** danh sách thay vì nối thêm nên cuộn xuống thay kết quả chứ không mở rộng, và có thể bắn liên tục vào một API bị giới hạn lượt; hàm `searchRecipes` nằm trong `ImageRow.kt` — file về dải ảnh trang trí, hoàn toàn không liên quan tìm kiếm; `addPreviousSearch` là thân hàm rỗng ở chapter 8 (việc của Chapter 9); quyền `INTERNET` sách không nhắc vì `starter` đã có; giải thích `object` + `by lazy` (SYNCHRONIZED) qua lăng kính tái dùng connection pool của OkHttp; quy tắc `baseUrl` phải kết thúc bằng `/` (Retrofit ném `IllegalArgumentException` ngay lúc dựng); bảng 7 dòng so sánh reflection ↔ codegen; phân biệt `implementation(...)` ↔ `ksp(...)`; typo của sách ở dòng 425 ("an empty listIf there were any errors."). **Ghi chú phiên bản (đã tra Maven Central + GitHub API, không đoán):** coroutines 1.7.2 → **1.11.0 (08/05/2026)**; Retrofit 2.9.0 → **3.0.0 (05/2025)** nhưng 3.x **không đổi API, vẫn package `retrofit2`, vẫn tương thích binary với 2.x** — thay đổi thật là nâng OkHttp 3.14 → 4.12 (kéo thêm dependency Kotlin), nhánh 2.x cuối là 2.12.0; Moshi 1.15.0 → **1.15.2 (12/2024)**; Timber 5.0.1 **vẫn là bản mới nhất** (08/2021); Kotlin 1.9.10 → 2.4.x (từ Kotlin 2.0 Compose compiler đi kèm plugin Kotlin, phải sửa cách khai `composeOptions`); AGP 8.2.0 phải khớp phiên bản Android Studio. Kèm khuyến nghị **đừng nâng version khi đang học chapter** và lưu ý free-tier + ảnh chụp Spoonacular gần như đã đổi so với sách. Đã kiểm: `astro check` **0 error / 0 warning / 0 hint** (31 file) · `astro build` 12 trang, `/chapters/ch08-networking/index.html` **263.277 byte** · route dev server HTTP **200**, HTML trả về 29 `<h2>` (23 lesson + 6 quiz/layout) + **19 fieldset** + **4 bài tập** + widget `#save-order`/`#check-order` với 7 `data-correct`, **0 placeholder `@@`**. **Sự cố trong lúc làm (đã xử lý):** (a) tham chiếu chéo "mục 20/21" viết trước khi biết số mục cuối → lệch, đã sửa bằng 2 lượt thay thế in-place (`mục 21`→`mục 18` 3 chỗ, `mục 20`→`mục 19` 5 chỗ) + typo `adexpter`→`adapter`; (b) **điểm lệch #8 ban đầu viết sai** — nói `@Preview` của `RecipeDetails` không render được và trích dòng 91; grep chứng minh `RecipeDetails.kt` **không có `@Preview` nào** và dòng đọc CompositionLocal là **92**, đã viết lại đúng thành 2 preview thật ở `RecipeList.kt`/`ShowRecipeList.kt`; (c) cảnh báo `ts(6133)` do const `AAF_START` khai mà không dùng → đổi thành đường dẫn gốc `projects/starter` và dùng nó ở 3 chỗ đang hard-code, hết cảnh báo. **Bài học giữ lại:** khi trích dòng cho `@Preview`, phải grep chính file đó chứ không suy từ file cùng thư mục.
- `[x]` **Task 40 — Viết lại Ch03 Android Fundamentals.** Hoàn thành 2026-08-28. Sách 13 heading → web **13.717 từ prose / 27 `h2` + 30 `h3`**, 27 code block, 27 citation `class="src"`, 23 bảng, file lesson 290 → **1.503 dòng**. Quiz nâng 4 câu → **19 câu + 4 bài tập** (`Ch03Quiz.astro` 203 → **714 dòng**), gồm widget xếp thứ tự 7 bước (từ lúc gõ 1 ký tự tới recomposition lần hai sau khi bấm Send). Mạch bài: bản đồ 6 khối kiến thức → Activity là gì (mô hình tư duy) → đọc `starter` + 4 dòng cốt lõi + plugin biên dịch Compose → lifecycle → thay code (imports + thân hàm) → đi qua từng phần UI (`Column`, state + `by`, `Text`, `OutlinedTextField`, `Button`) → Android thời XML (layout XML, Fragment, di sản, bảng đối chiếu cũ/mới) → vì sao dùng `strings.xml` → `strings.xml` + resource qualifier đa ngôn ngữ → lớp `R` → **lỗi biên dịch sách cố tình cho gõ sai** (vì sao lỗi, 4 nơi IDE báo, cách sửa, `LocalContext`) → 2 cách lấy chuỗi → debug (nút con bọ, breakpoint, vì sao chọn đúng dòng đó) → resource → manifest (vị trí + bẫy Project navigator, đọc toàn bộ manifest, `package` vs `namespace`) → thẻ `<activity>` → Intent → Permission → Service → Theme + mẹo bia ngắm → 2 hệ thống theme song song → **lệch #8: theme Compose không còn ai gọi** → **chuỗi 7 việc khi bấm Send** → bảng tổng hợp 8 lệch → ghi chú phiên bản → Key Points tự viết (11 điểm) → nối chapter 4 Gradle. **Citation đã mở file thật để kiểm:** `final/.../MainActivity.kt` (36–48 imports, 50–78 thân, dòng 55 `LocalContext`, 56–57 `remember`, 65 + 72 `stringResource`, 68–71 `onClick`, 73 dòng trống dư), `starter/.../MainActivity.kt` (37–47 imports, 49–64 `setContent` + `KodecoChatTheme` + `Surface`, 66–72 `Greeting`, 74–78 `@Preview`), `final|starter/res/values/strings.xml` (34–40 vs 34–36), `final/AndroidManifest.xml` (35–36 `<manifest>` không có `package`, 38–47 `<application>`, 48–52 `<activity>` + `exported`, 53–57 `<intent-filter>`), `res/values/themes.xml` (35–38 — cả file chỉ 1 dòng `<style>` không có `<item>` nào), `ui/theme/Theme.kt` (74–103), `app/build.gradle.kts` (41–42 `namespace`/`compileSdk`, `buildFeatures`, `composeOptions`), `build.gradle.kts` gốc (AGP 8.2.0, Kotlin 1.9.10). **8 điểm lệch sách ↔ project/tài liệu chính thức, gom thành bảng mục 24 kèm cột "nên theo cái nào":** (1) chuỗi mặc định ô hiển thị sách viết `"Press Send to update this text"` (dòng 127) nhưng `chat_display_default` và bản `final` dùng `"Messages will display here"` (dòng 222) — đổi âm thầm; (2) sách gọi `Context` là **interface** và nói `getString()` là "method do Activity cung cấp" → tài liệu Android: `Context` là `abstract class`, `getString()` định nghĩa **trên chính `Context`**, Activity chỉ thừa hưởng qua ContextThemeWrapper → ContextWrapper; (3) sách gợi ý chapter sẽ dùng style + drawable, nhưng `diff -r --strip-trailing-cr starter final` chứng minh **chỉ 2 file đổi nội dung thật** (`MainActivity.kt`, `strings.xml`) — mọi khác biệt còn lại chỉ là license header xuống dòng khác nhau; (4) sách nói `<manifest>` phải khai `package` → **0/22** manifest trong `aaf-materials/` có nó, đã chuyển sang `namespace` từ AGP 7.3; (5) **sách viết sai tên thẻ**: xin quyền là `<uses-permission>`, còn `<permission>` là để *định nghĩa* quyền mới — lệch nghiêm trọng nhất về kỹ thuật; (6) sách nói quyền mạng thêm ở chapter 7 → đếm thật: ch03/04/05/06/07 đều **0** `uses-permission`, ch**08** mới có `INTERNET` (dòng 5, cả starter và final), và ch07 dòng 64 của sách tự nói Ditto tự khai quyền → sách tự mâu thuẫn; (7) **không có thẻ `<service>` nào trong bất kỳ manifest nào của cả 11 chapter** dù sách lấy ví dụ Kodeco Chat lấy tin nhắn nền; (8) **web tự phát hiện:** bước "thay toàn bộ file" xoá lời gọi `KodecoChatTheme { }` — grep chứng minh chỉ còn **1** kết quả trong toàn `final/app/` chính là dòng khai báo hàm (`Theme.kt:74`), **0 lời gọi** → app mất dark mode + Material You + typography riêng, rơi về màu tím baseline của Material 3. **Phân tích web tự thêm:** ranh giới Composable / không-Composable là ý tưởng kỹ thuật quan trọng nhất chapter gieo mầm (dùng nó để giải thích lỗi `remember` + `stringResource` thay vì chỉ đưa cách sửa); `stringResource` vs `context.getString` **không tương đương** — 2 chuỗi bị `remember` "đóng băng" sẽ không dịch lại khi đổi ngôn ngữ lúc chạy, thành bài tập 3; xoay máy mất chữ vì `remember` chỉ nhớ trong 1 composition; đảo 2 dòng trong `onClick` cho lỗi logic **không crash, không lỗi biên dịch** — thành bài tập 4 "phá rồi dùng breakpoint bắt"; bản `final` **không còn `@Preview` nào** (starter có 1 ở dòng 74) nên mất khả năng xem trước không cần máy ảo; **không có `res/layout/` ở đâu trong cả `aaf-materials/`** — 11/11 chapter là Compose 100%; theo dõi khoảng trống theme qua các chapter: ch04 vẫn không gọi, ch05 lấp bằng **`KodecochatTheme` ở `theme/Themes.kt`** (chữ `c` thường, thư mục khác) khiến `ui/theme/Theme.kt` mồ côi hẳn; mẹo IDE "bia ngắm" của sách nay là *Select Opened File*; `tools:targetApi` là chỉ thị cho IDE, bị loại khi đóng gói. **Ghi chú phiên bản (tra Google Maven + Maven Central, không đoán):** compose-bom 2023.10.00 → **2026.08.00**; activity-compose 1.8.2 → **1.13.0**; core-ktx 1.12.0 → **1.19.0**; lifecycle-runtime-ktx 2.6.2 → **2.11.0**; material3 → **1.4.0**; AGP 8.2.0 → **9.3.2**; Kotlin 1.9.10 → **2.4.10**; `compileSdk` 34 → 36. Khác biệt *thực dụng* duy nhất khiến không copy được file Gradle của sách sang project mới: từ Kotlin 2.0, khối `composeOptions { kotlinCompilerExtensionVersion }` bị thay bằng plugin `org.jetbrains.kotlin.plugin.compose`. Kèm phần "những gì vẫn đúng nguyên" và lưu ý phân biệt lệch-do-thời-gian (#4) với lệch-do-viết-sai (#5). Đã kiểm: `astro check` **0 error / 0 warning / 0 hint** (31 file) · `astro build` 12 trang exit 0 · route dev server HTTP **200**, HTML trả về **314.043 byte**, 27 `<h2>` + **19 fieldset** + **4 bài tập** + widget `#save-order`/`#check-order` với 7 `data-correct`, **0 placeholder `@@`**, **0 anchor `href="#..."` bị vỡ** (22/22 trỏ tới `id` tồn tại). **Sự cố trong lúc làm (đã xử lý):** (a) rút kinh nghiệm Task 39, viết script Python đối chiếu **nhãn số trong link chéo với số heading thật** → bắt được 1 lệch (`#manifest` ghi "mục 14" nhưng heading là 15), đã sửa; (b) forward reference `#theme-mo-cua` viết sẵn nhãn "mục 22" từ đầu bài → phải tách phần theme thành 2 mục (21 "hai hệ thống theme song song" + 22 "lệch #8") cho khớp nhãn, kết quả lại rõ hơn ý ban đầu; (c) bài tập 2 ban đầu viết "thêm 2 import" rồi "3 import" → mở lại `starter/MainActivity.kt` đếm đúng: cần **5** dòng (`fillMaxSize`, `MaterialTheme`, `Surface`, `Modifier`, `KodecoChatTheme`) và `Surface` phải có `modifier = Modifier.fillMaxSize()` như bản starter, đã sửa cả lesson và quiz. **Bài học giữ lại:** khi bài tập yêu cầu "thêm N import", phải mở file gốc đếm thật thay vì ước lượng — cùng loại sai với bài học "phải grep chính file đó" của Task 39.
- `[x]` **Task 41 — Viết lại Ch06 Advanced Jetpack Compose.** Hoàn thành 2026-08-28. Sách 3.797 từ / 7 heading → web **10.424 từ prose / 20 `h2`**, 37 code block, file lesson 315 → **2.222 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch06Quiz.astro` 707 dòng). Nội dung theo đúng mạch sách: state → recomposition → `remember` → state hoisting → unidirectional data flow → ViewModel → MVI/Flow/StateFlow → nối dây → Jump to bottom. **Phát hiện lớn nhất — 2 file sách dùng mà không hướng dẫn tạo, thiếu là không compile được:** `data/model/ChatRoom.kt` và `data/Constants.kt` (`DEFAULT_PUBLIC_ROOM_MESSAGES_COLLECTION_ID`), cùng phần `emptyChatRoom`/`_currentChatRoom`/`currentRoom` trong ViewModel → đã viết mục riêng (mục 9) kèm thứ tự việc cần làm; suy luận nguyên nhân: `ChatRoom` có `messagesCollectionId` là hash 64 ký tự + `isPrivate` → được thiết kế cho chapter 8, project `final` lấy từ bản code về sau, sách viết theo trạng thái cũ hơn. **3 chỗ sách ≠ project:** comment ghi sai chapter Data Store · `KodecochatTheme` viết hoa không đồng nhất · nghiêm trọng nhất là `authorId = content.user.id` trong sách làm **mọi tin nhắn hiện như của chính bạn** (mục 8, 12.4, 14). **3 việc phải làm cùng nhau mà sách chia rời:** `reverseLayout` + `add(0, ...)` + đảo thứ tự `initialMessages` — kèm mô tả UI sai thế nào nếu làm thiếu một chỗ. **Giải thích các API sách dùng mà không nói:** `derivedStateOf` (và lý do hiệu năng), `LaunchedEffect`, `Dispatchers.Default`, `rememberSaveable` vs `remember`, `collectAsStateWithLifecycle` vs `collectAsState`, toàn bộ `JumpToBottom.kt` (sách chỉ bảo copy). **4 dấu vết code chưa hoàn thiện trong project mẫu (mục 17):** tính năng ảnh đứt ở 3 mắt xích + Coil khai báo mà không dùng · code chết trong ViewModel · 1 hàm đệ quy vô hạn · 1 property tính sai. Cảnh báo tên gọi: "Intent" trong MVI ≠ `android.content.Intent`; ViewModel sống qua xoay màn hình nhưng **không** sống qua việc tắt app. Sửa kèm: xoá `STARTER_ROOT` không dùng (hết cảnh báo `ts(6133)`). Đã kiểm: `astro check` 0/0/0 · `astro build` 12 trang · route `/chapters/ch06-advanced-jetpack-compose/` HTTP 200, 346.431 byte, 20 `h2` + 19 fieldset + 4 bài tập, 0 placeholder.
- `[x]` **Task 42 — Viết lại Ch01 Welcome to Android & Kotlin.** Hoàn thành 2026-08-28. Sách 2.227 từ / 8 heading → web **9.799 từ prose / 12 `h2`**, 17 code block, file lesson 169 → **1.735 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch01Quiz.astro` 751 dòng). Chapter này **không có project Android** (`aaf-materials/01-.../` chỉ có file `.keep`) nên chiến lược khác: mọi khái niệm trừu tượng của chapter 1 được **đối chiếu sang file thật của project chapter 2** để người học thấy chúng tồn tại ở đâu — `AndroidManifest.xml`, `res/values/{strings,colors,styles}.xml`, `res/xml/data_extraction_rules.xml`, `mipmap-anydpi/ic_launcher.xml`, `drawable/ic_launcher_background.xml`, `ui/theme/{Color,Theme}.kt`, `build.gradle.kts` (root + module), `settings.gradle.kts`, `gradle-wrapper.properties`, cộng ví dụ tự viết `values-vi/strings.xml` cho phần bản địa hoá. **4 điểm lệch đã ghi rõ:** (1) **sửa 1 ghi chú sai trong tài liệu nội bộ của chính dự án** — `docs/SOURCE_MAP.md` từng ghi chapter 1 "học Kotlin thuần trên Kotlin Playground"; đã đọc lại toàn bộ 137 dòng chapter: sai, chapter không dạy Kotlin thuần và tìm chữ "playground" cho 0 kết quả, lý do thật là chapter tổng quan bằng văn bản nên không cần project; (2) sách trình bày **4 app component ngang hàng** (dòng 33–38 và Key Points dòng 125) nhưng cả sách **chỉ thực hành Activity** → đặt lại kỳ vọng "học để dùng" vs "học để nhận diện", và chỉ ra việc Content Provider không xuất hiện lại **khớp đúng caveat của chính tác giả** ở dòng 37 (app chỉ cần lưu dữ liệu, không cần chia sẻ → đúng ra dùng DataStore/Room); (3) cảnh báo sớm **2 chữ "Intent" hoàn toàn khác nhau** — `android.content.Intent` của chapter 1 vs "Intent" trong MVI của chapter 6 (chỉ trùng tên, trong code chỉ là lời gọi hàm); (4) sách in `android:theme="@style/SplashTheme"` còn manifest thật của chapter 2 dùng `android:theme="@style/Theme.KodecoChat"`. Web thêm mục "Bốn con số định vị project vào cuối năm 2023" và bản đồ tới các chapter khác. Đã kiểm: `astro check` 0/0/0 · `astro build` 12 trang · route `/chapters/ch01-welcome-to-android-kotlin/` HTTP 200, 261.251 byte, 12 `h2` + 19 fieldset + 4 bài tập, 0 placeholder.
- `[x]` **Task 43 — Viết lại Ch09 Data Store.** Hoàn thành 2026-08-28. Sách 2.294 từ / 13 heading → web **9.169 từ prose / 18 `h2` + 5 `h3`**, 31 code block, file lesson 235 → **1.960 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch09Quiz.astro` 198 → **745 dòng**), gồm 1 widget xếp thứ tự tương tác (`#save-order`, 5 bước đường đi của dữ liệu khi lưu) và 1 bài tìm lỗi dùng **class `Prefs` giả lập tự viết** (`brokenPrefsCode`, cố tình gài 3 lỗi: thiếu `suspend` · dùng sai `intPreferencesKey` cho String · `runBlocking` chặn luồng UI — có ghi rõ trong comment là **không phải file thật** của `aaf-materials`). Mạch bài: 3 cách lưu dữ liệu → SharedPreferences (kèm `apply()` vs `commit()` và ANR) → DataStore (`Flow<T>` + `updateData`) → thêm thư viện → `Prefs.kt` → delegate `preferencesDataStore` → `RecipeApp` + `lateinit` → CompositionLocal → `viewModelFactory` → lưu lịch sử tìm kiếm → đọc lại trong `init` → lưu tab đang chọn. **Citation đã mở file thật để kiểm:** `data/Prefs.kt`, `RecipeApp.kt` 10/19, `AndroidManifest.xml` 12, `MainActivity.kt` 30–31/44–48, `utils/ComposeUtils.kt` 44–47, `ui/recipedetails/RecipeDetails.kt` 53/55, `viewmodels/RecipeViewModel.kt` 32/57–65/85–90/122–133, `ui/MainScreen.kt` 22/29/31–38/48/58, `ui/recipes/{ChipRow,SearchRow,ShowRecipeList}.kt`, `ui/bookmarks/ShowBookmarks.kt`, `gradle/libs.versions.toml`. **7 điểm lệch sách ↔ project mẫu, gom thành 1 bảng riêng (mục 16), kèm cột "nên theo cái nào":** thứ tự dependency · `hasKey` là **code chết và không bao giờ tìm được key kiểu Int** · dòng `remember { Prefs(context) }` sách bảo thêm **không tồn tại trong `final`** (đã đọc đủ 86 dòng `MainActivity.kt`) và nếu thêm sẽ tạo DataStore instance thứ hai trên cùng file `recipes`, vi phạm chính cảnh báo "Important" của sách · cách sửa 4 `@Preview` khác nhau (sách `Prefs(context)` ↔ `final` dùng `LocalPrefsProvider.current`) · lỗi chính tả `getInit` ở dòng 165 của sách · bookmark thật ra là nội dung chapter 10 dù chapter 9 mở đầu bằng câu chuyện đó · delegate khai báo trong class làm yếu chính lời cảnh báo một-instance của sách (project chỉ thoát được vì `Prefs` được tạo đúng một lần trong `RecipeApp.onCreate()`). **Phân tích web tự thêm:** cách của `final` sạch hơn về kiến trúc nhưng **preview vẫn không render được** vì không có `CompositionLocalProvider` bọc ngoài → `.current` rơi vào `error("No prefs provided")`; cách đúng là bọc preview bằng `Prefs` giả, và đó chính là lý do `Prefs` nên là `interface`; 4 dòng `val context = LocalContext.current` còn sót lại không dùng là dấu vết cả hai cách từng cùng tồn tại. Thêm nữa: bug lưu list bằng `joinToString(",")` (nhập "chicken, rice" → tách thành 2 mục) được đặt tên đúng loại lỗi **thiếu escaping** cùng họ với CSV vỡ và SQL injection, kèm bảng 3 cách sửa (đổi separator / lọc dấu phẩy / **dùng JSON — Moshi đã có sẵn trong project**) và Proto DataStore là câu trả lời sách không dạy; `first()` làm mất tính reactive của `Flow<T>` (ghi rõ là đơn giản hoá có chủ đích + đường nâng cấp); lưu tab bằng `0`/`1` gắn dữ liệu vào **thứ tự tab** → rủi ro migration thật mà sách bỏ qua; `LaunchedEffect { scope.launch { } }` trong `final` bọc coroutine 2 lớp (vô hại, bỏ được 1 lớp). Đã kiểm: `astro check` 0/0/0 (31 file) · `astro build` 12 trang · route `/chapters/ch09-data-store/` HTTP 200, 315.351 byte, 18 `h2` + 5 `h3` + 19 fieldset + 4 bài tập, 0 placeholder. **Sự cố trong lúc làm (đã xử lý):** vô tình ghi 1 byte NUL (0x00) vào file khiến mọi lệnh `grep` báo "Binary file … matches" và ẩn hết kết quả — định vị bằng `perl -0777 -ne 'while(/\x00/g){print pos}'` (byte 82160, dòng 1465), sửa bằng `perl -i -pe 's/\x00/ /g'`, verify count = 0. **Bài học giữ lại:** `file` vẫn báo UTF-8 hợp lệ nên không phát hiện được bằng cách đó; `grep -P` không dùng được trong môi trường này.
- `[x]` **Task 44 — Viết lại Ch07 Advanced Architecture.** Hoàn thành 2026-08-28. Sách 6 heading (ít heading nhất, nhưng mật độ kỹ thuật cao nhất vì phải dạy cả Repository pattern lẫn một SDK lạ) → web **13.647 từ prose / 29 `h2` + 2 `h3`**, 33 code block, **39 citation `class="src"`**, 10 bảng, file lesson 252 → **2.633 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch07Quiz.astro` 200 → **716 dòng**), gồm 1 widget xếp thứ tự 6 bước (từ lúc bấm nút gửi tới lúc tin nhắn hiện ở đáy màn hình) và 3 câu hỏi dạng "đọc code thật rồi chỉ ra vấn đề" dùng đúng snippet từ project (`getAllMessagesForRoom` side-effecting · `init` shadowing · `String.toInstant()` tự gọi chính nó). Mạch bài: chapter giải quyết vấn đề gì → Ditto là gì (local-first, mesh P2P, không có "bản chính") + đăng ký account → thêm SDK → quyền sync → `keys.properties` + `buildConfigField` → `DittoHandler` → `setupDitto()` → thứ tự gọi trong `onCreate` → Repository pattern (5 trách nhiệm) → interface → singleton `@Volatile` + double-checked locking → 3 state holder → live query vs subscription → chuỗi khởi tạo → ghi dữ liệu → đọc & mapping `DittoDocument` → `Constants.kt` → ViewModel (kèm `h3` về bug shadowing) → `.asReversed()` + `reverseLayout` → xoá `FakeData` → **đường dây từ Ditto tới màn hình** → `DateExtensions` → code chết → bảng 15 điểm lệch → ghi chú phiên bản → 12 lỗi thường gặp → Key Points tự viết (12 điểm) → 3 thử thách → nối chapter 8. **Citation đã mở file thật để kiểm:** `RepositoryImpl.kt` (80–83, 84–92, 94–105, 110–128, 130–142, 144–151, 153–160, 162–187, 189–198, 200–236, 238–240, 243–261, 263–266, 268–281, 283–294, 296–310, 312–314, 316–325), `Repository.kt` (41), `MainActivity.kt` (60, 62, 63–65, 67–72, 74, 80, 81, 84–89, 91–103), `MainViewModel.kt` (61, 65–69, 71, 73–81, 83–84, 87–98, 100–105, 107–111, 113–134), `DittoHandler.kt`, `conversation/ConversationUiState.kt` (37–55, 57–72, 74–93), `data/model/{Message,User,ChatRoom,MessageUiModel,DateExtensions}.kt`, `data/Constants.kt` (40–41, 44–59, 62), `app/build.gradle.kts`, `gradle/libs.versions.toml`, `AndroidManifest.xml` (starter dòng 52), `keys.properties`. **15 điểm lệch sách ↔ project mẫu, gom thành bảng riêng kèm cột "nên theo cái nào":** (1) sách in `val currentRoom` public, code thật `private` (dòng 84); (2) sách in `fun updateUserInfo` public, code thật `private` (dòng 107); (3) **`init` shadow field `firstName`/`lastName` bằng `val` cục bộ** → hai field của class mãi mãi là `""`, làm tham số mặc định của `updateUserInfo` thành code chết (Kotlin **không** cảnh báo shadowing của field, chỉ cảnh báo shadowing tham số); (4) `Repository.kt:41` import `live.ditto.DittoAttachment` → interface "back-end agnostic" **rò kiểu của Ditto ra ngoài**, tự mâu thuẫn với Key Points của chính chapter; (5) KDoc ghi "Chapter 8, Data Store" nhưng Data Store là chapter **9** (2 chỗ: `RepositoryImpl.kt:155`, `MainViewModel.kt:61`); (6) **`DateExtensions.kt:40–42` có `fun String.toInstant()` gọi `this.toInstant()`** — trình bày **trung thực là suy luận, không phải sự thật đã kiểm chứng**: kotlinx-datetime 0.4.0 thật sự ship `public fun String.toInstant(): Instant = Instant.parse(this)` và quy tắc phân giải của Kotlin xếp extension import tường minh (mức 3) trước extension cùng package (mức 4) nên import thắng → không đệ quy; nhưng **không compile được để xác nhận** nên đã ghi rõ điều đó và đề xuất Ctrl+B là cách kiểm dứt điểm. Ghi thêm: kotlinx-datetime **0.8.0 đã bỏ toàn bộ extension trên `String`** (breaking change im lặng, không có trong changelog); (7) `GlobalScope.launch` (dòng 139) — `postInitActions` thậm chí không phải `suspend`; (8) **code chết/vô hiệu:** `addPrivateRoomSubscriptions` (243–261, 0 call site) · biến cục bộ trong `addSubscriptionForRoom` (239 — **subscription im lặng không có hiệu lực**, đúng cái bẫy mà chapter vừa dạy) · `Message.authorImage` (83, không ai đọc; `Conversation.kt:238` tự tính `authorImageId` riêng) · `messagesDocs`/`usersDocs` (gán rồi không đọc); (9) `keys.properties` bị gitignore và không ship → **project `final` fail ngay ở bước Gradle *configuration***; (10) `createMessageForRoom` không ghi `dbIdKey` (Ditto tự sinh `_id`) trong khi `addUser` thì ghi → `UUID` do ViewModel tạo bị bỏ đi, hai loại document dùng hai chiến lược id; (11) `onCreateNewMessageClick` **âm thầm bỏ mọi tin nhắn có `photoUri != null`** (dòng 129); (12) manifest `final` mất `android:label="@string/app_name"` ở thẻ `<activity>` (starter có, dòng 52); (13) **không có `onRequestPermissionsResult`/`refreshPermissions()` ở đâu** → vừa cấp quyền xong Ditto vẫn chưa bật transport; (14) không có `stopSync()`/`close()`/`onDestroy`; (15) diff chỉ-khác-newline + refactor không được ghi lại ở `UserInput.kt`, `Conversation.kt`, `KodecochatIcon.kt`. **Phân tích web tự thêm:** một **race condition thật** — live query được khởi động *bên trong* coroutine `GlobalScope` trong khi `combine` có thể đã đọc `allUsers` trước đó; app sống được **chỉ vì `StateFlow` replay giá trị mới nhất**, không phải vì thứ tự đúng; giải thích vì sao `checkPermissions()`/`setupDitto()` đặt **sau** `setContent` mà không crash (composition chạy sau khi `onCreate` return · `by viewModels()` là lazy · `combine` là cold) đồng thời vẫn khuyến nghị chuyển lên trước; `MessageUiModel.invoke` quét tuyến tính không `break` và fallback về `User()` rỗng; `upsert()` trả về ngay là hệ quả trực tiếp của local-first, nên interface `Repository` **không có** trạng thái "đang tải"/"lỗi mạng" nào; `BuildConfig` chỉ chống commit khoá lên GitHub **chứ không phải bảo mật** (giá trị vẫn nằm trong APK). **Ghi chú phiên bản (tra thật, không đoán):** Ditto Kotlin 4.5.0 → **5.1.0 (18/08/2026)**; đổi coordinate `live.ditto:ditto` → **`com.ditto:ditto-kotlin`**; khởi tạo đổi hẳn sang `DittoConfig(databaseId, connect = Connect.Server(url) | Connect.SmallPeersOnly)` + `DittoFactory.create()`; `ditto.auth` giờ nullable + `login(token, DittoAuthenticationProvider.development())`; minSdk 24 / compileSdk 36 / JDK 17; helper quyền chuyển sang `com.ditto.kotlin.transports.DittoSyncPermissions` và **`refreshPermissions()` trở thành bắt buộc**; sync nền cần `DefaultDittoForegroundService`; **từ SDK 4.12 query builder bắt đầu bị deprecate, thay bằng DQL** — kèm bảng mapping đầy đủ và 3 bẫy hay sai (một dấu `=`, chuỗi dùng nháy đơn, DQL trả `Map<String, Any?>` nên **mọi `constructor(document: DittoDocument)` trong project phải viết lại**); AGP 8.2.0 → 9.x; Kotlin 1.9.10 → 2.x (khối `composeOptions` dòng 90–91 bị thay bằng plugin `org.jetbrains.kotlin.plugin.compose`). Đã kiểm: `astro check` **0 error / 0 warning / 0 hint** (31 file) · `astro build` exit 0, 12 trang, `dist/.../ch07-advanced-architecture/index.html` **304 KB** · route dev server HTTP **200**, HTML trả về 29 `<h2>` + **19 fieldset** + **4 bài tập** + widget `#save-order`/`#check-order` với 6 `data-correct`, **0 placeholder `@@`**. **Sự cố trong lúc làm (đã xử lý):** 4 lỗi `astro check` (`ts(2609)` + `ts(1109)`) do dấu `{` trần **bên trong `<code>` inline** (`setContent { ... }` và `observeLocal { ... }`) → escape thành `&#123;`/`&#125;`; hai lệnh `Edit` song song cho cùng bản sửa đó **đều fail** "String to replace not found" dù chuỗi có thật trong file → phải thay in-place bằng Python (`encoding='utf-8', newline=''`), báo `1 1` match và `remaining bare-brace <code>: 0`. **Bài học giữ lại:** khi 2 `Edit` song song chạm cùng vùng file thì lệnh sau nhìn thấy trạng thái cũ — sửa nhiều chỗ trong *một* file thì làm tuần tự hoặc gộp thành 1 lần thay thế.
- `[x]` **Task 45 — Viết lại Ch11 Advanced Storage.** Hoàn thành 2026-08-29 — **chapter cuối cùng → Phase 5 đóng, 11/11 chapter đã qua depth pass.** Sách 2.978 từ / 16 heading (bao phủ 54%, cao nhất nên để cuối) → web **12.145 từ prose / 21 `h2` + 23 `h3`**, 42 code block, **47 citation `class="src"`**, 18 bảng, file lesson 291 → **2.359 dòng**. Quiz nâng 3 câu → **19 câu + 4 bài tập** (`Ch11Quiz.astro` 741 dòng), gồm widget xếp thứ tự 6 bước (một lần khởi động app đã mã hoá: master key → `create()` → `hasKey` → sinh passcode → dựng `SupportFactory` → query đầu tiên mới thật sự mở file). Mạch bài: vì sao mã hoá dữ liệu đã nằm trong máy → bản đồ các nơi lưu file → `filesDir`/`cacheDir` (kèm `use {}`, main thread, đo cache quota) → Device Explorer → file cache → external storage → SAF (tạo file, `ACTION_OPEN_DOCUMENT_TREE`, giữ quyền qua các lần mở app) → Database Explorer → Android Keystore (3 mức an toàn TEE/SE/software, `KeyChain` khác mục đích) → thêm security library → đọc `SecurePrefs` từng dòng (master key alias, vì sao 2 scheme khác nhau) → đổi `Prefs`→`SecurePrefs` và cái giá phải trả → SQLCipher cho Room (sinh passcode, `getInstance` trước/sau) → nối dây → cái bẫy Auto Backup → ghi chú phiên bản 3 phần → bảng 15 điểm lệch → 12 lỗi thường gặp → Key Points → 4 thử thách → đóng sách.
  **Citation đã mở file thật để kiểm:** `{FINAL}/data/SecurePrefs.kt` (41–42, 44–54, 56–74 — file **duy nhất chỉ có ở `final`**), `{FINAL}/data/Prefs.kt` (class chapter 9 còn nguyên), `{FINAL}/MainActivity.kt` (55, 67, 82), `{FINAL}/RecipeApp.kt` (48–69), `{FINAL}/viewmodels/RecipeViewModel.kt` (40, 75–76, 78, 101–111, 130–135), `{FINAL}/ui/MainScreen.kt` (56, 67–68), `{FINAL}/data/database/RecipeDatabase.kt` (40–42, 51, 59–66, 68–84), `{FINAL_ROOT}/gradle/libs.versions.toml` (24–25, 86, 89), `{FINAL_ROOT}/app/build.gradle.kts` (84–91), `res/xml/data_extraction_rules.xml`, `AndroidManifest.xml` (dòng 7 `allowBackup="true"`), cộng `{CH10}/RecipeApp.kt` (57–62) và `{CH10}/data/database/RecipeDatabase.kt` (55) để cho thấy cái gì đổi khi thêm mã hoá; citation sách: `{BOOK}` dòng 15–18, 22–25, 48–62, 64, 70–72, 96–100, 112–123, 128, 139–146, 212, 223–227, 247, 252–255, 271–281, 286–300, 313–333, 374, 427–431, 436, 443–451, 459–481, 499–515.
  **15 điểm lệch sách ↔ project/tài liệu, gom thành bảng riêng (mục 17) kèm cột "nên theo cái nào".** Đáng chú ý nhất: (1) sách dòng 64 nói `StorageManager` "you must retrieve this from the main thread" — **ngược lại sự thật**, `getCacheQuotaBytes()` là phần *nên tránh* main thread; (2) mọi snippet ghi file của sách dùng `context.filesDir` trong Activity → **không compile**, Activity *chính là* Context; (5) sách viết `KeyInfo.isInsideSecurityHardware()` với mốc API 28/29 — **sai cả tên lẫn số**: tên đúng `isInsideSecureHardware()`, và cả deprecation lẫn `getSecurityLevel()` đều ở **API 31** (đã tra API diff level 31); (8) sách dòng 374 bảo viết `prefs = SecurePrefs(this)` nhưng chính sách đã đổi tên field thành `securePrefs` ở bước trước → **sách tự mâu thuẫn**; (9) tên database đổi âm thầm `"Recipes"` (ch10, `RecipeApp.kt:57–62`) → `"recipe_database"` (ch11) — **mất dữ liệu người dùng thật**, và đây cũng là chapter `getInstance()` thôi làm code chết; (12) `allowBackup="true"` + `data_extraction_rules.xml` **mọi rule đều bị comment** → khôi phục sang máy mới lấy về file database đã mã hoá nhưng **không lấy được master key** (nằm trong Keystore, không backup được) → app crash; sách không nhắc một chữ nào về mặt này.
  **Phân tích web tự thêm:** `SecurePrefs.getInt()` trả `Int` (mặc định 0) trong khi `Prefs.getInt()` của ch09 trả `Int?` → dòng `if (currentIndex != null)` ở `MainScreen.kt:68` **luôn đúng, đã thành code chết** — cùng loại phát hiện đã ghi ở Task 27 nhưng lần này truy đủ 6 chỗ phải sửa theo khi đổi một kiểu; toàn bộ chuỗi nối dây chạy **trên main thread trong `RecipeApp.onCreate()`** (tạo master key + đọc file XML đã mã hoá + mã hoá ghi lại) — đã tách thành mục riêng kèm phiên bản đề nghị; hàm sinh passcode 3 vấn đề trong 8 dòng (`kotlin.random.Random` không phải `SecureRandom` · chỉ a–z ≈ 70 bit · `String` không xoá được khỏi memory, khác `CharArray` có thể `fill('\u0000')`); `diff -rq --strip-trailing-cr` chứng minh chapter chỉ có **9 file khác nhau** giữa `starter`/`final`, trong đó 5 thay đổi là **dấu vết refactor chưa dọn** (import `Room` + `Prefs` không dùng ở `RecipeApp.kt:38–39` · `LocalContext` không dùng thêm vào `ShowRecipeList.kt:58` · một dòng trống thêm vào `IngredientDao.kt:45` · null-check luôn đúng ở `MainScreen.kt:68`) và project còn **nâng thêm 5 version không liên quan** (compose-ui, androidx-activity, material3, fonts, navigation) mà sách chỉ nói thêm 2 dependency; `grep filesDir` toàn bộ `aaf-materials/*/projects/` = **0 kết quả** → phần đầu chapter là kiến thức thuần, không có chỗ nào trong project dùng.
  **Ghi chú phiên bản (chia 3 mục con — chapter lỗi thời nhiều nhất cả sách):** `androidx.security:security-crypto` **toàn bộ API đã deprecated ngay ở bản stable 1.1.0** (30/07/2025) và sẽ không có bản mới; `MasterKeys.getOrCreate()` deprecated từ 1.1.0-alpha01 (10/06/2020) — **deprecated trước cả khi sách ra**; Google **không có migration guide chính thức** và trang Cryptography còn tự mâu thuẫn → đã trình bày trung thực là chưa có câu trả lời gọn, kèm 3 lựa chọn thực tế thay vì giả vờ có đáp án. SQLCipher: `net.zetetic:android-database-sqlcipher` EOL 31/08/2023 → `net.zetetic:sqlcipher-android:4.18.0` (18/08/2026), package `net.sqlcipher.database` → `net.zetetic.database.sqlcipher`, `SupportFactory` → `SupportOpenHelperFactory`, và **bẫy mới: `loadLibs` đã bị bỏ** nên phải tự `System.loadLibrary("sqlcipher")` không thì `UnsatisfiedLinkError` (nguyên nhân: yêu cầu 16KB page size của Google Play). Room: 2.x (2.8.4) vẫn dùng được `.openHelperFactory()` đúng như sách; **Room 3 (`androidx.room3` 3.0.2) bỏ hẳn `SupportSQLite` → không còn `openHelperFactory`**, phải dùng `setDriver()`. Ghi thêm một quan sát về vị thế: release notes của Room (cả 2.x và 3.x) **không nhắc SQLCipher hay encryption một lần nào**, toàn bộ phần tích hợp do Zetetic cung cấp → đó là phụ thuộc bên thứ ba cần biết khi chọn.
  Đã kiểm: `astro check` **0 error / 0 warning / 0 hint** (31 file) · `astro build` exit 0, 12 trang, `dist/.../ch11-advanced-storage/index.html` **278.505 byte** · route dev server HTTP **200**, HTML trả về **370.407 byte**, 27 `<h2>` + 27 `<h3>` + **19 fieldset** + **4 bài tập** + widget `#save-order`/`#check-order` với 6 `data-correct`, **0 placeholder `@@`** · smoke test regression **11/11 chapter + trang chủ = 200**, `ch99-fake` → **404** · 19/19 input `name` là duy nhất (`q1`–`q19`), mỗi `<fieldset>` có `data-answer` khớp logic chấm điểm · mọi `const` code block đều được dùng (0 biến khai mà không dùng) · `git -C aaf-materials status --porcelain` **rỗng** → reference source không bị đụng. **Sự cố đã xử lý:** còn sót placeholder `/* @@CODE@@ */` ở cuối frontmatter (dòng 415) từ lúc viết theo từng khối — vô hại về render (nằm trong comment JS nên không lọt ra HTML, `grep '@@'` trên HTML = 0) nhưng vẫn xoá để file sạch. **Bài học giữ lại:** placeholder đặt trong comment JS của frontmatter sẽ **không** bị phát hiện bởi lần kiểm "0 placeholder trong HTML render" — phải grep cả file source, không chỉ output.

### Phase 6 — Redesign UI/UX (mở & đóng 2026-08-29)

**Lý do mở phase:** user yêu cầu redesign toàn bộ giao diện website theo hướng **"Android Developer Learning Platform + Jetpack Compose + Material Design 3"**, với ràng buộc rất rõ: **chỉ đổi UI/UX, không đổi một chữ nội dung khoá học**, giữ nguyên 100% chức năng đang có, tận dụng DOM/component/function hiện tại thay vì rewrite, không thêm dependency mới, không đổi syllabus, không thêm backend/auth/database. 10 vùng phải làm: header · curriculum sidebar · dashboard · **lesson page (quan trọng nhất)** · learning outcomes · practice/quiz · common mistakes · lesson navigation · light+dark theme · responsive 375/tablet/1440.

- `[x]` **Task 46 — Redesign UI/UX toàn website.** Hoàn thành 2026-08-29. **Không sửa nội dung khoá học** — đã chứng minh bằng cách trích toàn bộ text trong `.lesson`/`.quiz` của 12 trang build ra và so sánh: **949.489 ký tự, md5 `e688285a164f793a33abe66d475b42d6`, y hệt baseline trước redesign** (`web/content-baseline.txt`), kiểm lại sau **mỗi** lần sửa.
  - **Design system mới (`web/src/styles/`, 8 file / 3.802 dòng):** `tokens.css` (166 dòng — màu M3 primary tím-indigo + accent xanh Android, typography, spacing scale `--s1`…`--s16`, radius, elevation, gradient; light + dark là 2 bộ token riêng), `base.css` (146), `shell.css` (606 — topbar + curriculum rail + drawer), `dashboard.css` (736), `lesson.css` (1.560), `quiz.css` (506), `icons.css` (61), `global.css` (21 — chỉ `@import` theo đúng thứ tự cascade). Trước đó CSS nằm rải rác trong **22 khối `<style>` scoped** của các file lesson/quiz.
  - **Header:** brand `Android Jetpack Compose` + icon robot Android, **ô tìm kiếm toàn khoá** (combobox + listbox, phím ↑↓/Enter/Escape, `Ctrl/Cmd + K`), thanh tiến độ toàn khoá `n/12 bài`, nút đổi sáng/tối, nút mở drawer trên mobile.
  - **Curriculum sidebar:** 3 module (đúng `SECTIONS` đang có) → lesson lồng bên trong, `<details>` gấp/mở được, module đang chứa bài đọc **tự mở**, bài đang học nổi bật (`primary-container` + vạch trái), bài đã xong có dấu check xanh Android, **thanh tiến độ riêng cho từng module** + bộ đếm `x/n bài xong`.
  - **Dashboard (`index.astro`, 254 dòng):** hero "Android Development with Jetpack Compose" + 5 số liệu thật, `Continue Learning` trỏ đúng bài kế tiếp chưa xong, khối "Tiến độ của bạn", 3 module card. **Mọi số đều dẫn xuất từ source thật**, không bịa syllabus.
  - **Lesson page (`[slug].astro`, 305 dòng):** breadcrumb → số module/lesson → tiêu đề lớn → thời lượng/độ khó/số code block → thẻ tổng quan → **mục lục sinh từ `h2` thật của file lesson** → nội dung với chiều rộng đọc tối ưu → navigation cuối trang (`← Bài trước` / `Đánh dấu đã học` / `Bài sau →`, nút Next mạnh nhất).
  - **Data layer mới (`src/data/lessonStats.ts`, 157 dòng):** dùng `import.meta.glob(..., { query: '?raw' })` đọc chính file `.astro` của lesson lúc build để đếm **`h2` thật, số code block, số câu quiz, số từ** → không có con số nào hard-code, không có syllabus nào bịa ra.
  - **`CodeEnhance.astro` (83 dòng, mới):** progressive enhancement bọc mọi `<pre class="astro-code">` thành `.codeblock` có **nhãn ngôn ngữ + nút copy**; code block giữ nền tối cả ở light theme. Callout đổi thành card Material có icon + accent (không còn kiểu `[INFO]`/`[WARN]`); bảng có header rõ, tự cuộn khi hẹp.
  - **Không thêm dependency nào.** `package.json` không đổi. Tìm kiếm tự viết bằng index JSON render sẵn lúc build (không dùng Pagefind) → **"Hướng mở rộng" mục 1 coi như đã làm xong mà không phải thêm thư viện**.

  **2 điều phải báo vì chạm vào ràng buộc "cần xoá file / thêm dependency / đổi kiến trúc thì phải hỏi trước":**
  1. **Cách phân phối CSS đã đổi.** Redesign buộc phải bỏ **22 khối `<style>` scoped (3.388 dòng CSS)** nằm trong các file lesson/quiz và chuyển hết sang stylesheet global. Đây là thay đổi *kiến trúc CSS*, không phải chỉ đổi màu. **Không file nào bị xoá** và **không nội dung khoá học nào bị đụng** (đã chứng minh byte-identical). Lý do bắt buộc: 22 bản CSS scoped khác nhau cho cùng một loại phần tử thì không thể có design system nhất quán, và Astro scope-hash làm mọi rule chỉ áp cho đúng 1 file.
  2. **"search" và "theme" được liệt kê trong brief như chức năng đang có, nhưng thực tế code không hề có.** Đã kiểm: trước redesign không có ô tìm kiếm và không có nút sáng/tối nào. Hai thứ này là **UI mới**, chỉ dựng trên dữ liệu đã có (`ALL_CHAPTERS` + `h2` thật của lesson) và `localStorage`, không thêm nguồn dữ liệu nào.

  **Audit đối kháng (chạy sau khi tự review xong):** dựng workflow 6 chiều để *cố tình phá* bản redesign (dead state class · dark theme state · overflow · brief compliance · CSS integrity · a11y), rồi mỗi finding được giao cho verifier độc lập với nhiệm vụ **bác bỏ**. Kết quả: **6 defect thật đã sửa, 3 finding bị bác bỏ có bằng chứng** (không sửa theo, nhưng vẫn làm 2 việc vệ sinh rẻ: Escape đóng drawer, `role="img"` cho icon check).
  - **Defect 1 (nặng nhất) — token `--s7` chưa bao giờ được định nghĩa.** Spacing scale nhảy từ `--s6` sang `--s8`, mà `dashboard.css` dùng `var(--s7)` ở 2 chỗ. Theo spec CSS, `var()` không giải được làm **cả khai báo** thành invalid at computed-value time → về `unset` → giá trị khởi tạo `0`, **và rule cũ bên dưới không được sống lại**. Hậu quả thật: `.hero-stats` mất cả margin trên và dưới, `.hero` ở tablet mất padding ngang. Sửa bằng **1 dòng** `--s7: 1.75rem` trong `tokens.css`.
  - **Defect 2 — drawer đóng vẫn nằm trong thứ tự Tab.** Ở `max-width: 1024px` drawer chỉ bị đẩy ra ngoài bằng `transform`, nên các link bên trong vẫn focus được; vì `.rail` là `position: fixed` nằm ngoài viewport, con trỏ bàn phím **không thể cuộn tới để nhìn thấy** (vi phạm WCAG 2.4.7). Sửa bằng `visibility: hidden` + `transition: visibility 0s linear 0.22s` để vẫn giữ animation trượt.
  - **Defect 3 + 4 — vòng focus bị cắt ở 2 chỗ.** `.module` (rail) và `.quiz/.lesson details` đều có `overflow: hidden`; vòng `:focus-visible` mặc định vẽ *ngoài* biên với `outline-offset: 2px` nên bị clip mất hoàn toàn — ảnh hưởng 3 module toggle + **31 thẻ `<details>`** trong nội dung. Sửa bằng `outline-offset: -3px` (vẽ vào trong), không phải sửa `overflow` nên bo góc vẫn nguyên.
  - **Defect 5 — combobox tìm kiếm thiếu `aria-activedescendant`.** Bấm ↑↓ chỉ đổi màu chữ; DOM focus không rời khỏi input nên screen reader không có kênh nào biết dòng nào đang được chọn. Sửa: mỗi `<li role="option">` có `id="sr-N"`, `render()`/`closePanel()` cập nhật `aria-activedescendant`, thêm `aria-autocomplete="list"`.
  - **Defect 6 — rule CSS chết + viền kép.** `.quiz pre:not(.astro-code):not(.tree)` trong `lesson.css` có specificity 0-3-1 (vì **`:not()` cũng tính specificity**) nên âm thầm thắng các rule dành riêng cho quiz (0-1-2 / 0-2-1) → khối quiz thành rule chết; đồng thời 1 khối Shiki trong `.exercise` bị viền 2 lớp. Sửa bằng cách thêm đủ 2 `:not()` vào 3 selector của `quiz.css` + comment giải thích để lần sau không ai xoá.
  - **3 defect có sẵn từ trước (không do redesign) cũng đã lộ ra và đã sửa:** `white-space: nowrap` trên code trong bảng (vỡ layout ở mobile) · `.chip` vô hiệu hoá thuộc tính `hidden` của `#lesson-status` · 3 chỗ tràn ngang ở 375px.
  - **Đã kiểm (chạy thật, không suy luận):** `astro check` → **0 error / 0 warning / 1 hint** (hint duy nhất là `document.execCommand` deprecated ở `CodeEnhance.astro:64` — chủ ý, đó là fallback khi không có Clipboard API) · `astro build` → exit 0, **12 trang** · contrast **37/37 đạt WCAG AA cả 2 theme** · 0 thẻ bị mất tác dụng `hidden` · 935 token dài đều nằm trong vùng đã có `overflow-wrap`, **0 token nằm ngoài** · 0 ngữ cảnh tràn ngang thật · **114/114 bảng** vừa khung hoặc tự cuộn · **6/6 flow học** (quiz grading, widget xếp thứ tự, progress, search, theme, copy code) OK · grep bundle xác nhận từng bản sửa đã ship (`--s7:1.75rem` · `quiz-code:not` ×2 · `visibility:hidden` · `summary:focus-visible` ×3 · `aria-activedescendant` ×3 · `Escape` ×2 · `aria-autocomplete="list"` · `class="check" role="img"` ×11).
  - **Cách kiểm CSS/DOM trong môi trường này (bài học giữ lại):** project **không có test runner, không jsdom, không playwright** → mọi kết luận về CSS/DOM đều phải rút ra bằng **phân tích tĩnh HTML build ra + CSS bundle trong `dist/_astro/*.css`**, không phải mở browser. Đã viết **82 script `.py` + 8 script `.mjs`** trong `web/scripts/` chỉ để verify (parse CSS bằng `css-tree`, so specificity, dò rule chết, đo contrast, đếm token dài, dựng lại cascade). **Chúng là công cụ build-time, không ship gì vào `dist`.** Lệnh build phải là `node_modules/.bin/astro build` (`npx astro build` fail vì thiếu `@rolldown/binding-wasm32-wasi`); shell này pipe sang `head` thỉnh thoảng trả chuỗi lạc nên phải ghi ra file rồi đọc; heredoc `python - <<'PY'` fail nên phải viết file `.py` thật; console Windows là cp1252 nên script in text audit phải `sys.stdout.reconfigure(encoding="utf-8")`.
  - **Chưa dọn:** `web/` còn **11 file scratch** ở root (`.checks*.txt`, `.markup.txt`, `.styledump.css*`, `.uibaseline-*.txt`, `.classsamples.txt`) mà `.gitignore` hiện **không** che, cộng `web/content-baseline.txt` (file dùng để chứng minh nội dung không đổi — nên giữ) và `web/scripts/`. Nếu muốn commit thì nên thêm rule ignore trước; chưa tự làm vì chưa được yêu cầu.

### Phase 7 — Khoá học đứng độc lập + tách chương dài (mở 2026-08-30)

**Lý do mở phase:** user phản hồi 2 vấn đề, cùng một gốc — website hiện đọc như **bài review sách**, không như một khoá học:

1. **Giọng văn bám sách.** Đo trên toàn bộ file lesson/quiz: **1.532 lần nhắc "sách"**, **418 trích dẫn dạng "chapter N"**, **429 câu dẫn nguồn** (≈139 câu/chapter). Người học **không có sách và sẽ không mua sách** — mỗi câu "sách nói…", "sách dòng 65 viết…" đẩy họ ra ngoài. Khoá học phải đọc như **khoá học tiếng Việt do một tác giả Việt biên soạn**; sách chỉ là nền, không phải một nhân vật trong bài.
2. **Chương quá dài.** 125.489 từ ≈ 10,5 giờ cho 11 chương; chương dài nhất 14.096 từ ≈ 70 phút. Chương lớn phải tách thành chương nhỏ.

**9 nguyên tắc bất biến của phase này** (áp cho mọi chương, không được thương lượng lại):

1. Khoá học **đứng độc lập** — người học không có sách, mọi thứ cần thiết phải nằm trong bài.
2. **GIỮ 100% ĐỘ SÂU.** Không cắt kiến thức, không cắt code, không cắt caveat, không viết ngắn lại. Dài là **có chủ đích**. Chỉ đổi **cách trình bày và giọng**. Đề xuất nào làm mất thông tin là **sai**.
3. Vẫn ghi nguồn (bản quyền) nhưng **gom vào một khối "Nguồn tham khảo" ở CUỐI mỗi chương nhỏ** — không rải trong mạch dạy.
4. Gọi là **"Chương"**, không bao giờ gọi "Bài".
5. Số chương nhỏ là **10.1, 10.2, 10.3…**; chương lớn trở thành tiêu đề nhóm ở sidebar, nội dung thật nằm ở chương nhỏ.
6. **Chương nhỏ KHÔNG được ngắn.** Mỗi chương nhỏ là **một đơn vị kiến thức hoàn chỉnh**: tối thiểu 2.000 từ, lý tưởng 2.500–4.000; trên 4.500 mới xét tách thêm **và chỉ khi không làm vỡ concept nào**. **Cấm tách một concept ra hai chương nhỏ.**
7. Nội dung dạng "chỗ này sách sai" / "sách lệch code thật" → **một mục riêng ở cuối chương nhỏ liên quan, tên cố định "Cạm bẫy & tài liệu lỗi thời"**. Giữ **toàn bộ** giá trị cảnh báo nhưng viết lại để **không nhắc sách**: diễn đạt kiểu "tài liệu cũ nói X, Android hiện nay là Y" hoặc "đây là chỗ người mới hay sai". **TUYỆT ĐỐI KHÔNG XOÁ loại nội dung này — nó là phần giá trị nhất của khoá học.**
8. **Quiz tách theo từng chương nhỏ**, đặt ngay sau nội dung; câu hỏi + bài tập được **chia lại theo chủ đề**, không dồn về một chỗ.
9. Không bịa: mọi khẳng định phải dựa trên nội dung đọc được thật. Không thêm framework/dependency, không đổi kiến trúc web.

- `[x]` **Task 47 — Tách Chương 10 (Room Database) thành 10.1–10.4 làm chuẩn mẫu.** Hoàn thành 2026-08-30. Chương 10 được chọn làm mẫu vì nó dài nhất và có nhiều mục nhất (23 `h2`), nên nếu chuẩn chạy được ở đây thì chạy được ở mọi chương.
  - **4 chương nhỏ, tất cả đều vượt ngưỡng 2.000 từ, không chương nào bị cắt nội dung:**

    | Chương nhỏ | File lesson | Từ prose | Mục | Khối code | Quiz |
    |---|---|---:|---|---:|---|
    | 10.1 Vì sao cần database | `Ch10_1RoomLaGi.astro` | 4.632 | 8 `h2` (mục 1–6 + Cạm bẫy + Nguồn) | 2 | 7 câu + 2 bài tập |
    | 10.2 Entity · DAO · Database | `Ch10_2EntityDaoDatabase.astro` | 5.564 | 6 `h2` + 8 `h3` (mục 7–10) | 6 | 16 câu + 3 bài tập |
    | 10.3 Repository & ViewModel | `Ch10_3RepositoryViewModel.astro` | 3.918 | 8 `h2` + 3 `h3` (mục 11–15) | 10 | 10 câu + 2 bài tập |
    | 10.4 Giao diện & cạm bẫy | `Ch10_4GiaoDienVaCamBay.astro` | 4.693 | 10 `h2` + 3 `h3` (mục 16–23) | 4 | 8 câu + 2 bài tập |

    Tổng **18.807 từ prose** so với 14.604 từ của bản gộp cũ — **tăng**, vì mỗi chương nhỏ phải tự đứng được (nhắc lại ngữ cảnh, thêm mental model) chứ không phải vì thêm kiến thức mới. Đúng nguyên tắc #2: không mất gì.
  - **Số mục chạy liên tục 1–23 xuyên 4 file** (không reset về 1 ở mỗi chương nhỏ) để người học vẫn thấy đây là một mạch kiến thức. Tham chiếu chéo giữa các file đều có tiền tố `Chương 10.x, mục N`.
  - **Quiz: 19 câu → 41 câu** (7+16+10+8), 9 bài tập, chia lại theo chủ đề. Chỉ Q1/Q2/Q4/Q5 của quiz cũ còn dùng được (chúng thuộc phạm vi 10.2); 19 câu cũ đều nằm trong phạm vi 10.1–10.3 nên **8 câu của 10.4 phải viết mới**. Widget xếp thứ tự (7 `data-correct`) chuyển sang 10.3. Script chấm điểm **scope theo `form`** và mẫu số là `fieldsets.length`, nên mỗi file quiz riêng vẫn chấm đúng mà không phải sửa JS.
  - **Mục "Cạm bẫy & tài liệu lỗi thời"** có ở cả 4 chương nhỏ, dùng chung `id="cam-bay"` (trước đó 10.3 dùng `de-hieu-sai` — đã đổi cho khớp; grep xác nhận không file nào trỏ vào anchor cũ). **Khối "Nguồn tham khảo"** đúng 1 khối ở cuối mỗi chương nhỏ, không còn dẫn nguồn rải trong mạch dạy.
  - **Giọng: đã sạch.** Trên 8 file mới (4 lesson + 4 quiz), mọi kết quả grep `sách` đều là chữ **`danh sách`** — không còn một câu nào nói *về* sách.
  - **Data + UI (không thêm dependency, không đổi kiến trúc):**
    - `src/data/chapters.ts`: thêm `subNumber?` + `parentTitle?` vào `ChapterInfo`, thêm 3 helper `chapterFileKey()` / `chapterLabel()` / `groupChapters()`; 1 entry `ch10` cũ → 4 entry `ch10-1…ch10-4`.
    - `src/lib/progress.ts`: `SPLIT_MAP` + `migrateProgress()` (khoá `hoc-android-tv:progress-migrated`) — ai đã đánh dấu xong `ch10-room-database` thì được ghi nhận xong cả 4 chương nhỏ, chạy **một lần**, không mất tiến độ cũ.
    - `src/data/lessonStats.ts`: sửa bucket key để `Ch10_1…Ch10_4` map đúng slug.
    - `src/data/lessons.ts`: đăng ký 4 cặp `{Lesson, Quiz}`.
    - `BaseLayout.astro`: sidebar gom `10.1–10.4` dưới một tiêu đề nhóm `Room Database` (`groupChapters`).
    - `index.astro` + `dashboard.css`: badge chứa được nhãn 4 ký tự (`min-width` thay vì cố định 30px) nên `10.1` không bị bóp; hero/curriculum đếm **11 chương lớn**, tiến độ đếm **14 trang** — hai con số khác nhau là **có chủ đích**, đã ghi comment tại chỗ.
    - **Bỏ hết chữ "Bài" trong UI** (nguyên tắc #4): `BaseLayout.astro` (aria-label menu, placeholder tìm kiếm, nhãn tiến độ), `ProgressToggle.astro` (chữ trên nút + chuỗi JS), `[slug].astro` (5 chỗ aria-label/heading/placeholder). **Bài học giữ lại:** 2 chuỗi "bài học" sót lại chỉ lộ ra khi grep **`dist/index.html`**, không lộ khi grep `index.astro` — sweep wording phải kiểm trên HTML build ra, vì chuỗi có thể đến từ layout.
  - **Đã xoá 2 file cũ** `Ch10RoomDatabase.astro` + `Ch10Quiz.astro` sau khi (a) xác nhận cả hai có trong commit `9e8800d` nên khôi phục được, và (b) audit xác nhận 4 file mới là **superset** nội dung của chúng.
  - **Tham chiếu vào Chương 10 từ chapter khác:** ~40 chỗ ở 12 file đã đổi. Chỗ nói về **cơ chế cụ thể** thì ghim đúng chương nhỏ (Ch07 → 10.1/10.2/10.3 · Ch08 → 10.2/10.3 · Ch09 → 10.1/10.3 · Ch11 → 10.2/10.3); chỗ nói **ở tầm lộ trình** thì giữ nguyên "Chương 10" — đã grep kiểm lại từng chỗ.
  - **Đã kiểm (chạy thật):** `astro build` → **exit 0, 15 trang**, có đủ 4 route `ch10-*` · `astro check` → **0 error**, 17 hint (đều có sẵn từ trước: 2 const không dùng trong `scripts/x_match.mjs` + `document.execCommand` trong `CodeEnhance.astro`) · 4 trang build ra nặng 132K/164K/160K/148K, **0 placeholder** · số `<fieldset>` trên HTML = 7/16/10/8, **khớp đúng con số ghi ở đầu quiz** · badge trang chủ render `1…9, 10.1, 10.2, 10.3, 10.4, 11` · sidebar render 1 `ch-group-head` + 4 dòng `10.x` · chip đọc `Chương 10.1 · 11 chương` · chip số liệu 25/30/25/25 phút và 8/6/8/10 mục · **0 id trùng** trên cả 4 trang (lấy ch09 làm đối chứng).
  - **Còn tồn (không blocker):** `Ch01WelcomeToAndroidKotlin.astro:1449-1450` và `:1466` còn trộn "chapter" tiếng Anh với "Chương" đã đổi — nói về chapter khác 10 nên **ngoài phạm vi Task 47**, sẽ xử lý khi tới lượt Ch01 ở Task 48.

- `[~]` **Task 48 — Áp chuẩn Phase 7 (Chương 10) sang 10 chapter còn lại. ĐANG CHẠY.** Với **từng** chapter: (1) xoá giọng bám sách theo nguyên tắc #1/#3/#7; (2) xét tách theo nguyên tắc #5/#6; (3) tách quiz theo nguyên tắc #8; (4) sửa tham chiếu chéo; (5) build + kiểm HTML như Task 47.

  **Trạng thái từng chapter (đo lại 2026-08-31 bằng `measure123.py`; giọng đo trên file `.astro` nguồn, KHÔNG đo trên `dist`):**

  | Chapter | Trạng thái | Tách thành | Quiz (số câu/chương nhỏ) | "sách" giọng-sách | "chapter" |
  |---|---|---|---|---:|---:|
  | Ch01 Welcome | ✅ xong | 1.1–1.4 | 9 · 10 · 11 · 10 | **0** | **0** |
  | Ch02 Android Studio | ✅ xong | 2.1–2.3 | 9 · 12 · 9 | **0** | **0** |
  | Ch03 Fundamentals | ✅ xong (session này) | 3.1–3.4 | 11 · 15 · 11 · 10 | **0** | **0** |
  | Ch04 Gradle | ⬜ kế tiếp | — | 19 (chưa tách) | 97 | — |
  | Ch05 Jetpack Compose | ⬜ | — | — | 40 | — |
  | Ch06 Advanced Compose | ⬜ | — | — | 82 | — |
  | Ch07 Architecture | ⬜ | — | — | 36 | — |
  | Ch08 Networking | ⬜ | — | — | 58 | — |
  | Ch09 DataStore | ⬜ | — | — | 80 | — |
  | Ch11 Advanced Storage | ⬜ | — | — | 89 | — |
  | Ch10 (Task 47) | ✅ xong | 10.1–10.4 | 7 · 16 · 10 · 8 | 0 | 0 |

  - **Ch01 (đã xong ở session trước):** 4 chương nhỏ, prose ≈ 15.4k từ (lesson) + 13.0k (quiz), mỗi lesson có đủ `#nguon` + `#cam-bay`, 0 `<p class="src">` sót trong body. 2 hit "sách" mà script bắt được là **false-positive "danh sách" bị xuống dòng** (Ch01_4 dòng 394 + Ch01_4Quiz dòng 38) — đã xác nhận bằng grep, không phải giọng-sách. Còn tồn (không blocker, đã ghi ở mục "Việc chưa lên kế hoạch"): chất lượng quiz Ch01 (đáp án đúng hay là lựa chọn DÀI NHẤT: Ch01_1Quiz 7/9, Ch01_2Quiz 9/10) + 2 chỗ trộn Anh–Việt ở monolith cũ.
  - **Ch02 (đã xong ở session trước):** 3 chương nhỏ, prose ≈ 22.2k (lesson) + 12.1k (quiz), đủ `#nguon` + `#cam-bay`, 0 giọng-sách/chapter.
  - **Ch03 (xong session này, 2026-08-31):** 4 chương nhỏ (3.1 Activity & giao diện · 3.2 String resource & debug · 3.3 Manifest/Intent/Permission · 3.4 Theme & đối chiếu). Prose lesson ≈ 39.0k từ (9.9k + 11.2k + 9.1k + 8.8k) + quiz ≈ 22.1k — so với monolith cũ 13.7k prose, **tăng gấp ~2.8 lần**, đúng nguyên tắc #2 (giữ nguyên độ sâu, chỉ đổi giọng). Mỗi lesson đủ `#nguon` + `#cam-bay`, 0 `<p class="src">` sót. Quiz 11/15/11/10 câu, mỗi quiz có widget xếp thứ tự riêng.
    - **Factcheck số dòng/tên file với `aaf-materials/`:** Ch03_2Quiz (72 claim → 1 sai, đã sửa) và Ch03_3Quiz (104 claim → 3 sai, đã sửa) do workflow verify; Ch03_4Quiz mình tự đối chiếu tay với project thật → **0 lỗi**. **`Ch03_1Quiz` CHƯA được reader độc lập factcheck** — cả 2 agent phân cho nó đều chết (1 lỗi 429, 1 stall qua 6 lần thử). Cần factcheck lại khi có dịp.
    - **6 sửa đã áp + build lại:** Ch03_2Quiz (2: comment "bài tập 3"→"2"; cắt câu lỗi Compose về đúng phần có trong nguồn) · Ch03_3Quiz (4: sửa số liệu 11/12→7/15; nới phạm vi bài tập intro sang project Chương 8; "bốn khác biệt nội dung"→"3 nội dung + 1 hình thức"; tách chắc/không-chắc ở Câu 4 `<permission>`). Thêm note length-bias 10/10 vào frontmatter Ch03_4Quiz.
    - **Ghi nhận length-bias (chưa sửa):** tỉ lệ đáp-án-đúng-là-lựa-chọn-NGẮN-NHẤT: 3.1 = 5/11 · 3.2 = 7/15 · 3.3 = 8/11 · 3.4 = **10/10**. Đây là tín hiệu đoán được; nếu rà chất lượng quiz cả khoá thì Ch03_4 rồi Ch03_3 là nơi nới dài đáp án đúng trước tiên. Đã ghi trong frontmatter 2 file.
    - **Đã verify (chạy thật session này):** `astro check` → **0 error**, 0 warning, 64 hint (đều có sẵn từ trước) · `astro build` → **EXIT 0, Complete**, `dist/chapters/` có **22 route** gồm đủ 4 route `ch03-*`.
  - **CÒN TỒN — cần user quyết (xoá file phải hỏi trước)** *(giải quyết xong về sau: monolith Ch01/02/03 + quiz ORPHAN đã xoá ở batch Stage 1 của redesign — `b43d81f`; scratch `_t48_*` nằm trong `web/scripts/` gitignored, quyết định cuối 2026-09-11 = GIỮ trên máy, không track — xem `QA_FINAL_REPORT.md` mục cleanup)*:
    - **Monolith cũ còn trên đĩa, không ai tham chiếu** nhưng vẫn bị `lessonStats.ts` glob vào bucket "01"/"02"/"03" không chương nào nhận: `Ch01WelcomeToAndroidKotlin.astro` + `Ch01Quiz.astro`, `Ch02GettingStartedAndroidStudio.astro` + `Ch02Quiz.astro`, `Ch03AndroidFundamentals.astro` + `Ch03Quiz.astro`. (Ở Task 47, monolith Ch10 đã được xoá sau khi xác nhận git khôi phục được + bản mới là superset — Ch01/02/03 CHƯA làm bước này.)
    - **~67 file rác scratch `web/scripts/_t48_*`** + vài file `.txt`/`.css` tạm ở `web/` (`.checks*.txt`, `.styledump.*`, `.uibaseline-*`, `content-baseline.txt`…) từ quá trình audit — dọn được nhưng cần user đồng ý.

## Quyết định quan trọng đã chốt

1. **Web stack: Astro.** Lý do: nội dung nhiều bài học + cần sidebar/search + Markdown là chính, Astro có sẵn xử lý Markdown/syntax highlighting, gần như 0 JS phía client trừ chỗ cần tương tác (quiz, progress) → ít code phải tự viết/bảo trì hơn so với tự viết HTML/JS thuần. **Đã cài đặt từ Task 5; hiện dùng Astro 7.2.8.**
2. **Trích xuất EPUB dùng Python stdlib** (`zipfile` + `xml.etree.ElementTree`), không cài `ebooklib`/`beautifulsoup4`/`lxml`. Lý do: nội dung EPUB (`OEBPS/html/seg*.xhtml`) là XHTML sạch, hợp lệ như XML, heading/code-block có class CSS rõ ràng (`segment-chapter`, `code-block`) → parser chuẩn của Python xử lý được, không cần thêm dependency.
3. **Cấu trúc thư mục:** `content/book/` (Markdown trích từ sách) · `docs/` (tài liệu quản lý project) · `web/` (website Astro). Không đụng `aaf-materials/` và file `.epub`.
4. **Reference sources không sửa:** mặc định. Chỉ sửa `aaf-materials/` nếu 1 task cụ thể yêu cầu và user đã đồng ý rõ ràng.
5. **Thứ tự ưu tiên nguồn khi viết lesson:** EPUB > `aaf-materials/` > developer.android.com / kotlinlang.org > nguồn khác.
6. **Chapter cho vertical slice (Task 7): CHỐT — Chapter 5 (Jetpack Compose).** Chapter đầu tiên có code Android thật + rõ ràng để mapping (Chapter 1-4 chủ yếu là nền tảng, Chapter 1 thậm chí không có Android project thật).
7. **Xử lý xung đột license `aaf-materials/` (mục Vấn đề/rủi ro, phát hiện khi chuẩn bị Task 7): user đã xác nhận — tiếp tục dùng code thật từ `aaf-materials/` trong lesson.** Lý do user đưa ra: đây là dự án cá nhân, không public/phân phối, và user đã trả tiền mua sách/vật liệu đi kèm nên tự nhận trách nhiệm rủi ro pháp lý này. Quyết định 2026-08-26, do user chốt trực tiếp (không phải suy luận của Claude). Vẫn giữ nguyên tắc: không sửa file gốc trong `aaf-materials/`, ghi rõ nguồn (file + dòng) khi trích code vào lesson.
8. **Không thêm giai đoạn "hiện đại hoá app" vào roadmap (2026-08-27, user xác nhận trực tiếp).** User hỏi làm rõ liệu website có nên tiến tới xây/nâng cấp lại app trong sách bằng kiến thức Android hiện nay không — đã trình bày 2 lựa chọn (giữ scope hiện tại vs. thêm phase hiện đại hoá) và user chọn **giữ scope hiện tại**. Nghĩa là: mục tiêu vẫn là dạy đúng nội dung + code thật trong sách/`aaf-materials/`, nguồn hiện đại (developer.android.com/kotlinlang.org) chỉ dùng để kiểm tra/ghi chú chênh lệch version (xem mục "Version công cụ trong sách cũ hơn hiện tại" bên dưới), không dùng để viết lại hay nâng cấp app trong lesson.
9. **Cập nhật knowledge base claude-obsidian là phần của Definition of Done (2026-08-30, user chốt trực tiếp).** Vault: `/mnt/d/Obsidian/MyKnowledgeVault`. Sau khi xong **và verify** mỗi task, trước khi viết report: tự đánh giá task có sinh ra knowledge lâu dài không; có thì cập nhật vault qua **transaction workflow chính thức** của claude-obsidian (không Write/Edit tay), **update note đã có thay vì tạo duplicate**, và giữ knowledge của dự án này tách biệt với các dự án khác trong cùng vault (tiền tố tên `Android Course Web`, mọi trang liên kết về `Android Course Web - Project Overview`). Không lưu transcript / log tạm / thay đổi nhỏ không có giá trị dài hạn. Chi tiết quy trình → `CLAUDE.md` mục G. **Trang hiện có trong vault (ingest 2026-08-30):** `Android Course Web - Project Overview` (`c-000001`), `Android Course Web Course Data Model` (`c-000002`), `Android Course Web Lesson Authoring Rules` (`c-000003`), `Android Course Web Frontend Architecture` (`c-000004`). **Chính rule này đã được ghi vào vault** bằng operation `save-bcv-dod-rule-001` (update trang `c-000001`, thêm 1 section mới, không tạo trang trùng) — vault hiện có 14 source / 22 claim, `lint` báo `issues_found: 0`.

## Vấn đề / rủi ro đang theo dõi

- ✅ **[Đã xử lý] Giấy phép `aaf-materials/` cấm dùng cho mục đích giảng dạy.** Phát hiện 2026-08-26 khi chuẩn bị Task 7: file `aaf-materials/LICENSE` (và header lặp lại trong 404/495 file `.kt`) có điều khoản cấm dùng code cho "any work... designed, intended, or marketed for pedagogical or instructional purposes related to programming". Chi tiết → `docs/SOURCE_MAP.md` mục 2.5. **User đã quyết định (Quyết định #7): tiếp tục dùng code thật**, chấp nhận rủi ro vì là dự án cá nhân đã mua bản quyền, không public. Không còn là blocker — Task 7 tiếp tục bình thường.
- **Ảnh minh hoạ trong sách chưa được copy ra ngoài.** `scripts/extract_epub.py` chỉ chèn link `graphics/imgNN.png` (đường dẫn tương đối bên trong EPUB) kèm comment ghi chú, chưa copy file ảnh thật ra `content/book/`. Lesson Chapter 5 (Task 7) hoá ra không cần ảnh (chỉ giải thích + code), nên chưa xử lý. Cần quyết định trước khi có lesson nào thật sự cần hiển thị ảnh/screenshot từ sách.


- **Version công cụ trong sách cũ hơn hiện tại** (đã xác minh 2026-08-26; **số AGP đã sửa lại 2026-09-12**): sách dùng Kotlin 1.9.10 / AGP 8.2.0 / Room 2.5.2; hiện tại bản ổn định là Kotlin 2.4.x / **AGP 9.4.0** / Room 2.8.4. Khi viết lesson liên quan phải **ghi chú rõ chênh lệch**, không âm thầm đổi nội dung sách.

  > **Vì sao sửa:** dòng này trước ghi "AGP 9.2.0", mâu thuẫn với chính mục "TRẠNG THÁI CUỐI CÙNG" ở đầu file (đã reconcile AGP **9.4.0** ngày 2026-09-11) và với bảng tra cứu AP3 trong bài học (`BangTraCuuNhanh.astro` dòng 74: "Dòng ổn định hiện hành **9.4.0** (09/2026)"). Đây là mục **đang theo dõi** nên phải khớp thực tế; các nhắc "AGP 9.3" ở Task 40/191 là ghi chép lịch sử tại thời điểm 2026-08-28 nên **giữ nguyên**.
- **Sách dùng DI thủ công, không Hilt/Koin/Dagger** trong toàn bộ 11 chapter — là lựa chọn sư phạm có chủ đích (giảm phức tạp cho người mới), khác khuyến nghị chính thức của Android cho app production. Cần ghi chú khi dạy phần kiến trúc/DI.
- ✅ **[Đã xử lý ở Task 33] Project gốc chưa có git ở cấp root** — đã `git init -b main` 2026-08-27, commit đầu `9e8800d` (50 file). `.gitignore` chặn `.epub` + `content/book/` (bản quyền) và `aaf-materials/` (repo riêng). **Không tạo remote, không push** — git ở đây chỉ để lưu lịch sử trên máy.
- ✅ **[Đã xử lý ở Task 32] Source/artifact lồng và file tạm trong `web/`** (phát hiện khi audit 2026-08-27, xử lý 2026-08-27): `web/web/`, `test-minimal.astro`, 14 file log/script tạm, file 0 byte `',`, `dist`+`node_modules` lạc trong `web/src/components/lessons/` — đã xoá sau khi salvage phần nội dung thật sự tốt hơn (quiz Chapter 3, 4 câu) từ `web/web/`. Bài học giữ lại: **mô tả artifact trong file kế hoạch phải được kiểm lại bằng cách mở file ra xem trước khi xoá** — 2 mô tả cũ của chính task này đều sai (xem Task 32).
- ✅ **[Đã xử lý ở Task 34] Cảnh báo Node khi `npm install`** — nguyên nhân là `undici@8.10.0` (qua `astro -> unifont`) yêu cầu Node >= 22.19.0. Đã nâng từ 22.14.0 → **22.23.2** bằng `nvm-windows` có sẵn; clean install lại từ đầu → **0 dòng `npm warn`**. Lưu ý còn tồn tại: máy có 2 bản Node trong PATH (nvm ở `/c/nvm4w/nodejs`, bản cài rời ở `/c/Program Files/nodejs` = v22.22.0); bản nvm thắng. Nếu một ngày `node -v` bỗng hiện số khác, kiểm tra `nvm list` và `which -a node` trước khi kết luận.
- ✅ **[Đã xử lý] Task 12 và Task 17 từng dở dù route đã render** — cả hai đã hoàn thành 2026-08-27 (citation Chapter 2 đủ số dòng; lesson Chapter 8 đủ 6 mục). Vẫn giữ nguyên tắc: không được hiểu “route HTTP 200” là “lesson hoàn thành” — luôn kiểm tra nội dung thật.
- **File scratch trong `web/` chưa được `.gitignore` che (từ Task 46).** 11 file ở `web/` root (`.checks.txt`, `.checks2.txt`, `.checks3.txt`, `.classsamples.txt`, `.markup.txt`, `.styledump.css`, `.styledump.css.selectors.txt`, `.uibaseline-content-before/after/final.txt`) là output tạm của các script verify, `git status` đang thấy chúng là untracked. Cộng thêm `web/scripts/` (82 `.py` + 8 `.mjs`, **build-time only, không ship vào `dist`**) và `web/content-baseline.txt` (bằng chứng nội dung không đổi — **nên giữ**). Trước khi commit nên thêm rule ignore cho nhóm scratch, hoặc xoá chúng; **chưa tự làm** vì nguyên tắc "cần xoá file thì phải hỏi trước".
- **Còn 2 chỗ trộn tiếng Anh–Việt ở `Ch01WelcomeToAndroidKotlin.astro` (từ Task 47).** Dòng 1449-1450 và 1466 còn viết "chapter" cạnh phần đã đổi thành "Chương". Nói về chapter khác 10 nên ngoài phạm vi Task 47; xử lý khi tới lượt Ch01 trong Task 48.
- **Kế hoạch triển khai Task 11** được lưu tại `docs/superpowers/plans/2026-08-27-chapter-1-lesson.md` để tham khảo lại nếu cần, không phải tài liệu bắt buộc đọc cho các task khác.

## Hướng mở rộng nếu user muốn tiếp

Phase 7 đang mở nên roadmap **còn task** (Task 48). Những việc dưới đây **chưa được yêu cầu, chưa lên kế hoạch** — ghi ra để lần sau khỏi phải nghĩ lại từ đầu. Xếp theo mức hữu ích so với công sức:

1. ✅ **[Đã làm ở Task 46] Ô tìm kiếm trong website.** Đã có trong header: tìm theo tên chapter **và theo từng mục `h2` thật** của lesson, bỏ dấu tiếng Việt khi so khớp (gõ `du lieu` ra `dữ liệu`), điều hướng bằng ↑↓/Enter, `Ctrl/Cmd + K` để nhảy vào ô tìm. **Không dùng Pagefind** như ghi chú cũ đề xuất — index được render sẵn thành `<script type="application/json">` lúc build nên **không thêm dependency nào**.
2. **Copy ảnh/screenshot từ sách ra `content/book/`.** Hiện `scripts/extract_epub.py` chỉ chèn link tương đối trong EPUB, chưa copy file ảnh (xem mục rủi ro ở trên). Cần khi nào có lesson thật sự phải xem ảnh — ví dụ hướng dẫn giao diện Android Studio ở Chapter 2.
3. **Lưu tiến độ học bền hơn.** Hiện quiz làm xong tải lại trang là mất, tiến độ chapter lưu trong `localStorage` (mất nếu xoá dữ liệu trình duyệt). Có thể lưu điểm quiz vào `localStorage` luôn — nhỏ, làm được nhanh.
4. **Trang tổng hợp "khác biệt sách vs Android hiện nay".** Ghi chú phiên bản hiện rải rác trong cả 11 lesson, dày nhất ở Ch11 (3 mục con: security-crypto khai tử, SQLCipher đổi artifact, Room 3 bỏ `openHelperFactory`), Ch02 (giao diện Android Studio) và Ch09. Gom thành 1 trang tra cứu sẽ tiện khi đọc lại. *(Con số "28 điểm" ghi ở bản kế hoạch trước là ước lượng chưa kiểm — nếu làm task này thì phải đếm lại từ file thật.)*
5. **Sổ tay Kotlin cơ bản.** Sách giả định người đọc đã biết Kotlin. Nếu học thấy vướng cú pháp (`val`/`var`, lambda, `data class`), thêm 1 trang phụ lục — **lưu ý: phải viết mới, không lấy từ sách** vì sách không dạy phần này.

Không nên làm nếu không có lý do rõ: đăng website lên internet (vướng bản quyền), thêm đăng nhập/cloud database, viết lại bằng framework khác.

## Ghi chú cho session mới

Nếu nghi ngờ nội dung file này đã cũ so với code thực tế (ví dụ: thấy `web/` đã tồn tại nhưng ở đây ghi "chưa tạo"), **kiểm tra project thật trước, báo lệch cho user, rồi sửa lại file này** — không tự ý tin theo một trong hai nguồn mà không kiểm tra.
